//! Generated by RustGen.  Do not edit by hand.
//!
//! One `pub fn` per ScalaScript top-level `def`.  Calls to console
//! intrinsics (`println`, `print`) route to `crate::runtime::_*`.
//! `rust` fence blocks from the source are appended verbatim.

#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct Diagnostic {
    pub code: String,
    pub message: String,
    pub severity: Severity,
    pub span: Option<SourceSpan>,
    pub dialect: Option<String>,
    pub details: Vec<(String, String)>,
}
impl From<Diagnostic> for crate::value::Value {
    fn from(x: Diagnostic) -> crate::value::Value {
        crate::value::Value::Obj("Diagnostic", vec![crate::value::Value::from(x.code), crate::value::Value::from(x.message), crate::value::Value::from(x.severity), crate::value::Value::from(x.span), crate::value::Value::from(x.dialect), crate::value::Value::from(x.details)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct SourceId {
    pub value: String,
}
impl From<SourceId> for crate::value::Value {
    fn from(x: SourceId) -> crate::value::Value {
        crate::value::Value::Obj("SourceId", vec![crate::value::Value::from(x.value)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct SourcePosition {
    pub offset: i64,
    pub line: i64,
    pub column: i64,
}
impl From<SourcePosition> for crate::value::Value {
    fn from(x: SourcePosition) -> crate::value::Value {
        crate::value::Value::Obj("SourcePosition", vec![crate::value::Value::from(x.offset), crate::value::Value::from(x.line), crate::value::Value::from(x.column)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct SourceSpan {
    pub source: SourceId,
    pub start: SourcePosition,
    pub end: SourcePosition,
}
impl From<SourceSpan> for crate::value::Value {
    fn from(x: SourceSpan) -> crate::value::Value {
        crate::value::Value::Obj("SourceSpan", vec![crate::value::Value::from(x.source), crate::value::Value::from(x.start), crate::value::Value::from(x.end)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct SourceChunk {
    pub text: String,
}
impl From<SourceChunk> for crate::value::Value {
    fn from(x: SourceChunk) -> crate::value::Value {
        crate::value::Value::Obj("SourceChunk", vec![crate::value::Value::from(x.text)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct SourceInput {
    pub source: SourceId,
    pub chunks: Vec<SourceChunk>,
}
impl From<SourceInput> for crate::value::Value {
    fn from(x: SourceInput) -> crate::value::Value {
        crate::value::Value::Obj("SourceInput", vec![crate::value::Value::from(x.source), crate::value::Value::from(x.chunks)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct SourceToken {
    pub id: i64,
    pub kind: String,
    pub lexeme: String,
    pub span: SourceSpan,
    pub channel: TokenChannel,
}
impl From<SourceToken> for crate::value::Value {
    fn from(x: SourceToken) -> crate::value::Value {
        crate::value::Value::Obj("SourceToken", vec![crate::value::Value::from(x.id), crate::value::Value::from(x.kind), crate::value::Value::from(x.lexeme), crate::value::Value::from(x.span), crate::value::Value::from(x.channel)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct UniEdge {
    pub role: Option<String>,
    pub child: UniNode,
}
impl From<UniEdge> for crate::value::Value {
    fn from(x: UniEdge) -> crate::value::Value {
        crate::value::Value::Obj("UniEdge", vec![crate::value::Value::from(x.role), crate::value::Value::from(x.child)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ProcessBatch<A> {
    pub values: Vec<A>,
    pub diagnostics: Vec<Diagnostic>,
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct Stepped<S, O> {
    pub state: S,
    pub batch: ProcessBatch<O>,
}
#[allow(dead_code)]
#[derive(Clone)]
pub struct DialectRegistry {
    pub byName: std::collections::HashMap<String, std::rc::Rc<dyn DialectAdapter>>,
}
impl std::fmt::Debug for DialectRegistry {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.write_str("DialectRegistry")
    }
}
impl From<DialectRegistry> for crate::value::Value {
    fn from(x: DialectRegistry) -> crate::value::Value {
        crate::value::Value::Obj("DialectRegistry", vec![crate::value::Value::Unit])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct LiteralProcessor {
    pub source: SourceId,
}
impl From<LiteralProcessor> for crate::value::Value {
    fn from(x: LiteralProcessor) -> crate::value::Value {
        crate::value::Value::Obj("LiteralProcessor", vec![crate::value::Value::from(x.source)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct FrameSpec {
    pub kind: String,
    pub role: Option<String>,
}
impl From<FrameSpec> for crate::value::Value {
    fn from(x: FrameSpec) -> crate::value::Value {
        crate::value::Value::Obj("FrameSpec", vec![crate::value::Value::from(x.kind), crate::value::Value::from(x.role)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct VmToken {
    pub token: SourceToken,
    pub instruction: VmInstruction,
}
impl From<VmToken> for crate::value::Value {
    fn from(x: VmToken) -> crate::value::Value {
        crate::value::Value::Obj("VmToken", vec![crate::value::Value::from(x.token), crate::value::Value::from(x.instruction)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct Limits {
    pub maxDepth: i64,
    pub maxNodes: i64,
    pub maxTokenCodePoints: i64,
    pub maxDiagnostics: i64,
}
impl From<Limits> for crate::value::Value {
    fn from(x: Limits) -> crate::value::Value {
        crate::value::Value::Obj("Limits", vec![crate::value::Value::from(x.maxDepth), crate::value::Value::from(x.maxNodes), crate::value::Value::from(x.maxTokenCodePoints), crate::value::Value::from(x.maxDiagnostics)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct VmFrame {
    pub kind: String,
    pub role: Option<String>,
    pub edges: Vec<UniEdge>,
    pub openingSpan: SourceSpan,
}
impl From<VmFrame> for crate::value::Value {
    fn from(x: VmFrame) -> crate::value::Value {
        crate::value::Value::Obj("VmFrame", vec![crate::value::Value::from(x.kind), crate::value::Value::from(x.role), crate::value::Value::from(x.edges), crate::value::Value::from(x.openingSpan)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct VmState {
    pub stack: Vec<VmFrame>,
    pub nodeCount: i64,
    pub lastTokenId: Option<i64>,
    pub diagnosticCount: i64,
    pub diagnosticLimitReported: bool,
    pub finished: bool,
    pub halted: bool,
}
impl From<VmState> for crate::value::Value {
    fn from(x: VmState) -> crate::value::Value {
        crate::value::Value::Obj("VmState", vec![crate::value::Value::from(x.stack), crate::value::Value::from(x.nodeCount), crate::value::Value::from(x.lastTokenId), crate::value::Value::from(x.diagnosticCount), crate::value::Value::from(x.diagnosticLimitReported), crate::value::Value::from(x.finished), crate::value::Value::from(x.halted)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct TreeVm {
    pub limits: Limits,
}
impl From<TreeVm> for crate::value::Value {
    fn from(x: TreeVm) -> crate::value::Value {
        crate::value::Value::Obj("TreeVm", vec![crate::value::Value::from(x.limits)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ParseResult {
    pub roots: Vec<UniNode>,
    pub diagnostics: Vec<Diagnostic>,
    pub status: CompletionStatus,
}
impl From<ParseResult> for crate::value::Value {
    fn from(x: ParseResult) -> crate::value::Value {
        crate::value::Value::Obj("ParseResult", vec![crate::value::Value::from(x.roots), crate::value::Value::from(x.diagnostics), crate::value::Value::from(x.status)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MarkdownLimits {
    pub core: Limits,
    pub maxSourceCodePoints: i64,
    pub maxLineCodePoints: i64,
    pub maxDelimiterRun: i64,
    pub maxFenceCodePoints: i64,
    pub maxReferences: i64,
    pub maxBlocks: i64,
}
impl From<MarkdownLimits> for crate::value::Value {
    fn from(x: MarkdownLimits) -> crate::value::Value {
        crate::value::Value::Obj("MarkdownLimits", vec![crate::value::Value::from(x.core), crate::value::Value::from(x.maxSourceCodePoints), crate::value::Value::from(x.maxLineCodePoints), crate::value::Value::from(x.maxDelimiterRun), crate::value::Value::from(x.maxFenceCodePoints), crate::value::Value::from(x.maxReferences), crate::value::Value::from(x.maxBlocks)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct TableCell {
    pub inlines: Vec<MarkdownInline>,
}
impl From<TableCell> for crate::value::Value {
    fn from(x: TableCell) -> crate::value::Value {
        crate::value::Value::Obj("TableCell", vec![crate::value::Value::from(x.inlines)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ListItem {
    pub blocks: Vec<MarkdownBlock>,
    pub task: Option<bool>,
}
impl From<ListItem> for crate::value::Value {
    fn from(x: ListItem) -> crate::value::Value {
        crate::value::Value::Obj("ListItem", vec![crate::value::Value::from(x.blocks), crate::value::Value::from(x.task)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MarkdownDocument {
    pub blocks: Vec<MarkdownBlock>,
    pub references: Vec<MarkdownBlock>,
}
impl From<MarkdownDocument> for crate::value::Value {
    fn from(x: MarkdownDocument) -> crate::value::Value {
        crate::value::Value::Obj("MarkdownDocument", vec![crate::value::Value::from(x.blocks), crate::value::Value::from(x.references)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MarkdownProjectionResult {
    pub document: Option<MarkdownDocument>,
    pub diagnostics: Vec<Diagnostic>,
}
impl From<MarkdownProjectionResult> for crate::value::Value {
    fn from(x: MarkdownProjectionResult) -> crate::value::Value {
        crate::value::Value::Obj("MarkdownProjectionResult", vec![crate::value::Value::from(x.document), crate::value::Value::from(x.diagnostics)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct LinkRef {
    pub destination: String,
    pub title: Option<String>,
}
impl From<LinkRef> for crate::value::Value {
    fn from(x: LinkRef) -> crate::value::Value {
        crate::value::Value::Obj("LinkRef", vec![crate::value::Value::from(x.destination), crate::value::Value::from(x.title)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct DestTitleSpans {
    pub destStart: i64,
    pub destEnd: i64,
    pub titleStart: i64,
    pub titleEnd: i64,
    pub closeStart: i64,
}
impl From<DestTitleSpans> for crate::value::Value {
    fn from(x: DestTitleSpans) -> crate::value::Value {
        crate::value::Value::Obj("DestTitleSpans", vec![crate::value::Value::from(x.destStart), crate::value::Value::from(x.destEnd), crate::value::Value::from(x.titleStart), crate::value::Value::from(x.titleEnd), crate::value::Value::from(x.closeStart)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MarkdownBlockResult {
    pub tokens: Vec<VmToken>,
    pub diagnostics: Vec<Diagnostic>,
}
impl From<MarkdownBlockResult> for crate::value::Value {
    fn from(x: MarkdownBlockResult) -> crate::value::Value {
        crate::value::Value::Obj("MarkdownBlockResult", vec![crate::value::Value::from(x.tokens), crate::value::Value::from(x.diagnostics)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ParaSeg {
    pub prefix: String,
    pub content: String,
    pub ending: String,
}
impl From<ParaSeg> for crate::value::Value {
    fn from(x: ParaSeg) -> crate::value::Value {
        crate::value::Value::Obj("ParaSeg", vec![crate::value::Value::from(x.prefix), crate::value::Value::from(x.content), crate::value::Value::from(x.ending)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct RefDef {
    pub indent: String,
    pub labelLex: String,
    pub colon: String,
    pub afterColon: String,
    pub destLex: String,
    pub betweenDestTitle: String,
    pub titleLex: String,
    pub trailing: String,
    pub linesConsumed: i64,
}
impl From<RefDef> for crate::value::Value {
    fn from(x: RefDef) -> crate::value::Value {
        crate::value::Value::Obj("RefDef", vec![crate::value::Value::from(x.indent), crate::value::Value::from(x.labelLex), crate::value::Value::from(x.colon), crate::value::Value::from(x.afterColon), crate::value::Value::from(x.destLex), crate::value::Value::from(x.betweenDestTitle), crate::value::Value::from(x.titleLex), crate::value::Value::from(x.trailing), crate::value::Value::from(x.linesConsumed)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MdLine {
    pub content: String,
    pub ending: String,
}
impl From<MdLine> for crate::value::Value {
    fn from(x: MdLine) -> crate::value::Value {
        crate::value::Value::Obj("MdLine", vec![crate::value::Value::from(x.content), crate::value::Value::from(x.ending)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ConfiguredMarkdownDialect {
    pub profile: MarkdownProfile,
    pub limits: MarkdownLimits,
}
impl From<ConfiguredMarkdownDialect> for crate::value::Value {
    fn from(x: ConfiguredMarkdownDialect) -> crate::value::Value {
        crate::value::Value::Obj("ConfiguredMarkdownDialect", vec![crate::value::Value::from(x.profile), crate::value::Value::from(x.limits)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct MarkdownInstructionProcessor {
    pub source: SourceId,
    pub profile: MarkdownProfile,
    pub limits: MarkdownLimits,
}
impl From<MarkdownInstructionProcessor> for crate::value::Value {
    fn from(x: MarkdownInstructionProcessor) -> crate::value::Value {
        crate::value::Value::Obj("MarkdownInstructionProcessor", vec![crate::value::Value::from(x.source), crate::value::Value::from(x.profile), crate::value::Value::from(x.limits)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct RustLexToken {
    pub kind: String,
    pub lexeme: String,
}
impl From<RustLexToken> for crate::value::Value {
    fn from(x: RustLexToken) -> crate::value::Value {
        crate::value::Value::Obj("RustLexToken", vec![crate::value::Value::from(x.kind), crate::value::Value::from(x.lexeme)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct WindowLex {
    pub tokens: Vec<RustLexToken>,
    pub consumed: i64,
}
impl From<WindowLex> for crate::value::Value {
    fn from(x: WindowLex) -> crate::value::Value {
        crate::value::Value::Obj("WindowLex", vec![crate::value::Value::from(x.tokens), crate::value::Value::from(x.consumed)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct ItemSpan {
    pub start: i64,
    pub end: i64,
    pub kind: String,
    pub name: String,
    pub bodyOpen: i64,
}
impl From<ItemSpan> for crate::value::Value {
    fn from(x: ItemSpan) -> crate::value::Value {
        crate::value::Value::Obj("ItemSpan", vec![crate::value::Value::from(x.start), crate::value::Value::from(x.end), crate::value::Value::from(x.kind), crate::value::Value::from(x.name), crate::value::Value::from(x.bodyOpen)])
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub struct RustProcessor {
    pub source: SourceId,
}
impl From<RustProcessor> for crate::value::Value {
    fn from(x: RustProcessor) -> crate::value::Value {
        crate::value::Value::Obj("RustProcessor", vec![crate::value::Value::from(x.source)])
    }
}
#[allow(dead_code)]
pub struct MarkdownBlocks {
    pub source: SourceId,
    pub profile: MarkdownProfile,
    pub limits: MarkdownLimits,
    pub gfm: bool,
    pub scala: bool,
    pub htmlBlock6Tags: Vec<String>,
    pub htmlType1Tags: Vec<String>,
}

impl MarkdownBlocks {
    fn new(source: SourceId, profile: MarkdownProfile, limits: MarkdownLimits) -> MarkdownBlocks {
        let gfm = (profile == MarkdownProfile::Gfm);
        let scala = (profile == MarkdownProfile::ScalaScript);
        let htmlBlock6Tags = vec!["address".to_string(), "article".to_string(), "aside".to_string(), "base".to_string(), "basefont".to_string(), "blockquote".to_string(), "body".to_string(), "caption".to_string(), "center".to_string(), "col".to_string(), "colgroup".to_string(), "dd".to_string(), "details".to_string(), "dialog".to_string(), "dir".to_string(), "div".to_string(), "dl".to_string(), "dt".to_string(), "fieldset".to_string(), "figcaption".to_string(), "figure".to_string(), "footer".to_string(), "form".to_string(), "frame".to_string(), "frameset".to_string(), "h1".to_string(), "h2".to_string(), "h3".to_string(), "h4".to_string(), "h5".to_string(), "h6".to_string(), "head".to_string(), "header".to_string(), "hr".to_string(), "html".to_string(), "iframe".to_string(), "legend".to_string(), "li".to_string(), "link".to_string(), "main".to_string(), "menu".to_string(), "menuitem".to_string(), "nav".to_string(), "noframes".to_string(), "ol".to_string(), "optgroup".to_string(), "option".to_string(), "p".to_string(), "param".to_string(), "section".to_string(), "summary".to_string(), "table".to_string(), "tbody".to_string(), "td".to_string(), "tfoot".to_string(), "th".to_string(), "thead".to_string(), "title".to_string(), "tr".to_string(), "track".to_string(), "ul".to_string()];
        let htmlType1Tags = vec!["script".to_string(), "pre".to_string(), "style".to_string(), "textarea".to_string()];
        MarkdownBlocks { source, profile, limits, gfm, scala, htmlBlock6Tags, htmlType1Tags }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone)]
pub enum Either<L, R> {
    Left(L),
    Right(R),
}
// The same lift every user enum gets (`enumLift`), which this one was missing: a struct
// field holding an Either emitted `Value::from(x.result)` and rustc answered
// `Value: From<Either<VfsError, i64>> is not satisfied` (std/scljet/index.ssc). Generic
// because this enum is, and positional-with-ctor-name like every other Obj.
impl<L: Into<crate::value::Value>, R: Into<crate::value::Value>> From<Either<L, R>> for crate::value::Value {
    fn from(x: Either<L, R>) -> crate::value::Value {
        match x {
            Either::Left(v)  => crate::value::Value::Obj("Left",  vec![v.into()]),
            Either::Right(v) => crate::value::Value::Obj("Right", vec![v.into()]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum Severity {
    Info,
    Warning,
    Error,
    Fatal,
}
impl From<Severity> for crate::value::Value {
    fn from(x: Severity) -> crate::value::Value {
        match x {
            Severity::Info => crate::value::Value::Obj("Info", vec![]),
            Severity::Warning => crate::value::Value::Obj("Warning", vec![]),
            Severity::Error => crate::value::Value::Obj("Error", vec![]),
            Severity::Fatal => crate::value::Value::Obj("Fatal", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum CompletionStatus {
    Complete,
    Incomplete,
    Halted,
}
impl From<CompletionStatus> for crate::value::Value {
    fn from(x: CompletionStatus) -> crate::value::Value {
        match x {
            CompletionStatus::Complete => crate::value::Value::Obj("Complete", vec![]),
            CompletionStatus::Incomplete => crate::value::Value::Obj("Incomplete", vec![]),
            CompletionStatus::Halted => crate::value::Value::Obj("Halted", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum TokenChannel {
    Syntax,
    Trivia,
    Comment,
    Embedded,
    Error,
}
impl From<TokenChannel> for crate::value::Value {
    fn from(x: TokenChannel) -> crate::value::Value {
        match x {
            TokenChannel::Syntax => crate::value::Value::Obj("Syntax", vec![]),
            TokenChannel::Trivia => crate::value::Value::Obj("Trivia", vec![]),
            TokenChannel::Comment => crate::value::Value::Obj("Comment", vec![]),
            TokenChannel::Embedded => crate::value::Value::Obj("Embedded", vec![]),
            TokenChannel::Error => crate::value::Value::Obj("Error", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum Origin {
    SourceBacked,
    Synthetic { reason: String },
}
impl From<Origin> for crate::value::Value {
    fn from(x: Origin) -> crate::value::Value {
        match x {
            Origin::SourceBacked => crate::value::Value::Obj("SourceBacked", vec![]),
            Origin::Synthetic { reason } => crate::value::Value::Obj("Synthetic", vec![Into::<crate::value::Value>::into(reason)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum UniNode {
    Branch { kind: String, edges: Vec<UniEdge>, span: SourceSpan, origin: Origin },
    Token { value: SourceToken },
}
impl From<UniNode> for crate::value::Value {
    fn from(x: UniNode) -> crate::value::Value {
        match x {
            UniNode::Branch { kind, edges, span, origin } => crate::value::Value::Obj("Branch", vec![Into::<crate::value::Value>::into(kind), Into::<crate::value::Value>::into(edges), Into::<crate::value::Value>::into(span), Into::<crate::value::Value>::into(origin)]),
            UniNode::Token { value } => crate::value::Value::Obj("Token", vec![Into::<crate::value::Value>::into(value)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum Projection {
    Identity { tree: UniNode },
}
impl From<Projection> for crate::value::Value {
    fn from(x: Projection) -> crate::value::Value {
        match x {
            Projection::Identity { tree } => crate::value::Value::Obj("Identity", vec![Into::<crate::value::Value>::into(tree)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum VmInstruction {
    Open { kind: String, role: Option<String> },
    Close { expectedKind: Option<String>, role: Option<String> },
    Emit { role: Option<String> },
    Reframe { closeBefore: Vec<String>, open: Vec<FrameSpec>, closeAfter: Vec<String>, role: Option<String> },
    Report { code: String, message: String, severity: Severity },
}
impl From<VmInstruction> for crate::value::Value {
    fn from(x: VmInstruction) -> crate::value::Value {
        match x {
            VmInstruction::Open { kind, role } => crate::value::Value::Obj("Open", vec![Into::<crate::value::Value>::into(kind), Into::<crate::value::Value>::into(role)]),
            VmInstruction::Close { expectedKind, role } => crate::value::Value::Obj("Close", vec![Into::<crate::value::Value>::into(expectedKind), Into::<crate::value::Value>::into(role)]),
            VmInstruction::Emit { role } => crate::value::Value::Obj("Emit", vec![Into::<crate::value::Value>::into(role)]),
            VmInstruction::Reframe { closeBefore, open, closeAfter, role } => crate::value::Value::Obj("Reframe", vec![Into::<crate::value::Value>::into(closeBefore), Into::<crate::value::Value>::into(open), Into::<crate::value::Value>::into(closeAfter), Into::<crate::value::Value>::into(role)]),
            VmInstruction::Report { code, message, severity } => crate::value::Value::Obj("Report", vec![Into::<crate::value::Value>::into(code), Into::<crate::value::Value>::into(message), Into::<crate::value::Value>::into(severity)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum MarkdownProfile {
    CommonMark,
    Gfm,
    ScalaScript,
}
impl From<MarkdownProfile> for crate::value::Value {
    fn from(x: MarkdownProfile) -> crate::value::Value {
        match x {
            MarkdownProfile::CommonMark => crate::value::Value::Obj("CommonMark", vec![]),
            MarkdownProfile::Gfm => crate::value::Value::Obj("Gfm", vec![]),
            MarkdownProfile::ScalaScript => crate::value::Value::Obj("ScalaScript", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum ColumnAlignment {
    Default,
    Left,
    Center,
    Right,
}
impl From<ColumnAlignment> for crate::value::Value {
    fn from(x: ColumnAlignment) -> crate::value::Value {
        match x {
            ColumnAlignment::Default => crate::value::Value::Obj("Default", vec![]),
            ColumnAlignment::Left => crate::value::Value::Obj("Left", vec![]),
            ColumnAlignment::Center => crate::value::Value::Obj("Center", vec![]),
            ColumnAlignment::Right => crate::value::Value::Obj("Right", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum MarkdownBlock {
    Paragraph { inlines: Vec<MarkdownInline> },
    Heading { level: i64, inlines: Vec<MarkdownInline>, setext: bool },
    ThematicBreak,
    BlockQuote { blocks: Vec<MarkdownBlock> },
    ListBlock { ordered: bool, start: Option<i64>, tight: bool, items: Vec<ListItem> },
    CodeBlock { info: Option<String>, literal: String, fenced: bool },
    HtmlBlock { raw: String },
    LinkDefinition { label: String, destination: String, title: Option<String> },
    Table { header: Vec<TableCell>, alignments: Vec<ColumnAlignment>, rows: Vec<Vec<TableCell>> },
}
impl From<MarkdownBlock> for crate::value::Value {
    fn from(x: MarkdownBlock) -> crate::value::Value {
        match x {
            MarkdownBlock::Paragraph { inlines } => crate::value::Value::Obj("Paragraph", vec![Into::<crate::value::Value>::into(inlines)]),
            MarkdownBlock::Heading { level, inlines, setext } => crate::value::Value::Obj("Heading", vec![Into::<crate::value::Value>::into(level), Into::<crate::value::Value>::into(inlines), Into::<crate::value::Value>::into(setext)]),
            MarkdownBlock::ThematicBreak => crate::value::Value::Obj("ThematicBreak", vec![]),
            MarkdownBlock::BlockQuote { blocks } => crate::value::Value::Obj("BlockQuote", vec![Into::<crate::value::Value>::into(blocks)]),
            MarkdownBlock::ListBlock { ordered, start, tight, items } => crate::value::Value::Obj("ListBlock", vec![Into::<crate::value::Value>::into(ordered), Into::<crate::value::Value>::into(start), Into::<crate::value::Value>::into(tight), Into::<crate::value::Value>::into(items)]),
            MarkdownBlock::CodeBlock { info, literal, fenced } => crate::value::Value::Obj("CodeBlock", vec![Into::<crate::value::Value>::into(info), Into::<crate::value::Value>::into(literal), Into::<crate::value::Value>::into(fenced)]),
            MarkdownBlock::HtmlBlock { raw } => crate::value::Value::Obj("HtmlBlock", vec![Into::<crate::value::Value>::into(raw)]),
            MarkdownBlock::LinkDefinition { label, destination, title } => crate::value::Value::Obj("LinkDefinition", vec![Into::<crate::value::Value>::into(label), Into::<crate::value::Value>::into(destination), Into::<crate::value::Value>::into(title)]),
            MarkdownBlock::Table { header, alignments, rows } => crate::value::Value::Obj("Table", vec![Into::<crate::value::Value>::into(header), Into::<crate::value::Value>::into(alignments), Into::<crate::value::Value>::into(rows)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum MarkdownInline {
    Text { value: String },
    Emphasis { children: Vec<MarkdownInline> },
    Strong { children: Vec<MarkdownInline> },
    Strikethrough { children: Vec<MarkdownInline> },
    Code { value: String },
    Link { label: Vec<MarkdownInline>, destination: String, title: Option<String> },
    Image { alt: Vec<MarkdownInline>, destination: String, title: Option<String> },
    Autolink { destination: String, label: String },
    RawHtml { raw: String },
    SoftBreak,
    HardBreak,
    Expression { source: String },
}
impl From<MarkdownInline> for crate::value::Value {
    fn from(x: MarkdownInline) -> crate::value::Value {
        match x {
            MarkdownInline::Text { value } => crate::value::Value::Obj("Text", vec![Into::<crate::value::Value>::into(value)]),
            MarkdownInline::Emphasis { children } => crate::value::Value::Obj("Emphasis", vec![Into::<crate::value::Value>::into(children)]),
            MarkdownInline::Strong { children } => crate::value::Value::Obj("Strong", vec![Into::<crate::value::Value>::into(children)]),
            MarkdownInline::Strikethrough { children } => crate::value::Value::Obj("Strikethrough", vec![Into::<crate::value::Value>::into(children)]),
            MarkdownInline::Code { value } => crate::value::Value::Obj("Code", vec![Into::<crate::value::Value>::into(value)]),
            MarkdownInline::Link { label, destination, title } => crate::value::Value::Obj("Link", vec![Into::<crate::value::Value>::into(label), Into::<crate::value::Value>::into(destination), Into::<crate::value::Value>::into(title)]),
            MarkdownInline::Image { alt, destination, title } => crate::value::Value::Obj("Image", vec![Into::<crate::value::Value>::into(alt), Into::<crate::value::Value>::into(destination), Into::<crate::value::Value>::into(title)]),
            MarkdownInline::Autolink { destination, label } => crate::value::Value::Obj("Autolink", vec![Into::<crate::value::Value>::into(destination), Into::<crate::value::Value>::into(label)]),
            MarkdownInline::RawHtml { raw } => crate::value::Value::Obj("RawHtml", vec![Into::<crate::value::Value>::into(raw)]),
            MarkdownInline::SoftBreak => crate::value::Value::Obj("SoftBreak", vec![]),
            MarkdownInline::HardBreak => crate::value::Value::Obj("HardBreak", vec![]),
            MarkdownInline::Expression { source } => crate::value::Value::Obj("Expression", vec![Into::<crate::value::Value>::into(source)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum InlinePiece {
    Tok { kind: String, lexeme: String, role: Option<String>, channel: TokenChannel },
    Open { branch: String, kind: String, lexeme: String, role: Option<String> },
    Close { branch: String, kind: String, lexeme: String, role: Option<String> },
}
impl From<InlinePiece> for crate::value::Value {
    fn from(x: InlinePiece) -> crate::value::Value {
        match x {
            InlinePiece::Tok { kind, lexeme, role, channel } => crate::value::Value::Obj("Tok", vec![Into::<crate::value::Value>::into(kind), Into::<crate::value::Value>::into(lexeme), Into::<crate::value::Value>::into(role), Into::<crate::value::Value>::into(channel)]),
            InlinePiece::Open { branch, kind, lexeme, role } => crate::value::Value::Obj("Open", vec![Into::<crate::value::Value>::into(branch), Into::<crate::value::Value>::into(kind), Into::<crate::value::Value>::into(lexeme), Into::<crate::value::Value>::into(role)]),
            InlinePiece::Close { branch, kind, lexeme, role } => crate::value::Value::Obj("Close", vec![Into::<crate::value::Value>::into(branch), Into::<crate::value::Value>::into(kind), Into::<crate::value::Value>::into(lexeme), Into::<crate::value::Value>::into(role)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum AngleKind {
    Autolink,
    Html,
}
impl From<AngleKind> for crate::value::Value {
    fn from(x: AngleKind) -> crate::value::Value {
        match x {
            AngleKind::Autolink => crate::value::Value::Obj("Autolink", vec![]),
            AngleKind::Html => crate::value::Value::Obj("Html", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum OpenLeaf {
    None,
    Paragraph,
    FencedCode { char: i64, len: i64 },
    IndentedCode,
}
impl From<OpenLeaf> for crate::value::Value {
    fn from(x: OpenLeaf) -> crate::value::Value {
        match x {
            OpenLeaf::None => crate::value::Value::Obj("None", vec![]),
            OpenLeaf::Paragraph => crate::value::Value::Obj("Paragraph", vec![]),
            OpenLeaf::FencedCode { char, len } => crate::value::Value::Obj("FencedCode", vec![Into::<crate::value::Value>::into(char), Into::<crate::value::Value>::into(len)]),
            OpenLeaf::IndentedCode => crate::value::Value::Obj("IndentedCode", vec![]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum WNode {
    WFixed { pieces: Vec<InlinePiece> },
    WDelim { lexeme: String, ch: i64, canOpen: bool, canClose: bool },
}
impl From<WNode> for crate::value::Value {
    fn from(x: WNode) -> crate::value::Value {
        match x {
            WNode::WFixed { pieces } => crate::value::Value::Obj("WFixed", vec![Into::<crate::value::Value>::into(pieces)]),
            WNode::WDelim { lexeme, ch, canOpen, canClose } => crate::value::Value::Obj("WDelim", vec![Into::<crate::value::Value>::into(lexeme), Into::<crate::value::Value>::into(ch), Into::<crate::value::Value>::into(canOpen), Into::<crate::value::Value>::into(canClose)]),
        }
    }
}
#[allow(dead_code)]
#[derive(Debug, Clone, PartialEq)]
pub enum Container {
    Blockquote,
    ListFrame { ordered: bool },
    ListItemFrame { ordered: bool, contentIndent: i64 },
}
impl From<Container> for crate::value::Value {
    fn from(x: Container) -> crate::value::Value {
        match x {
            Container::Blockquote => crate::value::Value::Obj("Blockquote", vec![]),
            Container::ListFrame { ordered } => crate::value::Value::Obj("ListFrame", vec![Into::<crate::value::Value>::into(ordered)]),
            Container::ListItemFrame { ordered, contentIndent } => crate::value::Value::Obj("ListItemFrame", vec![Into::<crate::value::Value>::into(ordered), Into::<crate::value::Value>::into(contentIndent)]),
        }
    }
}
impl ConfiguredMarkdownDialect {
    pub fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
        let profile = self.profile.clone();
        let limits = self.limits.clone();
        std::rc::Rc::new(MarkdownInstructionProcessor { source: source.source.clone(), profile: profile, limits: limits })
    }
}
impl DialectRegistry {
    pub fn get(&self, id: String) -> Option<std::rc::Rc<dyn DialectAdapter>> {
        let byName = self.byName.clone();
        byName.get(&id).cloned()
    }

    pub fn register(&self, adapter: std::rc::Rc<dyn DialectAdapter>) -> Either<String, DialectRegistry> {
        let byName = self.byName.clone();
        let names = [&(adapter.aliases())[..], &[adapter.id()][..]].concat();
        match names.iter().cloned().find(|__f: &String| (|__x| byName.contains_key(__x))(__f)) {
            Some(duplicate) => Either::Left(format!("dialect name '{}' is already registered", duplicate)),
            None => Either::Right(DialectRegistry { byName: { let mut __m = (byName).clone(); __m.extend(names.iter().cloned().map(|__p0| { (__p0, adapter.clone()) }).collect::<Vec<_>>()); __m } }),
        }
    }
}
impl LiteralProcessor {
    pub fn start(&self) -> String {
        "".to_string()
    }

    pub fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> {
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        Stepped { state: format!("{}{}", state, input.text), batch: ProcessBatch_empty() }
    }

    pub fn stop(&self, text: String) -> ProcessBatch<VmToken> {
        let source = self.source.clone();
        let Start = SourcePosition { offset: 0i64, line: 1i64, column: 1i64 };
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        fn emit(lexeme: String, valid: bool, nextTokenId: &mut i64, out: &mut Vec<VmToken>, position: &mut SourcePosition, source: SourceId) {
            let start = (*position).clone();
            let end = advance(start.clone(), lexeme.clone());
            (*position) = end.clone();
            let token = SourceToken { id: (*nextTokenId).clone(), kind: if valid { "literal.code-point".to_string() } else { "literal.unpaired-surrogate".to_string() }, lexeme: lexeme.clone(), span: SourceSpan { source: source, start: start.clone(), end: end.clone() }, channel: if valid { TokenChannel::Syntax.clone() } else { TokenChannel::Error.clone() } };
            ((*nextTokenId) += 1i64);
            let instruction = if valid { VmInstruction::Emit { role: None } } else { VmInstruction::Report { code: "uniml.literal.unpaired-surrogate".to_string(), message: "input contains an unpaired UTF-16 surrogate".to_string(), severity: Severity::Error } };
            (*out).push(VmToken { token: token, instruction: instruction });
        }
        let mut position = Start.clone();
        let mut nextTokenId = 0i64;
        let mut out: Vec<VmToken> = Vec::new();
        let mut index = 0i64;
        while (index < crate::runtime::_str_length(&text)) {
            let char = crate::runtime::_str_char_at(&text, index);
            if ((isHighSurrogate((char.clone()).0) && ((index + 1i64) < crate::runtime::_str_length(&text))) && isLowSurrogate((crate::runtime::_str_char_at(&text, (index + 1i64))).0)) { emit(format!("{}{}", char, crate::runtime::_str_char_at(&text, (index + 1i64))), true, &mut nextTokenId, &mut out, &mut position, source.clone());
            (index += 2i64); } else { if (isHighSurrogate((char.clone()).0) || isLowSurrogate((char.clone()).0)) { emit((char).to_string(), false, &mut nextTokenId, &mut out, &mut position, source.clone());
            (index += 1i64); } else { emit((char).to_string(), true, &mut nextTokenId, &mut out, &mut position, source.clone());
            (index += 1i64); } }
        };
        ProcessBatch { values: out.clone(), diagnostics: Vec::new() }
    }
}
impl MarkdownBlocks {
    pub fn parse(&self, text: String) -> MarkdownBlockResult {
        let Start = SourcePosition { offset: 0i64, line: 1i64, column: 1i64 };
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        let Indent = "markdown.indent".to_string();
        let LineBreak = "markdown.line-break".to_string();
        let Blank = "markdown.blank".to_string();
        let BlockquoteMarker = "markdown.blockquote-marker".to_string();
        let ListMarker = "markdown.list-marker".to_string();
        let AtxMarker = "markdown.atx-marker".to_string();
        let AtxClose = "markdown.atx-close".to_string();
        let SetextUnderline = "markdown.setext-underline".to_string();
        let ThematicMarker = "markdown.thematic-marker".to_string();
        let FenceOpen = "markdown.fence-open".to_string();
        let FenceClose = "markdown.fence-close".to_string();
        let Info = "markdown.info".to_string();
        let CodeContent = "markdown.code-content".to_string();
        let Html = "markdown.html".to_string();
        let FrontMatterFence = "markdown.front-matter-fence".to_string();
        let Destination = "markdown.destination".to_string();
        let Title = "markdown.title".to_string();
        let ReferenceLabel = "markdown.reference-label".to_string();
        let Colon = "markdown.colon".to_string();
        let MdKind_TableRow = "markdown.table-row".to_string();
        let TableDelim = "markdown.table-delim".to_string();
        let TaskMarker = "markdown.task-marker".to_string();
        let SoftBreak = "markdown.soft-break".to_string();
        let Heading = "markdown.heading".to_string();
        let Paragraph = "markdown.paragraph".to_string();
        let Blockquote = "markdown.blockquote".to_string();
        let List = "markdown.list".to_string();
        let ListItem = "markdown.list-item".to_string();
        let CodeBlock = "markdown.code-block".to_string();
        let HtmlBlock = "markdown.html-block".to_string();
        let Definition = "markdown.definition".to_string();
        let Table = "markdown.table".to_string();
        let ThematicBreak = "markdown.thematic-break".to_string();
        let FrontMatter = "markdown.front-matter".to_string();
        fn emit(kind: String, lexeme: String, instruction: VmInstruction, channel: TokenChannel, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            if lexeme.is_empty() { (); } else { let start = (*pos).clone();
            (*pos) = advance((*pos).clone(), lexeme.clone());
            let token = SourceToken { id: (*nextId).clone(), kind: kind.clone(), lexeme: lexeme.clone(), span: SourceSpan { source: source, start: start, end: (*pos).clone() }, channel: channel.clone() };
            ((*nextId) += 1i64);
            (*out).push(VmToken { token: token, instruction: instruction.clone() });
            track(instruction.clone(), frames); }
        }
        fn track(instruction: VmInstruction, frames: &mut Vec<String>) {
            match instruction {
                VmInstruction::Open { kind: k, role: _ } => { (*frames).push(k); },
                VmInstruction::Close { expectedKind: expected, role: _ } => { if (!(*frames).is_empty() && expected.iter().cloned().all(|__p0| { (__p0 == (*frames)[(*frames).len() - 1].clone()) })) { (*frames) = { let __v = ((*frames)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() }; } else { (); } },
                VmInstruction::Reframe { closeBefore, open: opens, closeAfter, role: _ } => { for _ in closeBefore.iter().cloned() {
                    if !(*frames).is_empty() { (*frames) = { let __v = ((*frames)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() } } else { () };
                };
                for spec in opens.iter().cloned() {
                    (*frames).push(spec.kind.clone());
                };
                for _ in closeAfter.iter().cloned() {
                    if !(*frames).is_empty() { (*frames) = { let __v = ((*frames)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() } } else { () };
                }; },
                _ => { (); },
            }
        }
        fn closeDangling(frames: &mut Vec<String>, open: &mut OpenLeaf, out: &mut Vec<VmToken>) {
            if (!(*frames).is_empty() && !(*out).is_empty()) { let remaining = { let mut __v = (*frames).clone(); __v.reverse(); __v };
            let last = (*out)[(*out).len() - 1].clone();
            let rewritten = match (last.instruction).clone() {
                VmInstruction::Emit { role } => VmInstruction::Reframe { closeBefore: Vec::new(), open: Vec::new(), closeAfter: remaining.clone(), role: role.clone() },
                VmInstruction::Close { expectedKind: expected, role } => VmInstruction::Reframe { closeBefore: Vec::new(), open: Vec::new(), closeAfter: [&(expected.clone().into_iter().collect::<Vec<_>>())[..], &(remaining)[..]].concat(), role: role.clone() },
                VmInstruction::Reframe { closeBefore: cb, open: op, closeAfter: ca, role } => VmInstruction::Reframe { closeBefore: cb, open: op, closeAfter: [&(ca)[..], &(remaining)[..]].concat(), role: role.clone() },
                other => other,
            };
            (*out) = {
                let mut m2 = (*out).clone();
                m2[((((*out).len() as i64) - 1i64)) as usize] = VmToken { instruction: rewritten.clone(), ..(last).clone() };
                m2
              };
            (*frames) = Vec::new(); } else { (); }
        }
        fn leaf(kind: String, lexeme: String, role: Option<String>, channel: TokenChannel, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            emit(kind.clone(), lexeme.clone(), VmInstruction::Emit { role: role.clone() }, channel.clone(), frames, nextId, out, pos, source.clone());
        }
        fn openBranch(branch: String, kind: String, lexeme: String, role: Option<String>, channel: TokenChannel, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            emit(kind.clone(), lexeme.clone(), VmInstruction::Open { kind: branch.clone(), role: role.clone() }, channel.clone(), frames, nextId, out, pos, source.clone());
        }
        fn close(branch: String, kind: String, lexeme: String, role: Option<String>, channel: TokenChannel, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            emit(kind.clone(), lexeme.clone(), VmInstruction::Close { expectedKind: Some(branch.clone()), role: role.clone() }, channel.clone(), frames, nextId, out, pos, source.clone());
        }
        fn processLine(lines: &Vec<MdLine>, index: i64, containers: &mut Vec<Container>, frames: &mut Vec<String>, gfm: bool, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> i64 {
            let line = lines[(index) as usize].clone();
            match (*open) {
                OpenLeaf::FencedCode { char: fchar, len: flen } => { let fenceContent = matchContainers(line.clone(), containers, frames, indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
                handleFenceBody(MdLine { content: fenceContent, ending: line.ending.clone() }, fchar.clone(), flen.clone(), frames, nextId, open, out, pos, source.clone(), __self);
                (index + 1i64) },
                _ => { let content = matchContainers(line.clone(), containers, frames, indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
                let indentWidth = indentWidth(content.clone());
                if content.chars().all(|__ch| { let c = (__ch as u32) as i64; ((c == 32i64) || (c == 9i64)) }) { { handleBlank(line.clone(), content.clone(), frames, indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
                (index + 1i64) } } else { dispatchLeaf(lines, index, line.clone(), content.clone(), indentWidth.clone(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self) } },
            }
        }
        fn dispatchLeaf(lines: &Vec<MdLine>, index: i64, line: MdLine, content: String, indentWidth: i64, containers: &mut Vec<Container>, frames: &mut Vec<String>, gfm: bool, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> i64 {
            let trimmed = crate::runtime::_str_substring_from(&content, indentPrefixLength(content.clone()));
            let isIndentedCode = (((indentWidth >= 4i64) && ((*open) != OpenLeaf::Paragraph)) && !(__self.startsListOrQuote(trimmed.clone())));
            if isIndentedCode { releaseInteriorBlanks(frames, indentedCodeBlanks, nextId, out, pos, source.clone()); } else { finishIndentedCode(frames, indentedCodeBlanks, nextId, open, out, pendingClose, pos, source.clone()); }
            emitIndentedCodePrefix(frames, indentedCodePendingPrefix, nextId, open, out, pendingClose, pos, source.clone());
            if isIndentedCode { { handleIndentedCode(line.clone(), content.clone(), frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            (index + 1i64) } } else { if (((*open) == OpenLeaf::Paragraph) && __self.isSetextUnderline(trimmed.clone())) { { emitSetextUnderline(line.clone(), frames, nextId, open, out, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone());
            (index + 1i64) } } else { if __self.isThematicBreak(trimmed.clone()) { { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            emitThematicBreak(line.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            (index + 1i64) } } else { if __self.startsAtxHeading(trimmed.clone()) { { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            emitAtxHeading(line.clone(), content.clone(), frames, nextId, open, out, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            (index + 1i64) } } else { if __self.startsFence(trimmed.clone()).is_some() { { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            startFence(line.clone(), content.clone(), __self.startsFence(trimmed.clone()).clone().unwrap(), frames, nextId, open, out, pendingClose, pos, source.clone());
            (index + 1i64) } } else { if __self.startsBlockquote(trimmed.clone()) { openBlockquoteAndReprocess(lines, index, line.clone(), content.clone(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self) } else { if __self.startsListItem(trimmed.clone()).is_some() { openListItemAndReprocess(lines, index, line.clone(), content.clone(), __self.startsListItem(trimmed.clone()).clone().unwrap(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self) } else { if ((indentWidth < 4i64) && __self.htmlBlockType(trimmed.clone(), ((*open) == OpenLeaf::Paragraph)).is_some()) { { let ht = __self.htmlBlockType(trimmed.clone(), ((*open) == OpenLeaf::Paragraph)).clone().unwrap();
            finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            handleHtmlBlock(lines, index, ht, frames, nextId, open, out, pendingClose, pos, source.clone()) } } else { if __self.refDefAt(lines, index, line.clone(), content.clone(), containers, (*open).clone()).is_some() { (index + emitDefinition(__self.refDefAt(lines, index, line.clone(), content.clone(), containers, (*open).clone()).clone().unwrap(), frames, nextId, open, out, pendingClose, pos, source.clone())) } else { if ((gfm && ((*open) != OpenLeaf::Paragraph)) && __self.isTableStart(lines, index, content.clone())) { emitTable(lines, index, frames, nextId, open, out, pendingClose, pos, source.clone()) } else { { appendParagraph(line.clone(), content.clone(), open, paragraphPendingPrefix, paragraphSegs);
            (index + 1i64) } } } } } } } } } } }
        }
        fn matchContainers(line: MdLine, containers: &mut Vec<Container>, frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> String {
            fn consume(kind: String, lex: String, buffering: bool, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, prefix: &mut Vec<String>, source: SourceId) {
                if buffering { (*prefix).push(lex.clone()); } else { if (kind == "markdown.blockquote-marker".to_string()) { emitContainerMarker(kind.clone(), lex.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { emitContainerIndent(lex.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } }
            }
            let mut rest = line.content.clone();
            let mut matched = 0i64;
            let mut done = false;
            let mut i = 0i64;
            let buffering = (((*open) == OpenLeaf::Paragraph) || ((*open) == OpenLeaf::IndentedCode));
            let mut prefix: Vec<String> = Vec::new();
            while ((i < ((*containers).len() as i64)) && !(done)) {
                match containers[(i) as usize].clone() {
                    _ => { match (__self.stripBlockquoteMarker(rest.clone())).clone() {
                        Some((marker, remainder)) => { consume("markdown.blockquote-marker".to_string(), marker.clone(), buffering.clone(), frames, nextId, open, out, pendingClose, pos, &mut prefix, source.clone());
                        rest = remainder.clone();
                        (matched += 1i64); },
                        None => { done = true; },
                    } },
                    ref item @ Container::ListItemFrame { ref ordered, ref contentIndent } => { if ((indentWidth(rest.clone()) >= (*contentIndent)) || rest.chars().all(|__ch| { let c = (__ch as u32) as i64; ((c == 32i64) || (c == 9i64)) })) { let take = __self.consumeIndent(rest.clone(), (*contentIndent).clone());
                    if !take.is_empty() { consume("markdown.indent".to_string(), take.clone(), buffering.clone(), frames, nextId, open, out, pendingClose, pos, &mut prefix, source.clone()); } else { (); }
                    rest = crate::runtime::_str_substring_from(&rest, crate::runtime::_str_length(&take));
                    (matched += 1i64); } else { done = true; } },
                    _ => { (matched += 1i64); },
                }
                (i += 1i64);
            };
            if (matched >= ((*containers).len() as i64)) { if ((*open) == OpenLeaf::IndentedCode) { (*indentedCodePendingPrefix) = (prefix).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""); } else { if buffering { (*paragraphPendingPrefix) = (prefix).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""); } else { (); } } } else { if ((buffering && ((*open) == OpenLeaf::Paragraph)) && __self.isLazyContinuation(rest.clone())) { (*paragraphPendingPrefix) = (prefix).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""); } else { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            if !prefix.is_empty() { flushPending("markdown.indent".to_string(), (prefix).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""), &Vec::new(), Some("continuation".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); }
            scheduleContainerClose(listAwareKeep(matched, rest.clone(), containers, __self), containers, frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self); } }
            rest
        }
        fn listAwareKeep(matched: i64, rest: String, containers: &Vec<Container>, __self: &MarkdownBlocks) -> i64 {
            if ((matched <= 0i64) || (matched > ((*containers).len() as i64))) { matched } else { match containers[((matched - 1i64)) as usize].clone() {
                ref lf @ Container::ListFrame { ref ordered } => { let trimmed = crate::runtime::_str_substring_from(&rest, indentPrefixLength(rest.clone()));
                match __self.startsListItem(trimmed.clone()) {
                    Some(item) if (item.0 == (*ordered)) => matched,
                    _ => (matched - 1i64),
                } },
                _ => matched,
            } }
        }
        fn scheduleContainerClose(keep: i64, containers: &mut Vec<Container>, frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            while (((*containers).len() as i64) > keep) {
                let ctr = (*containers)[(*containers).len() - 1].clone();
                (*containers) = { let __v = ((*containers)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
                (*pendingClose).push(ctr.frame());
            };
        }
        fn emitContainerMarker(kind: String, lexeme: String, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            flushPending(kind.clone(), lexeme.clone(), &Vec::new(), Some("marker".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
        }
        fn emitContainerIndent(lexeme: String, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            flushPending("markdown.indent".to_string(), lexeme.clone(), &Vec::new(), Some("continuation".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
        }
        fn handleBlank(line: MdLine, content: String, frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            if ((*open) == OpenLeaf::IndentedCode) { (*indentedCodeBlanks).push((format!("{}{}", (*indentedCodePendingPrefix), content), line.ending.clone()));
            (*indentedCodePendingPrefix) = "".to_string(); } else { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            let lexeme = format!("{}{}", content, line.ending);
            if !lexeme.is_empty() { flushPending("markdown.blank".to_string(), lexeme.clone(), &Vec::new(), Some("blank".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); } }
        }
        fn appendParagraph(line: MdLine, content: String, open: &mut OpenLeaf, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>) {
            if ((*open) != OpenLeaf::Paragraph) { (*open) = OpenLeaf::Paragraph.clone();
            (*paragraphSegs) = vec![ParaSeg { prefix: "".to_string(), content: content.clone(), ending: line.ending.clone() }]; } else { (*paragraphSegs).push(ParaSeg { prefix: (*paragraphPendingPrefix).clone(), content: content.clone(), ending: line.ending.clone() }); }
            (*paragraphPendingPrefix) = "".to_string();
        }
        fn finishParagraph(frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            finishIndentedCode(frames, indentedCodeBlanks, nextId, open, out, pendingClose, pos, source.clone());
            if ((*open) == OpenLeaf::Paragraph) { let segs = (*paragraphSegs).clone();
            (*open) = OpenLeaf::None.clone();
            (*paragraphSegs) = Vec::new();
            let content = (segs.iter().cloned().map(|s| { format!("{}{}", s.content, s.ending) }).collect::<Vec<_>>()).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("");
            let pieces = MarkdownInlines_parse(content.clone(), (*refs).clone(), profile.clone());
            emitParagraphWithSegments(&pieces, &segs, frames, nextId, open, out, pendingClose, pos, source.clone(), __self); } else { (); }
            if !(*paragraphPendingPrefix).is_empty() { flushPending("markdown.indent".to_string(), (*paragraphPendingPrefix).clone(), &Vec::new(), Some("continuation".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            (*paragraphPendingPrefix) = "".to_string(); } else { (); }
        }
        fn emitParagraphWithSegments(pieces: &Vec<InlinePiece>, segs: &Vec<ParaSeg>, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId, __self: &MarkdownBlocks) {
            if !(*pieces).is_empty() { let n = ((*pieces).len() as i64);
            let mut i = 0i64;
            let mut breakCount = 0i64;
            while (i < n) {
                let piece = match __self.spliceSwallowedBreaks(pieces[(i) as usize].clone(), segs, breakCount) {
                    (rewritten, consumed) => { breakCount = consumed;
                    rewritten },
                };
                if (n == 1i64) { emitFirstLast("markdown.paragraph".to_string(), piece.clone(), Some("content".to_string()), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { if (i == 0i64) { emitFirst("markdown.paragraph".to_string(), piece.clone(), Some("content".to_string()), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { if (i == (n - 1i64)) { emitLast("markdown.paragraph".to_string(), piece.clone(), frames, nextId, open, out, pos, source.clone()); } else { replay(piece.clone(), frames, nextId, out, pos, source.clone()); } } }
                if __self.isBreakPiece(piece.clone()) { (breakCount += 1i64);
                if ((breakCount < ((*segs).len() as i64)) && !segs[(breakCount) as usize].clone().prefix.is_empty()) { leaf("markdown.indent".to_string(), segs[(breakCount) as usize].clone().prefix, Some("continuation".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); } } else { (); }
                (i += 1i64);
            }; } else { (); }
        }
        fn emitAtxHeading(line: MdLine, content: String, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            let lead = indentPrefixLength(content.clone());
            if (lead > 0i64) { flushPending("markdown.indent".to_string(), crate::runtime::_str_substring(&content, 0i64, lead), &Vec::new(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); }
            let body = crate::runtime::_str_substring_from(&content, lead);
            let mut h = 0i64;
            while ((h < crate::runtime::_str_length(&body)) && (crate::runtime::_str_char_at(&body, h) == 35i64)) {
                (h += 1i64);
            };
            let marker = crate::runtime::_str_substring(&body, 0i64, h);
            flushPending("markdown.atx-marker".to_string(), marker.clone(), &vec![FrameSpec { kind: "markdown.heading".to_string(), role: None }], Some("marker".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            let mut rest = crate::runtime::_str_substring_from(&body, h);
            let restLead = rest.chars().take_while(|__ch| { let c = ((*__ch) as u32) as i64; ((c == 32i64) || (c == 9i64)) }).collect::<String>();
            if !restLead.is_empty() { leaf("markdown.indent".to_string(), restLead.clone(), Some("space".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            rest = crate::runtime::_str_substring_from(&rest, crate::runtime::_str_length(&restLead));
            let (rawText, closing) = __self.splitAtxClosing(rest.clone());
            let mut textEnd = crate::runtime::_str_length(&rawText);
            while ((textEnd > 0i64) && ((crate::runtime::_str_char_at(&rawText, (textEnd - 1i64)) == 32i64) || (crate::runtime::_str_char_at(&rawText, (textEnd - 1i64)) == 9i64))) {
                (textEnd -= 1i64);
            };
            let hText = crate::runtime::_str_substring(&rawText, 0i64, textEnd);
            let trailWs = crate::runtime::_str_substring_from(&rawText, textEnd);
            let pieces = MarkdownInlines_parse(hText, (*refs).clone(), profile.clone());
            pieces.iter().cloned().for_each(|__x| replay(__x, frames, nextId, out, pos, source.clone()));;
            if !trailWs.is_empty() { leaf("markdown.indent".to_string(), trailWs.clone(), Some("space".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            if !closing.is_empty() { leaf("markdown.atx-close".to_string(), closing.clone(), Some("close".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            if !line.ending.is_empty() { close("markdown.heading".to_string(), "markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
        }
        fn emitSetextUnderline(line: MdLine, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId) {
            let segs = (*paragraphSegs).clone();
            (*open) = OpenLeaf::None.clone();
            (*paragraphSegs) = Vec::new();
            let content = (segs.iter().cloned().enumerate().map(|(__i, __e)| (__e, __i as i64)).collect::<Vec<_>>().iter().cloned().map(|(s, idx)| { let pfx = if (idx == 0i64) { "".to_string() } else { s.prefix.clone() };
            let end = if (idx == ((segs.len() as i64) - 1i64)) { "".to_string() } else { s.ending.clone() };
            format!("{}{}{}", pfx, s.content, end) }).collect::<Vec<_>>()).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("");
            let interior = segs.last().cloned().map(|__p0| { __p0.ending }).unwrap_or("".to_string());
            let pieces = MarkdownInlines_parse(content.clone(), (*refs).clone(), profile.clone());
            if pieces.is_empty() { flushPending("markdown.setext-underline".to_string(), line.content.clone(), &vec![FrameSpec { kind: "markdown.heading".to_string(), role: None }], Some("underline".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            if !line.ending.is_empty() { close("markdown.heading".to_string(), "markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); } } else { emitInlineOpenOnly("markdown.heading".to_string(), &pieces, frames, nextId, open, out, pendingClose, pos, source.clone());
            if !interior.is_empty() { leaf("markdown.soft-break".to_string(), interior.clone(), Some("space".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            if !line.ending.is_empty() { leaf("markdown.setext-underline".to_string(), line.content.clone(), Some("underline".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            close("markdown.heading".to_string(), "markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { close("markdown.heading".to_string(), "markdown.setext-underline".to_string(), line.content.clone(), Some("underline".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); } }
        }
        fn emitThematicBreak(line: MdLine, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            flushPending("markdown.thematic-marker".to_string(), line.content.clone(), &vec![FrameSpec { kind: "markdown.thematic-break".to_string(), role: None }], Some("marker".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            if !line.ending.is_empty() { close("markdown.thematic-break".to_string(), "markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (*pendingClose).push("markdown.thematic-break".to_string()); }
        }
        fn startFence(line: MdLine, content: String, fence: (i64, i64), frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            let lead = indentPrefixLength(content.clone());
            if (lead > 0i64) { flushPending("markdown.indent".to_string(), crate::runtime::_str_substring(&content, 0i64, lead), &Vec::new(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); }
            let body = crate::runtime::_str_substring_from(&content, lead);
            let flen = fence.1.clone();
            let fenceLex = crate::runtime::_str_substring(&body, 0i64, flen);
            flushPending("markdown.fence-open".to_string(), fenceLex, &vec![FrameSpec { kind: "markdown.code-block".to_string(), role: None }], Some("fence.open".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            let info = crate::runtime::_str_substring_from(&body, flen);
            if !info.is_empty() { leaf("markdown.info".to_string(), info.clone(), Some("info".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            if !line.ending.is_empty() { leaf("markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            (*open) = OpenLeaf::FencedCode { char: fence.0.clone(), len: flen.clone() };
        }
        fn handleFenceBody(line: MdLine, fchar: i64, flen: i64, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId, __self: &MarkdownBlocks) {
            let trimmed = crate::runtime::_str_substring_from(&line.content, indentPrefixLength(line.content.clone()));
            let closes = (((!trimmed.is_empty() && trimmed.chars().all(|__ch| (|__p0| { (__p0 == fchar) })((__ch as u32) as i64))) && (__self.countRun(trimmed.clone(), fchar.clone()) >= flen)) && (indentWidth(line.content.clone()) <= 3i64));
            if closes { (*open) = OpenLeaf::None.clone();
            let lead = indentPrefixLength(line.content.clone());
            if (lead > 0i64) { leaf("markdown.indent".to_string(), crate::runtime::_str_substring(&line.content, 0i64, lead), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            close("markdown.code-block".to_string(), "markdown.fence-close".to_string(), crate::runtime::_str_substring_from(&line.content, lead), Some("fence.close".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            if !line.ending.is_empty() { leaf("markdown.line-break".to_string(), line.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); } } else { if !line.content.is_empty() { leaf("markdown.code-content".to_string(), line.content.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            if !line.ending.is_empty() { leaf("markdown.line-break".to_string(), line.ending.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); } }
        }
        fn handleIndentedCode(line: MdLine, content: String, frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            let cut = indentCut(content.clone(), 4i64);
            let indent = if (cut >= 0i64) { crate::runtime::_str_substring(&content, 0i64, cut) } else { crate::runtime::_str_substring(&content, 0i64, indentPrefixLength(content.clone())) };
            let code = crate::runtime::_str_substring_from(&content, crate::runtime::_str_length(&indent));
            if ((*open) != OpenLeaf::IndentedCode) { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            (*open) = OpenLeaf::IndentedCode.clone();
            flushPending("markdown.indent".to_string(), indent.clone(), &vec![FrameSpec { kind: "markdown.code-block".to_string(), role: None }], Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { leaf("markdown.indent".to_string(), indent.clone(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); }
            leaf("markdown.code-content".to_string(), code.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone());
            if !line.ending.is_empty() { leaf("markdown.line-break".to_string(), line.ending.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
        }
        fn emitIndentedCodePrefix(frames: &mut Vec<String>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            if !(*indentedCodePendingPrefix).is_empty() { flushPending("markdown.indent".to_string(), (*indentedCodePendingPrefix).clone(), &Vec::new(), Some("continuation".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            (*indentedCodePendingPrefix) = "".to_string(); } else { (); }
        }
        fn releaseInteriorBlanks(frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            let held = (*indentedCodeBlanks).clone();
            (*indentedCodeBlanks) = Vec::new();
            held.iter().cloned().for_each(|__pf| match __pf {
                (blankContent, ending) => { let cut = indentCut(blankContent.clone(), 4i64);
                let trivia = if (cut >= 0i64) { crate::runtime::_str_substring(&blankContent, 0i64, cut) } else { blankContent.clone() };
                let keep = if (cut >= 0i64) { crate::runtime::_str_substring_from(&blankContent, cut) } else { "".to_string() };
                if !trivia.is_empty() { leaf("markdown.indent".to_string(), trivia.clone(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
                if !keep.is_empty() { leaf("markdown.code-content".to_string(), keep.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
                if !ending.is_empty() { leaf("markdown.line-break".to_string(), ending.clone(), Some("code".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()) } else { () } },
            });;
        }
        fn finishIndentedCode(frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            if ((*open) == OpenLeaf::IndentedCode) { (*open) = OpenLeaf::None.clone();
            (*pendingClose).push("markdown.code-block".to_string());
            let held = (*indentedCodeBlanks).clone();
            (*indentedCodeBlanks) = Vec::new();
            held.iter().cloned().for_each(|__pf| match __pf {
                (blankContent, ending) => { let lexeme = format!("{}{}", blankContent, ending);
                if !lexeme.is_empty() { flushPending("markdown.blank".to_string(), lexeme.clone(), &Vec::new(), Some("blank".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()) } else { () } },
            });; } else { (); }
        }
        fn handleHtmlBlock(lines: &Vec<MdLine>, index: i64, htmlType: i64, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) -> i64 {
            let endMarkers: Option<Vec<String>> = match htmlType {
                1i64 => Some(vec!["</script>".to_string(), "</pre>".to_string(), "</style>".to_string(), "</textarea>".to_string()]),
                2i64 => Some(vec!["-->".to_string()]),
                3i64 => Some(vec!["?>".to_string()]),
                4i64 => Some(vec![">".to_string()]),
                5i64 => Some(vec!["]]>".to_string()]),
                _ => None.clone(),
            };
            let mut i = index;
            let mut first = true;
            let mut done = false;
            while ((i < ((*lines).len() as i64)) && !(done)) {
                let l = lines[(i) as usize].clone();
                match (endMarkers).clone() {
                    None => { if l.isBlank() { done = true; } else { emitHtmlLine(l.clone(), first, frames, nextId, open, out, pendingClose, pos, source.clone());
                    first = false;
                    (i += 1i64); } },
                    Some(markers) => { emitHtmlLine(l.clone(), first, frames, nextId, open, out, pendingClose, pos, source.clone());
                    first = false;
                    (i += 1i64);
                    let lc = asciiLower(l.content.clone());
                    if markers.iter().cloned().any(|__x| lc.contains(&__x)) { done = true; } else { (); } },
                }
            };
            (*pendingClose).push("markdown.html-block".to_string());
            i
        }
        fn emitHtmlLine(l: MdLine, first: bool, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            if first { flushPending("markdown.html".to_string(), l.content.clone(), &vec![FrameSpec { kind: "markdown.html-block".to_string(), role: None }], Some("html".to_string()), TokenChannel::Embedded.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { if !l.content.is_empty() { leaf("markdown.html".to_string(), l.content.clone(), Some("html".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); } }
            if !l.ending.is_empty() { leaf("markdown.line-break".to_string(), l.ending.clone(), Some("html".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
        }
        fn emitTable(lines: &Vec<MdLine>, index: i64, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) -> i64 {
            let header = lines[(index) as usize].clone();
            flushPending("markdown.table-row".to_string(), header.content.clone(), &vec![FrameSpec { kind: "markdown.table".to_string(), role: None }], Some("header".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            if !header.ending.is_empty() { leaf("markdown.line-break".to_string(), header.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            let delim = lines[((index + 1i64)) as usize].clone();
            leaf("markdown.table-delim".to_string(), delim.content.clone(), Some("delimiter".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            if !delim.ending.is_empty() { leaf("markdown.line-break".to_string(), delim.ending.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            let mut i = (index + 2i64);
            while (((i < ((*lines).len() as i64)) && !(lines[(i) as usize].clone().isBlank())) && lines[(i) as usize].clone().content.contains(char::from_u32((124i64) as u32).unwrap_or('\u{FFFD}'))) {
                leaf("markdown.table-row".to_string(), lines[(i) as usize].clone().content, Some("row".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
                if !lines[(i) as usize].clone().ending.is_empty() { leaf("markdown.line-break".to_string(), lines[(i) as usize].clone().ending, Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
                (i += 1i64);
            };
            (*pendingClose).push("markdown.table".to_string());
            i
        }
        fn openBlockquoteAndReprocess(lines: &Vec<MdLine>, index: i64, line: MdLine, content: String, containers: &mut Vec<Container>, frames: &mut Vec<String>, gfm: bool, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> i64 {
            finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            let stripped = __self.stripBlockquoteMarker(content.clone()).clone().unwrap();
            let lead = indentPrefixLength(content.clone());
            if ((lead > 0i64) && !(stripped.0.starts_with(&(crate::runtime::_str_substring(&content, 0i64, lead))))) { (); } else { (); }
            flushPending("markdown.blockquote-marker".to_string(), stripped.0.clone(), &vec![FrameSpec { kind: "markdown.blockquote".to_string(), role: None }], Some("marker".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            (*containers).push(Container::Blockquote);
            let innerLine = MdLine { content: stripped.1.clone(), ending: line.ending.clone() };
            reprocessInner(lines, index, innerLine.clone(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self)
        }
        fn openListItemAndReprocess(lines: &Vec<MdLine>, index: i64, line: MdLine, content: String, item: (bool, String, i64), containers: &mut Vec<Container>, frames: &mut Vec<String>, gfm: bool, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> i64 {
            finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            let lead = indentPrefixLength(content.clone());
            let (ordered, marker, contentIndent) = item;
            let needList = !(((*containers).last().cloned()).is_some_and(|__pf| match __pf {
                ref lf @ Container::ListFrame { ref ordered } => (ordered == ordered),
                _ => false,
            }));
            let mut opens: Vec<FrameSpec> = Vec::new();
            if needList { match ((*containers).last().cloned()).clone() {
                Some(ref lf @ Container::ListFrame { ref ordered }) => { (*containers) = { let __v = ((*containers)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
                (*pendingClose).push(lf.frame()); },
                _ => { (); },
            } } else { (); }
            if (lead > 0i64) { flushPending("markdown.indent".to_string(), crate::runtime::_str_substring(&content, 0i64, lead), &Vec::new(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); }
            if needList { opens.push(FrameSpec { kind: "markdown.list".to_string(), role: None });
            (*containers).push(Container::ListFrame { ordered: ordered.clone() }); } else { (); }
            opens.push(FrameSpec { kind: "markdown.list-item".to_string(), role: None });
            (*containers).push(Container::ListItemFrame { ordered: ordered.clone(), contentIndent: (contentIndent + lead) });
            let body = crate::runtime::_str_substring_from(&content, lead);
            let markerLex = crate::runtime::_str_substring(&body, 0i64, crate::runtime::_str_length(&marker));
            flushPending("markdown.list-marker".to_string(), markerLex, &opens, Some("marker".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            let mut remainder = crate::runtime::_str_substring_from(&body, crate::runtime::_str_length(&marker));
            if (gfm && ((remainder.starts_with("[ ] ") || remainder.starts_with("[x] ")) || remainder.starts_with("[X] "))) { leaf("markdown.task-marker".to_string(), crate::runtime::_str_substring(&remainder, 0i64, 3i64), Some("task".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            remainder = crate::runtime::_str_substring_from(&remainder, 3i64); } else { (); }
            let innerLine = MdLine { content: remainder.clone(), ending: line.ending.clone() };
            reprocessInner(lines, index, innerLine.clone(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self)
        }
        fn reprocessInner(lines: &Vec<MdLine>, index: i64, innerLine: MdLine, containers: &mut Vec<Container>, frames: &mut Vec<String>, gfm: bool, indentedCodeBlanks: &mut Vec<(String, String)>, indentedCodePendingPrefix: &mut String, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) -> i64 {
            let content = innerLine.content.clone();
            let indentWidth = indentWidth(content.clone());
            if innerLine.isBlank() { { finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            if !innerLine.ending.is_empty() { leaf("markdown.line-break".to_string(), innerLine.ending.clone(), Some("blank".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            (index + 1i64) } } else { dispatchLeaf(lines, index, innerLine.clone(), content.clone(), indentWidth.clone(), containers, frames, gfm.clone(), indentedCodeBlanks, indentedCodePendingPrefix, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self) }
        }
        fn scanFrontMatter(lines: &Vec<MdLine>, index: i64, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) -> i64 {
            if ((index < ((*lines).len() as i64)) && (lines[(index) as usize].clone().content == "---".to_string())) { { let mut i = (index + 1i64);
            while (((i < ((*lines).len() as i64)) && (lines[(i) as usize].clone().content != "---".to_string())) && (lines[(i) as usize].clone().content != "...".to_string())) {
                (i += 1i64);
            };
            if (i < ((*lines).len() as i64)) { { openBranch("markdown.front-matter".to_string(), "markdown.front-matter-fence".to_string(), lines[(index) as usize].clone().content, Some("open".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            if !lines[(index) as usize].clone().ending.is_empty() { leaf("markdown.line-break".to_string(), lines[(index) as usize].clone().ending, Some("open".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            let mut j = (index + 1i64);
            while (j < i) {
                if !lines[(j) as usize].clone().content.is_empty() { leaf("markdown.code-content".to_string(), lines[(j) as usize].clone().content, Some("yaml".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
                if !lines[(j) as usize].clone().ending.is_empty() { leaf("markdown.line-break".to_string(), lines[(j) as usize].clone().ending, Some("yaml".to_string()), TokenChannel::Embedded.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
                (j += 1i64);
            };
            close("markdown.front-matter".to_string(), "markdown.front-matter-fence".to_string(), lines[(i) as usize].clone().content, Some("close".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            if !lines[(i) as usize].clone().ending.is_empty() { leaf("markdown.line-break".to_string(), lines[(i) as usize].clone().ending, Some("close".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (); }
            (i + 1i64) } } else { index } } } else { index }
        }
        fn collectReferences(lines: &Vec<MdLine>, limits: MarkdownLimits, refs: &mut std::collections::HashMap<String, LinkRef>, __self: &MarkdownBlocks) {
            let mut inFence = false;
            let mut fenceChar = 32i64;
            let mut inParagraph = false;
            let mut i = 0i64;
            while (i < ((*lines).len() as i64)) {
                let content = lines[(i) as usize].clone().content;
                let trimmed = crate::runtime::_str_substring_from(&content, indentPrefixLength(content.clone()));
                if inFence { if ((!trimmed.is_empty() && trimmed.chars().all(|__ch| (|__p0| { (__p0 == fenceChar) })((__ch as u32) as i64))) && (crate::runtime::_str_length(&trimmed) >= 3i64)) { inFence = false; } else { (); } } else { if (trimmed.starts_with("```") || trimmed.starts_with("~~~")) { inFence = true;
                fenceChar = (crate::runtime::_str_char_at(&trimmed, 0i64)).0;
                inParagraph = false; } else { if lines[(i) as usize].clone().isBlank() { inParagraph = false; } else { if ((!(inParagraph) && (indentWidth(content.clone()) < 4i64)) && trimmed.starts_with("[")) { match __self.scanRefDef(lines, i) {
                    Some(defn) => { let norm = normalizeLabel(defn.label());
                    if (!((*refs).contains_key(&(norm))) && (((*refs).len() as i64) < limits.maxReferences)) { (*refs) = {
                        let mut m2 = (*refs).clone();
                        m2.insert(norm.clone(), LinkRef { destination: defn.destination(), title: defn.title() });
                        m2
                      }; } else { (); }
                    (i += (defn.linesConsumed - 1i64)); },
                    None => { inParagraph = true; },
                } } else { inParagraph = true; } } } }
                (i += 1i64);
            };
        }
        fn emitDefinition(defn: RefDef, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) -> i64 {
            if !defn.indent.is_empty() { flushPending("markdown.indent".to_string(), defn.indent.clone(), &Vec::new(), Some("indent".to_string()), TokenChannel::Trivia.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); } else { (); }
            flushPending("markdown.reference-label".to_string(), defn.labelLex.clone(), &vec![FrameSpec { kind: "markdown.definition".to_string(), role: None }], Some("label".to_string()), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone());
            leaf("markdown.colon".to_string(), defn.colon.clone(), Some("colon".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            leaf("markdown.indent".to_string(), defn.afterColon.clone(), Some("space".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone());
            leaf("markdown.destination".to_string(), defn.destLex.clone(), Some("destination".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            leaf("markdown.indent".to_string(), defn.betweenDestTitle.clone(), Some("space".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone());
            leaf("markdown.title".to_string(), defn.titleLex.clone(), Some("title".to_string()), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone());
            if !defn.trailing.is_empty() { close("markdown.definition".to_string(), "markdown.line-break".to_string(), defn.trailing.clone(), Some("trailing".to_string()), TokenChannel::Trivia.clone(), frames, nextId, out, pos, source.clone()); } else { (*pendingClose).push("markdown.definition".to_string()); }
            defn.linesConsumed
        }
        fn emitInlineOpenOnly(branch: String, pieces: &Vec<InlinePiece>, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            if (*pieces).is_empty() { (); } else { emitFirst(branch.clone(), (*pieces)[0].clone(), Some("content".to_string()), frames, nextId, open, out, pendingClose, pos, source.clone());
            let mut i = 1i64;
            while (i < ((*pieces).len() as i64)) {
                replay(pieces[(i) as usize].clone(), frames, nextId, out, pos, source.clone());
                (i += 1i64);
            }; }
        }
        fn emitFirst(branch: String, piece: InlinePiece, role: Option<String>, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            match piece {
                InlinePiece::Tok { kind, lexeme: lex, role: r, channel: ch } => { flushPending(kind.clone(), lex.clone(), &vec![FrameSpec { kind: branch.clone(), role: None }], r.or_else(|| role), ch.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); },
                InlinePiece::Open { branch: b2, kind, lexeme: lex, role: r } => { flushPending(kind.clone(), lex.clone(), &vec![FrameSpec { kind: branch.clone(), role: None }, FrameSpec { kind: b2.clone(), role: None }], r.clone(), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); },
                InlinePiece::Close { branch: _, kind, lexeme: lex, role: r } => { flushPending(kind.clone(), lex.clone(), &vec![FrameSpec { kind: branch.clone(), role: None }], r.clone(), TokenChannel::Syntax.clone(), frames, nextId, open, out, pendingClose, pos, source.clone()); },
            }
        }
        fn emitLast(branch: String, piece: InlinePiece, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            match piece {
                InlinePiece::Tok { kind, lexeme: lex, role: r, channel: ch } => { emit(kind.clone(), lex.clone(), VmInstruction::Reframe { closeBefore: Vec::new(), open: Vec::new(), closeAfter: vec![branch.clone()], role: r.clone() }, ch.clone(), frames, nextId, out, pos, source.clone()); },
                InlinePiece::Close { branch: b2, kind, lexeme: lex, role: r } => { emit(kind.clone(), lex.clone(), VmInstruction::Reframe { closeBefore: Vec::new(), open: Vec::new(), closeAfter: vec![b2.clone(), branch.clone()], role: r.clone() }, TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); },
                InlinePiece::Open { branch: b2, kind, lexeme: lex, role: r } => { emit(kind.clone(), lex.clone(), VmInstruction::Open { kind: b2.clone(), role: r.clone() }, TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); },
            }
        }
        fn emitFirstLast(branch: String, piece: InlinePiece, role: Option<String>, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            match piece {
                InlinePiece::Tok { kind, lexeme: lex, role: r, channel: ch } => { emit(kind.clone(), lex.clone(), foldPending(VmInstruction::Reframe { closeBefore: Vec::new(), open: vec![FrameSpec { kind: branch.clone(), role: None }], closeAfter: vec![branch.clone()], role: r.or_else(|| role) }, pendingClose), ch.clone(), frames, nextId, out, pos, source.clone()); },
                InlinePiece::Open { branch: b2, kind, lexeme: lex, role: r } => { emit(kind.clone(), lex.clone(), foldPending(VmInstruction::Reframe { closeBefore: Vec::new(), open: vec![FrameSpec { kind: branch.clone(), role: None }, FrameSpec { kind: b2.clone(), role: None }], closeAfter: vec![b2.clone(), branch.clone()], role: r.clone() }, pendingClose), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); },
                InlinePiece::Close { branch: b2, kind, lexeme: lex, role: r } => { emit(kind.clone(), lex.clone(), foldPending(VmInstruction::Reframe { closeBefore: Vec::new(), open: vec![FrameSpec { kind: branch.clone(), role: None }], closeAfter: vec![b2.clone(), branch.clone()], role: r.clone() }, pendingClose), TokenChannel::Syntax.clone(), frames, nextId, out, pos, source.clone()); },
            }
        }
        fn replay(piece: InlinePiece, frames: &mut Vec<String>, nextId: &mut i64, out: &mut Vec<VmToken>, pos: &mut SourcePosition, source: SourceId) {
            match piece {
                InlinePiece::Tok { kind, lexeme: lex, role, channel: ch } => { leaf(kind.clone(), lex.clone(), role.clone(), ch.clone(), frames, nextId, out, pos, source.clone()); },
                InlinePiece::Open { branch: b2, kind, lexeme: lex, role } => { openBranch(b2.clone(), kind.clone(), lex.clone(), role.clone(), TokenChannel::Syntax, frames, nextId, out, pos, source.clone()); },
                InlinePiece::Close { branch: b2, kind, lexeme: lex, role } => { close(b2.clone(), kind.clone(), lex.clone(), role.clone(), TokenChannel::Syntax, frames, nextId, out, pos, source.clone()); },
            }
        }
        fn flushPending(kind: String, lexeme: String, opens: &Vec<FrameSpec>, role: Option<String>, channel: TokenChannel, frames: &mut Vec<String>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, source: SourceId) {
            if lexeme.is_empty() { (); } else { if ((*pendingClose).is_empty() && (*opens).is_empty()) { leaf(kind.clone(), lexeme.clone(), role.clone(), channel.clone(), frames, nextId, out, pos, source.clone()); } else { let instr = VmInstruction::Reframe { closeBefore: (*pendingClose).clone(), open: (*opens).clone(), closeAfter: Vec::new(), role: role.clone() };
            (*pendingClose) = Vec::new();
            emit(kind.clone(), lexeme.clone(), instr, channel.clone(), frames, nextId, out, pos, source.clone()); } }
        }
        fn foldPending(reframe: VmInstruction, pendingClose: &mut Vec<String>) -> VmInstruction {
            let folded = (match &(reframe) { VmInstruction::Reframe { closeBefore, open, closeAfter, role, .. } => VmInstruction::Reframe { closeBefore: [&((*pendingClose))[..], &((match &reframe { VmInstruction::Reframe { closeBefore, .. } => closeBefore.clone(), _ => unreachable!() }))[..]].concat(), open: open.clone(), closeAfter: closeAfter.clone(), role: role.clone() }, _ => unreachable!() });
            (*pendingClose) = Vec::new();
            folded
        }
        fn finishOpenBlocks(diagnostics: &mut Vec<Diagnostic>, frames: &mut Vec<String>, indentedCodeBlanks: &mut Vec<(String, String)>, nextId: &mut i64, open: &mut OpenLeaf, out: &mut Vec<VmToken>, paragraphPendingPrefix: &mut String, paragraphSegs: &mut Vec<ParaSeg>, pendingClose: &mut Vec<String>, pos: &mut SourcePosition, profile: MarkdownProfile, refs: &std::collections::HashMap<String, LinkRef>, source: SourceId, __self: &MarkdownBlocks) {
            finishParagraph(frames, indentedCodeBlanks, nextId, open, out, paragraphPendingPrefix, paragraphSegs, pendingClose, pos, profile.clone(), refs, source.clone(), __self);
            match (*open) {
                OpenLeaf::FencedCode { char: _, len: _ } => { (*diagnostics).push(Diagnostic { code: "uniml.markdown.unterminated-fence".to_string(), message: "fenced code block was not closed before end of input".to_string(), severity: Severity::Warning, span: None.clone(), dialect: Some("markdown".to_string()), details: Vec::new() });
                (*open) = OpenLeaf::None.clone(); },
                _ => { (); },
            }
        }
        let mut pos = Start.clone();
        let mut nextId = 0i64;
        let mut out: Vec<VmToken> = Vec::new();
        let mut frames: Vec<String> = Vec::new();
        let mut diagnostics: Vec<Diagnostic> = Vec::new();
        let mut refs: std::collections::HashMap<String, LinkRef> = std::collections::HashMap::new();
        let mut containers: Vec<Container> = Vec::new();
        let mut pendingClose: Vec<String> = Vec::new();
        let mut indentedCodeBlanks: Vec<(String, String)> = Vec::new();
        let mut indentedCodePendingPrefix = "".to_string();
        let mut open: OpenLeaf = OpenLeaf::None.clone();
        let mut paragraphSegs: Vec<ParaSeg> = Vec::new();
        let mut paragraphPendingPrefix = "".to_string();
        if (codePointCount(text.clone()) > self.limits.maxSourceCodePoints) { MarkdownBlockResult { tokens: Vec::new(), diagnostics: vec![self.limitDiag("uniml.markdown.limit.source".to_string(), format!("Markdown source exceeds the {} code-point limit", self.limits.maxSourceCodePoints))] } } else { { let lines = split(text.clone());
        collectReferences(&lines, self.limits.clone(), &mut refs, self);
        let mut index = 0i64;
        if self.scala { index = scanFrontMatter(&lines, index, &mut frames, &mut nextId, &mut out, &mut pos, self.source.clone()); } else { (); }
        while (index < (lines.len() as i64)) {
            index = processLine(&lines, index, &mut containers, &mut frames, self.gfm.clone(), &mut indentedCodeBlanks, &mut indentedCodePendingPrefix, &mut nextId, &mut open, &mut out, &mut paragraphPendingPrefix, &mut paragraphSegs, &mut pendingClose, &mut pos, self.profile.clone(), &mut refs, self.source.clone(), self);
        };
        finishOpenBlocks(&mut diagnostics, &mut frames, &mut indentedCodeBlanks, &mut nextId, &mut open, &mut out, &mut paragraphPendingPrefix, &mut paragraphSegs, &mut pendingClose, &mut pos, self.profile.clone(), &mut refs, self.source.clone(), self);
        closeDangling(&mut frames, &mut open, &mut out);
        MarkdownBlockResult { tokens: out.clone(), diagnostics: diagnostics.clone() } } }
    }

    pub fn refDefAt(&self, lines: &Vec<MdLine>, index: i64, line: MdLine, content: String, containers: &Vec<Container>, open: OpenLeaf) -> Option<RefDef> {
        let Paragraph = "markdown.paragraph".to_string();
        if (open == OpenLeaf::Paragraph) { None } else { if (*containers).is_empty() { self.scanRefDef(lines, index) } else { self.scanRefDef(&vec![MdLine { content: content, ending: line.ending }], 0i64) } }
    }

    pub fn scanRefDef(&self, lines: &Vec<MdLine>, index: i64) -> Option<RefDef> {
        fn isSpace(c: i64) -> bool {
            ((c == 32i64) || (c == 9i64))
        }
        fn isBreakChar(c: i64) -> bool {
            ((c == 10i64) || (c == 13i64))
        }
        fn skipWs(from: i64, maxBreaks: i64, joined: String, n: i64) -> i64 {
            let mut j = from.clone();
            let mut breaks = 0i64;
            let mut stop = false;
            while ((j < n) && !(stop)) {
                let c = crate::runtime::_str_char_at(&joined, j);
                if isSpace((c).0.clone()) { (j += 1i64); } else { if isBreakChar((c).0.clone()) { (breaks += 1i64);
                if (breaks > maxBreaks) { stop = true; } else { if (((c == 13i64) && ((j + 1i64) < n)) && (crate::runtime::_str_char_at(&joined, (j + 1i64)) == 10i64)) { (j += 2i64); } else { (j += 1i64); } } } else { stop = true; } }
            };
            if (breaks > maxBreaks) { -1i64 } else { j }
        }
        fn endOfLine(at: i64, joined: String, n: i64) -> i64 {
            let mut j = at;
            while ((j < n) && !(isBreakChar((crate::runtime::_str_char_at(&joined, j)).0))) {
                (j += 1i64);
            };
            if ((((j < n) && (crate::runtime::_str_char_at(&joined, j) == 13i64)) && ((j + 1i64) < n)) && (crate::runtime::_str_char_at(&joined, (j + 1i64)) == 10i64)) { (j + 2i64) } else { if (j < n) { (j + 1i64) } else { j } }
        }
        fn onlySpacesTo(from: i64, to: i64, joined: String) -> bool {
            let mut j = from.clone();
            let mut ok = true;
            while ((j < to) && ok) {
                if (!(isSpace((crate::runtime::_str_char_at(&joined, j)).0)) && !(isBreakChar((crate::runtime::_str_char_at(&joined, j)).0))) { ok = false; } else { (); }
                (j += 1i64);
            };
            ok
        }
        let mut last = index;
        while ((last < ((*lines).len() as i64)) && !(lines[(last.clone()) as usize].clone().isBlank())) {
            (last += 1i64);
        };
        if (last == index) { return None; } else { (); }
        let window = ((*lines))[(index as usize)..(last as usize)].to_vec();
        let joined = (window.iter().cloned().map(|__p0| { __p0.raw() }).collect::<Vec<_>>()).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("");
        let n = crate::runtime::_str_length(&joined);
        if (indentWidth(joined.clone()) >= 4i64) { return None; } else { (); }
        let labelStart = indentPrefixLength(joined.clone());
        if ((labelStart >= n) || (crate::runtime::_str_char_at(&joined, labelStart) != 91i64)) { return None; } else { (); }
        let mut i = (labelStart + 1i64);
        let mut labelEnd = -1i64;
        while ((i < n) && (labelEnd < 0i64)) {
            let c = crate::runtime::_str_char_at(&joined, i);
            if ((c == 92i64) && ((i + 1i64) < n)) { (i += 2i64); } else { if (c == 93i64) { labelEnd = i; } else { if (c == 91i64) { return None; } else { (i += 1i64); } } }
        };
        if (labelEnd < 0i64) { return None; } else { (); }
        let labelLex = crate::runtime::_str_substring(&joined, labelStart, (labelEnd + 1i64));
        if (crate::runtime::_str_length(&labelLex) > 1002i64) { return None; } else { (); }
        if crate::runtime::_str_substring(&labelLex, 1i64, (crate::runtime::_str_length(&labelLex) - 1i64)).trim().to_string().is_empty() { return None; } else { (); }
        if (((labelEnd + 1i64) >= n) || (crate::runtime::_str_char_at(&joined, (labelEnd + 1i64)) != 58i64)) { return None; } else { (); }
        let afterColonStart = (labelEnd + 2i64);
        let destStart = skipWs(afterColonStart, 1i64, joined.clone(), n.clone());
        if ((destStart < 0i64) || (destStart >= n)) { return None; } else { (); }
        let mut destEnd = destStart.clone();
        if (crate::runtime::_str_char_at(&joined, destStart) == 60i64) { let mut j = (destStart + 1i64);
        let mut closed = -1i64;
        let mut failed = false;
        while (((j < n) && (closed < 0i64)) && !(failed)) {
            let c = crate::runtime::_str_char_at(&joined, j);
            if ((c == 92i64) && ((j + 1i64) < n)) { (j += 2i64); } else { if (isBreakChar((c).0.clone()) || (c == 60i64)) { failed = true; } else { if (c == 62i64) { closed = j; } else { (j += 1i64); } } }
        };
        if (failed || (closed < 0i64)) { return None; } else { (); }
        destEnd = (closed + 1i64); } else { let mut j = destStart.clone();
        let mut parens = 0i64;
        let mut stop = false;
        while ((j < n) && !(stop)) {
            let c = crate::runtime::_str_char_at(&joined, j);
            if ((c == 92i64) && ((j + 1i64) < n)) { (j += 2i64); } else { if ((isSpace((c).0.clone()) || isBreakChar((c).0.clone())) || (crate::runtime::_to_int(&c) < 32i64)) { stop = true; } else { if (c == 40i64) { (parens += 1i64);
            (j += 1i64); } else { if (c == 41i64) { (parens -= 1i64);
            if (parens < 0i64) { stop = true; } else { (j += 1i64); } } else { (j += 1i64); } } } }
        };
        destEnd = j;
        if (destEnd == destStart) { return None; } else { (); } }
        let titleStart = skipWs(destEnd.clone(), 1i64, joined.clone(), n.clone());
        let mut titleEnd = -1i64;
        if ((titleStart > destEnd) && (titleStart < n)) { let open = crate::runtime::_str_char_at(&joined, titleStart);
        let close = if (open == 40i64) { 41i64 } else { (open).0 };
        if (((open == 34i64) || (open == 39i64)) || (open == 40i64)) { let mut j = (titleStart + 1i64);
        let mut found = -1i64;
        let mut failed = false;
        while (((j < n) && (found < 0i64)) && !(failed)) {
            let c = crate::runtime::_str_char_at(&joined, j);
            if ((c == 92i64) && ((j + 1i64) < n)) { (j += 2i64); } else { if (c == 10i64) { if (((j + 1i64) < n) && self.lineIsBlankAt(joined.clone(), (j + 1i64))) { failed = true; } else { (j += 1i64); } } else { if (c == close) { found = j; } else { if ((c == open) && (open == 40i64)) { failed = true; } else { (j += 1i64); } } } }
        };
        if ((!(failed) && (found >= 0i64)) && onlySpacesTo((found + 1i64), endOfLine(found, joined.clone(), n.clone()), joined.clone())) { titleEnd = (found + 1i64); } else { (); } } else { (); } } else { (); }
        let (bodyEnd, titleSlice, betweenSlice) = if (titleEnd > 0i64) { (titleEnd, crate::runtime::_str_substring(&joined, titleStart, titleEnd), crate::runtime::_str_substring(&joined, destEnd, titleStart)) } else { (destEnd.clone(), "".to_string(), "".to_string()) };
        let lineEnd = endOfLine(bodyEnd.clone(), joined.clone(), n.clone());
        if !(onlySpacesTo(bodyEnd.clone(), lineEnd.clone(), joined.clone())) { return None; } else { (); }
        let consumed = crate::runtime::_str_substring(&joined, 0i64, lineEnd);
        let mut linesUsed = 0i64;
        let mut acc = 0i64;
        while ((acc < lineEnd) && (linesUsed < (window.len() as i64))) {
            (acc += (window[(linesUsed) as usize].clone().raw().len() as i64));
            (linesUsed += 1i64);
        };
        Some(RefDef { indent: crate::runtime::_str_substring(&joined, 0i64, labelStart), labelLex: labelLex.clone(), colon: ":".to_string(), afterColon: crate::runtime::_str_substring(&joined, afterColonStart, destStart), destLex: crate::runtime::_str_substring(&joined, destStart, destEnd), betweenDestTitle: betweenSlice, titleLex: titleSlice, trailing: crate::runtime::_str_substring(&joined, bodyEnd, lineEnd), linesConsumed: linesUsed }).filter(|__p0| { (__p0.consumedText() == consumed) })
    }

    pub fn lineIsBlankAt(&self, joined: String, from: i64) -> bool {
        let mut j = from;
        while ((j < crate::runtime::_str_length(&joined)) && ((crate::runtime::_str_char_at(&joined, j) == 32i64) || (crate::runtime::_str_char_at(&joined, j) == 9i64))) {
            (j += 1i64);
        };
        (((j >= crate::runtime::_str_length(&joined)) || (crate::runtime::_str_char_at(&joined, j) == 10i64)) || (crate::runtime::_str_char_at(&joined, j) == 13i64))
    }

    pub fn isLazyContinuation(&self, rest: String) -> bool {
        if rest.chars().all(|__ch| { let c = (__ch as u32) as i64; ((c == 32i64) || (c == 9i64)) }) { false } else { { let t = crate::runtime::_str_substring_from(&rest, indentPrefixLength(rest.clone()));
        !(((((((self.startsAtxHeading(t.clone()) || self.isThematicBreak(t.clone())) || self.startsFence(t.clone()).is_some()) || self.startsBlockquote(t.clone())) || self.startsListItem(t.clone()).is_some()) || self.htmlBlockType(t.clone(), true).is_some()) || self.isSetextUnderline(t.clone()))) } }
    }

    pub fn stripBlockquoteMarker(&self, content: String) -> Option<(String, String)> {
        let lead = indentPrefixLength(content.clone());
        if (((indentWidth(content.clone()) <= 3i64) && (lead < crate::runtime::_str_length(&content))) && (crate::runtime::_str_char_at(&content, lead) == 62i64)) { { let mut end = (lead + 1i64);
        if ((end < crate::runtime::_str_length(&content)) && (crate::runtime::_str_char_at(&content, end) == 32i64)) { (end += 1i64); } else { if ((end < crate::runtime::_str_length(&content)) && (crate::runtime::_str_char_at(&content, end) == 9i64)) { (end += 1i64); } else { (); } }
        Some((crate::runtime::_str_substring(&content, 0i64, end), crate::runtime::_str_substring_from(&content, end))) } } else { None }
    }

    pub fn consumeIndent(&self, content: String, columns: i64) -> String {
        let mut col = 0i64;
        let mut i = 0i64;
        while (((i < crate::runtime::_str_length(&content)) && (col < columns)) && ((crate::runtime::_str_char_at(&content, i) == 32i64) || (crate::runtime::_str_char_at(&content, i) == 9i64))) {
            (col += if (crate::runtime::_str_char_at(&content, i) == 9i64) { (4i64 - (col % 4i64)) } else { 1i64 });
            (i += 1i64);
        };
        crate::runtime::_str_substring(&content, 0i64, i)
    }

    pub fn spliceSwallowedBreaks(&self, piece: InlinePiece, segs: &Vec<ParaSeg>, breaksSoFar: i64) -> (InlinePiece, i64) {
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        let lexeme = self.pieceLexeme(piece.clone());
        if (self.isBreakPiece(piece.clone()) || ({ let __h: &str = &lexeme; let __n: &str = &char::from_u32((10i64) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) } < 0i64)) { (piece.clone(), breaksSoFar) } else { { let mut out: Vec<String> = Vec::new();
        let mut k = breaksSoFar;
        let mut i = 0i64;
        while (i < crate::runtime::_str_length(&lexeme)) {
            let c = crate::runtime::_str_char_at(&lexeme, i);
            out.push((c).to_string());
            if (c == 10i64) { (k += 1i64);
            if (k < ((*segs).len() as i64)) { out.push(segs[(k.clone()) as usize].clone().prefix); } else { (); } } else { (); }
            (i += 1i64);
        };
        (self.withLexeme(piece.clone(), (out).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")), k.clone()) } }
    }

    pub fn pieceLexeme(&self, piece: InlinePiece) -> String {
        match piece {
            InlinePiece::Tok { kind: _, lexeme, role: _, channel: _ } => lexeme.clone(),
            InlinePiece::Open { branch: _, kind: _, lexeme, role: _ } => lexeme.clone(),
            InlinePiece::Close { branch: _, kind: _, lexeme, role: _ } => lexeme.clone(),
        }
    }

    pub fn withLexeme(&self, piece: InlinePiece, lexeme: String) -> InlinePiece {
        match (piece).clone() {
            ref p @ InlinePiece::Tok { ref kind, ref lexeme, ref role, ref channel } => (match &((*p)) { InlinePiece::Tok { kind, lexeme, role, channel, .. } => InlinePiece::Tok { kind: kind.clone(), lexeme: (*lexeme).clone(), role: role.clone(), channel: channel.clone() }, _ => unreachable!() }),
            ref p @ InlinePiece::Open { ref branch, ref kind, ref lexeme, ref role } => (match &((*p)) { InlinePiece::Open { branch, kind, lexeme, role, .. } => InlinePiece::Open { branch: branch.clone(), kind: kind.clone(), lexeme: (*lexeme).clone(), role: role.clone() }, _ => unreachable!() }),
            ref p @ InlinePiece::Close { ref branch, ref kind, ref lexeme, ref role } => (match &((*p)) { InlinePiece::Close { branch, kind, lexeme, role, .. } => InlinePiece::Close { branch: branch.clone(), kind: kind.clone(), lexeme: (*lexeme).clone(), role: role.clone() }, _ => unreachable!() }),
        }
    }

    pub fn isBreakPiece(&self, piece: InlinePiece) -> bool {
        let SoftBreak = "markdown.soft-break".to_string();
        let HardBreak = "markdown.hard-break".to_string();
        match piece {
            InlinePiece::Tok { kind, lexeme: _, role: _, channel: _ } => ((kind == SoftBreak) || (kind == HardBreak)),
            _ => false,
        }
    }

    pub fn startsAtxHeading(&self, trimmed: String) -> bool {
        let mut i = 0i64;
        while ((i < crate::runtime::_str_length(&trimmed)) && (crate::runtime::_str_char_at(&trimmed, i) == 35i64)) {
            (i += 1i64);
        };
        (((i >= 1i64) && (i <= 6i64)) && (((i == crate::runtime::_str_length(&trimmed)) || (crate::runtime::_str_char_at(&trimmed, i) == 32i64)) || (crate::runtime::_str_char_at(&trimmed, i) == 9i64)))
    }

    pub fn splitAtxClosing(&self, rest: String) -> (String, String) {
        let mut end = crate::runtime::_str_length(&rest);
        while ((end > 0i64) && ((crate::runtime::_str_char_at(&rest, (end - 1i64)) == 32i64) || (crate::runtime::_str_char_at(&rest, (end - 1i64)) == 9i64))) {
            (end -= 1i64);
        };
        let mut hashEnd = end.clone();
        while ((hashEnd > 0i64) && (crate::runtime::_str_char_at(&rest, (hashEnd - 1i64)) == 35i64)) {
            (hashEnd -= 1i64);
        };
        if ((hashEnd < end) && (((hashEnd == 0i64) || (crate::runtime::_str_char_at(&rest, (hashEnd - 1i64)) == 32i64)) || (crate::runtime::_str_char_at(&rest, (hashEnd - 1i64)) == 9i64))) { (crate::runtime::_str_substring(&rest, 0i64, hashEnd), crate::runtime::_str_substring_from(&rest, hashEnd)) } else { (rest.clone(), "".to_string()) }
    }

    pub fn isSetextUnderline(&self, trimmed: String) -> bool {
        ((!trimmed.is_empty() && (trimmed.chars().all(|__ch| (|__p0| { (__p0 == 61i64) })((__ch as u32) as i64)) || trimmed.chars().all(|__ch| (|__p0| { (__p0 == 45i64) })((__ch as u32) as i64)))) && { let t = trimmed.trim().to_string();
        (!t.is_empty() && (t.chars().all(|__ch| (|__p0| { (__p0 == 61i64) })((__ch as u32) as i64)) || t.chars().all(|__ch| (|__p0| { (__p0 == 45i64) })((__ch as u32) as i64)))) })
    }

    pub fn isThematicBreak(&self, trimmed: String) -> bool {
        let stripped = trimmed.chars().filter(|__ch| { let c = (*__ch as u32) as i64; ((c != 32i64) && (c != 9i64)) }).collect::<String>();
        ((crate::runtime::_str_length(&stripped) >= 3i64) && ((stripped.chars().all(|__ch| (|__p0| { (__p0 == 42i64) })((__ch as u32) as i64)) || stripped.chars().all(|__ch| (|__p0| { (__p0 == 45i64) })((__ch as u32) as i64))) || stripped.chars().all(|__ch| (|__p0| { (__p0 == 95i64) })((__ch as u32) as i64))))
    }

    pub fn startsFence(&self, trimmed: String) -> Option<(i64, i64)> {
        if trimmed.starts_with("```") { Some((96i64, self.countRun(trimmed.clone(), 96i64))) } else { if trimmed.starts_with("~~~") { Some((126i64, self.countRun(trimmed.clone(), 126i64))) } else { None } }
    }

    pub fn countRun(&self, s: String, c: i64) -> i64 {
        let mut i = 0i64;
        while ((i < crate::runtime::_str_length(&s)) && (crate::runtime::_str_char_at(&s, i) == c)) {
            (i += 1i64);
        };
        i
    }

    pub fn htmlBlockType(&self, trimmed: String, paragraphOpen: bool) -> Option<i64> {
        if !(trimmed.starts_with("<")) { None } else { { let lower = asciiLower(trimmed.clone());
        if self.htmlType1Start(lower) { Some(1i64) } else { if trimmed.starts_with("<!--") { Some(2i64) } else { if trimmed.starts_with("<?") { Some(3i64) } else { if trimmed.starts_with("<![CDATA[") { Some(5i64) } else { if ((trimmed.starts_with("<!") && (crate::runtime::_str_length(&trimmed) > 2i64)) && MdChars_isAsciiLetter((crate::runtime::_str_char_at(&trimmed, 2i64)).0)) { Some(4i64) } else { if self.htmlType6Start(trimmed.clone()) { Some(6i64) } else { if (!(paragraphOpen) && self.htmlType7Start(trimmed.clone())) { Some(7i64) } else { None } } } } } } } } }
    }

    pub fn htmlType1Start(&self, lower: String) -> bool {
        self.htmlType1Tags.iter().cloned().any(|tag| { (lower.starts_with(&(format!("{}{}", "<".to_string(), tag))) && { let next = (1i64 + crate::runtime::_str_length(&tag));
        ((next >= crate::runtime::_str_length(&lower)) || { let c = crate::runtime::_str_char_at(&lower, next);
        (((c == 32i64) || (c == 9i64)) || (c == 62i64)) }) }) })
    }

    pub fn htmlTagName(&self, t: String) -> (String, i64) {
        let mut i = 1i64;
        if ((i < crate::runtime::_str_length(&t)) && (crate::runtime::_str_char_at(&t, i) == 47i64)) { (i += 1i64); } else { (); }
        let start = i;
        while ((i < crate::runtime::_str_length(&t)) && (MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&t, i)).0) || (crate::runtime::_str_char_at(&t, i) == 45i64))) {
            (i += 1i64);
        };
        (asciiLower(crate::runtime::_str_substring(&t, start, i)), i)
    }

    pub fn htmlType6Start(&self, t: String) -> bool {
        let (name, after) = self.htmlTagName(t.clone());
        (self.htmlBlock6Tags.contains(&(name)) && ((after >= crate::runtime::_str_length(&t)) || { let c = crate::runtime::_str_char_at(&t, after);
        ((((c == 32i64) || (c == 9i64)) || (c == 62i64)) || (c == 47i64)) }))
    }

    pub fn htmlType7Start(&self, t: String) -> bool {
        let (name, _) = self.htmlTagName(t.clone());
        if (name.is_empty() || self.htmlType1Tags.contains(&(name))) { false } else { match self.completeTagLength(t.clone()) {
            Some(len) => crate::runtime::_str_substring_from(&t, len).chars().all(|__ch| { let c = (__ch as u32) as i64; ((c == 32i64) || (c == 9i64)) }),
            None => false,
        } }
    }

    pub fn completeTagLength(&self, t: String) -> Option<i64> {
        let n = crate::runtime::_str_length(&t);
        if ((n < 2i64) || (crate::runtime::_str_char_at(&t, 0i64) != 60i64)) { None } else { { let mut i = 1i64;
        if ((i < n) && (crate::runtime::_str_char_at(&t, i) == 47i64)) { (i += 1i64); } else { (); }
        if ((i >= n) || !(MdChars_isAsciiLetter((crate::runtime::_str_char_at(&t, i)).0))) { None } else { { while ((i < n) && (MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&t, i)).0) || (crate::runtime::_str_char_at(&t, i) == 45i64))) {
            (i += 1i64);
        };
        if ((i < n) && !(((((crate::runtime::_str_char_at(&t, i) == 32i64) || (crate::runtime::_str_char_at(&t, i) == 9i64)) || (crate::runtime::_str_char_at(&t, i) == 47i64)) || (crate::runtime::_str_char_at(&t, i) == 62i64)))) { None } else { { let mut ok = true;
        while ((ok && (i < n)) && (crate::runtime::_str_char_at(&t, i) != 62i64)) {
            if (crate::runtime::_str_char_at(&t, i) == 60i64) { ok = false; } else { (i += 1i64); }
        };
        if ((!(ok) || (i >= n)) || (crate::runtime::_str_char_at(&t, i) != 62i64)) { None } else { Some((i + 1i64)) } } } } } } }
    }

    pub fn isTableStart(&self, lines: &Vec<MdLine>, index: i64, content: String) -> bool {
        ((content.contains(char::from_u32((124i64) as u32).unwrap_or('\u{FFFD}')) && ((index + 1i64) < ((*lines).len() as i64))) && self.isTableDelimiter(lines[((index + 1i64)) as usize].clone().content))
    }

    pub fn isTableDelimiter(&self, content: String) -> bool {
        let count = 2125i64;
        let t = content.trim().to_string();
        (((!t.is_empty() && t.chars().all(|__ch| { let c = (__ch as u32) as i64; (((((c == 124i64) || (c == 45i64)) || (c == 58i64)) || (c == 32i64)) || (c == 9i64)) })) && t.contains(char::from_u32((45i64) as u32).unwrap_or('\u{FFFD}'))) && ((t.chars().filter(|__ch| (|__p0| { (__p0 == 124i64) })(((*__ch) as u32) as i64)).count() as i64) >= 1i64))
    }

    pub fn startsBlockquote(&self, trimmed: String) -> bool {
        trimmed.starts_with(">")
    }

    pub fn startsListOrQuote(&self, trimmed: String) -> bool {
        (self.startsBlockquote(trimmed.clone()) || self.startsListItem(trimmed.clone()).is_some())
    }

    pub fn startsListItem(&self, trimmed: String) -> Option<(bool, String, i64)> {
        if trimmed.is_empty() { None } else { { let c = crate::runtime::_str_char_at(&trimmed, 0i64);
        if ((((c == 45i64) || (c == 43i64)) || (c == 42i64)) && (((crate::runtime::_str_length(&trimmed) == 1i64) || (crate::runtime::_str_char_at(&trimmed, 1i64) == 32i64)) || (crate::runtime::_str_char_at(&trimmed, 1i64) == 9i64))) { { let spaces = crate::runtime::_str_substring_from(&trimmed, 1i64).chars().take_while(|__ch| { let ch = ((*__ch) as u32) as i64; ((ch == 32i64) || (ch == 9i64)) }).collect::<String>();
        Some((false, crate::runtime::_str_substring(&trimmed, 0i64, (1i64 + crate::runtime::_str_length(&spaces))), (1i64 + (crate::runtime::_str_length(&spaces)).max(1i64)))) } } else { { let mut i = 0i64;
        while (((i < crate::runtime::_str_length(&trimmed)) && (i < 9i64)) && isAsciiDigit((crate::runtime::_str_char_at(&trimmed, i)).0)) {
            (i += 1i64);
        };
        if ((((i >= 1i64) && (i < crate::runtime::_str_length(&trimmed))) && ((crate::runtime::_str_char_at(&trimmed, i) == 46i64) || (crate::runtime::_str_char_at(&trimmed, i) == 41i64))) && ((((i + 1i64) == crate::runtime::_str_length(&trimmed)) || (crate::runtime::_str_char_at(&trimmed, (i + 1i64)) == 32i64)) || (crate::runtime::_str_char_at(&trimmed, (i + 1i64)) == 9i64))) { { let markerCore = crate::runtime::_str_substring(&trimmed, 0i64, (i + 1i64));
        let spaces = crate::runtime::_str_substring_from(&trimmed, (i + 1i64)).chars().take_while(|__ch| { let ch = ((*__ch) as u32) as i64; ((ch == 32i64) || (ch == 9i64)) }).collect::<String>();
        Some((true, format!("{}{}", markerCore, spaces), (crate::runtime::_str_length(&markerCore) + (crate::runtime::_str_length(&spaces)).max(1i64)))) } } else { None } } } } }
    }

    pub fn limitDiag(&self, code: String, message: String) -> Diagnostic {
        Diagnostic { code: code, message: message, severity: Severity::Fatal, span: None, dialect: Some("markdown".to_string()), details: Vec::new() }
    }
}
impl MarkdownInstructionProcessor {
    pub fn start(&self) -> String {
        "".to_string()
    }

    pub fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> {
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        Stepped { state: format!("{}{}", state, input.text), batch: ProcessBatch_empty() }
    }

    pub fn stop(&self, state: String) -> ProcessBatch<VmToken> {
        let source = self.source.clone();
        let profile = self.profile.clone();
        let limits = self.limits.clone();
        let result = MarkdownBlocks::new(source, profile, limits).parse(state);
        ProcessBatch { values: result.tokens.clone(), diagnostics: result.diagnostics.clone() }
    }
}
impl MdLine {
    pub fn raw(&self) -> String {
        let content = self.content.clone();
        let ending = self.ending.clone();
        format!("{}{}", content, ending)
    }

    pub fn isBlank(&self) -> bool {
        let content = self.content.clone();
        content.chars().all(|__ch| { let c = (__ch as u32) as i64; ((c == 32i64) || (c == 9i64)) })
    }
}
impl<A: Clone> ProcessBatch<A> {
    pub fn map<B>(&self, f: impl Fn(A) -> B) -> ProcessBatch<B> {
        let values = self.values.clone();
        let diagnostics = self.diagnostics.clone();
        ProcessBatch { values: values.iter().cloned().map(f).collect::<Vec<_>>(), diagnostics: diagnostics }
    }

    pub fn concat(&self, other: ProcessBatch<A>) -> ProcessBatch<A> {
        let values = self.values.clone();
        let diagnostics = self.diagnostics.clone();
        ProcessBatch { values: [&(values)[..], &(other.values)[..]].concat(), diagnostics: [&(diagnostics)[..], &(other.diagnostics)[..]].concat() }
    }
}
impl RefDef {
    pub fn label(&self) -> String {
        let labelLex = self.labelLex.clone();
        crate::runtime::_str_substring(&labelLex, 1i64, ((labelLex.len() as i64) - 1i64))
    }

    pub fn destination(&self) -> String {
        let destLex = self.destLex.clone();
        unwrapDestinationSlice(destLex)
    }

    pub fn title(&self) -> Option<String> {
        let titleLex = self.titleLex.clone();
        if titleLex.is_empty() { None } else { Some(crate::runtime::_str_substring(&titleLex, 1i64, ((titleLex.len() as i64) - 1i64))) }
    }

    pub fn consumedText(&self) -> String {
        let indent = self.indent.clone();
        let labelLex = self.labelLex.clone();
        let colon = self.colon.clone();
        let afterColon = self.afterColon.clone();
        let destLex = self.destLex.clone();
        let betweenDestTitle = self.betweenDestTitle.clone();
        let titleLex = self.titleLex.clone();
        let trailing = self.trailing.clone();
        format!("{}{}{}{}{}{}{}{}", indent, labelLex, colon, afterColon, destLex, betweenDestTitle, titleLex, trailing)
    }
}
impl RustProcessor {
    pub fn start(&self) -> String {
        "".to_string()
    }

    pub fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> {
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        Stepped { state: format!("{}{}", state, input.text), batch: ProcessBatch_empty() }
    }

    pub fn stop(&self, text: String) -> ProcessBatch<VmToken> {
        let source = self.source.clone();
        let Start = SourcePosition { offset: 0i64, line: 1i64, column: 1i64 };
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        let lexed = lex(text);
        let items = collect(lexed.clone(), 0i64, (lexed.len() as i64), 0i64);
        let mut out: Vec<VmToken> = Vec::new();
        let mut position = Start.clone();
        let mut nextTokenId = 0i64;
        let mut nextItem = 0i64;
        let mut open: Vec<i64> = Vec::new();
        let mut openBody: Vec<i64> = Vec::new();
        let mut i = 0i64;
        while (i < (lexed.len() as i64)) {
            let mut lexeme = lexed[(i) as usize].clone().lexeme;
            let mut kind = lexed[(i) as usize].clone().kind;
            let mut instruction: VmInstruction = VmInstruction::Emit { role: None };
            if ((nextItem < (items.len() as i64)) && (items[(nextItem) as usize].clone().start == i)) { instruction = VmInstruction::Open { kind: items[(nextItem) as usize].clone().kind, role: None };
            open.push(items[(nextItem) as usize].clone().end);
            openBody.push(items[(nextItem) as usize].clone().bodyOpen);
            (nextItem += 1i64);
            (i += 1i64); } else { if (!open.is_empty() && (open[open.len() - 1].clone() == i)) { instruction = VmInstruction::Close { expectedKind: None, role: None };
            open = { let __v = (open).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
            openBody = { let __v = (openBody).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
            (i += 1i64); } else { if ((!open.is_empty() && (openBody[openBody.len() - 1].clone() >= 0i64)) && (i <= openBody[openBody.len() - 1].clone())) { (i += 1i64); } else { let mut limit = (lexed.len() as i64);
            if ((nextItem < (items.len() as i64)) && (items[(nextItem) as usize].clone().start < limit)) { limit = items[(nextItem) as usize].clone().start; } else { (); }
            if (!open.is_empty() && (open[open.len() - 1].clone() < limit)) { limit = open[open.len() - 1].clone(); } else { (); }
            if (limit <= i) { (i += 1i64); } else { let mut pieces: Vec<String> = Vec::new();
            while (i < limit) {
                pieces.push(lexed[(i) as usize].clone().lexeme);
                (i += 1i64);
            };
            kind = "rust.span".to_string();
            lexeme = (pieces).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""); } } } }
            let start = position.clone();
            let end = advance(start.clone(), lexeme.clone());
            position = end.clone();
            let channel = if (kind == "rust.ws".to_string()) { TokenChannel::Trivia.clone() } else { if ((kind == "rust.line-comment".to_string()) || (kind == "rust.block-comment".to_string())) { TokenChannel::Comment.clone() } else { TokenChannel::Syntax.clone() } };
            let token = SourceToken { id: nextTokenId, kind: kind.clone(), lexeme: lexeme.clone(), span: SourceSpan { source: source.clone(), start: start.clone(), end: end.clone() }, channel: channel.clone() };
            (nextTokenId += 1i64);
            out.push(VmToken { token: token, instruction: instruction.clone() });
        };
        ProcessBatch { values: out.clone(), diagnostics: Vec::new() }
    }
}
impl TreeVm {
    pub fn start(&self) -> VmState {
        let initial = VmState { stack: Vec::new(), nodeCount: 0i64, lastTokenId: None, diagnosticCount: 0i64, diagnosticLimitReported: false, finished: false, halted: false };
        initial
    }

    pub fn step(&self, state: VmState, input: VmToken) -> Stepped<VmState, UniNode> {
        let limits = self.limits.clone();
        let preflight = |__a0, __a1, __a2| self.preflight(__a0, __a1, __a2);
        let reframeProblem = |__a0, __a1| self.reframeProblem(__a0, __a1);
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        let finishedDiagnostic = Diagnostic { code: "uniml.vm.finished".to_string(), message: "tree VM cannot accept input or finish more than once".to_string(), severity: Severity::Error, span: None, dialect: None, details: Vec::new() };
        if state.finished { Stepped { state: state.clone(), batch: ProcessBatch { values: Vec::new(), diagnostics: vec![finishedDiagnostic.clone()] } } } else { if state.halted { Stepped { state: state.clone(), batch: ProcessBatch_empty() } } else { { fn record(d: Diagnostic, diagCount: &mut i64, diagLimitReported: &mut bool, diags: &mut Vec<Diagnostic>, halted: &mut bool, limits: Limits) {
            if ((*diagCount) < limits.maxDiagnostics) { ((*diagCount) += 1i64);
            (*diags).push(d.clone()); } else { if !((*diagLimitReported)) { (*diagLimitReported) = true;
            (*halted) = true;
            (*diags).push(Diagnostic { code: "uniml.limit.diagnostics".to_string(), message: format!("diagnostic count exceeds the {} limit", limits.maxDiagnostics), severity: Severity::Fatal.clone(), span: d.span.clone(), dialect: None.clone(), details: Vec::new() }); } else { (); } }
        }
        fn addTop(edge: UniEdge, stack: &mut Vec<VmFrame>) {
            let top = (*stack)[(*stack).len() - 1].clone();
            (*stack) = [&({ let __v = ((*stack)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() })[..], &[VmFrame { kind: top.kind.clone(), role: top.role.clone(), edges: [&(top.edges)[..], &[edge][..]].concat(), openingSpan: top.openingSpan.clone() }][..]].concat();
        }
        fn attach(branch: UniNode, role: Option<String>, roots: &mut Vec<UniNode>, stack: &mut Vec<VmFrame>) {
            if !(*stack).is_empty() { addTop(UniEdge { role: role.clone(), child: branch.clone() }, stack); } else { (*roots).push(branch.clone()); }
        }
        fn closeFrame(roots: &mut Vec<UniNode>, stack: &mut Vec<VmFrame>) {
            let frame = (*stack)[(*stack).len() - 1].clone();
            (*stack) = { let __v = ((*stack)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
            attach(buildBranch(frame.clone(), Origin::SourceBacked.clone()), frame.role.clone(), roots, stack);
        }
        let mut stack = state.stack.clone();
        let mut nodeCount = state.nodeCount.clone();
        let mut lastTokenId = state.lastTokenId.clone();
        let mut diagCount = state.diagnosticCount.clone();
        let mut diagLimitReported = state.diagnosticLimitReported.clone();
        let mut halted = state.halted.clone();
        let mut roots: Vec<UniNode> = Vec::new();
        let mut diags: Vec<Diagnostic> = Vec::new();
        match preflight(&stack.clone(), nodeCount.clone(), input.clone()) {
            Some(diagnostic) => { if (diagnostic.severity == Severity::Fatal) { halted = true; } else { (); }
            record(diagnostic.clone(), &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone()); },
            None => { validateToken(lastTokenId, input.token.clone()).iter().cloned().for_each(|__x| record(__x, &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone()));;
            lastTokenId = Some(input.token.id.clone());
            match (input.instruction).clone() {
                VmInstruction::Open { kind, role } => { (nodeCount += 2i64);
                stack.push(VmFrame { kind: kind.clone(), role: role.clone(), edges: vec![UniEdge { role: None.clone(), child: UniNode::Token { value: input.token.clone() } }], openingSpan: input.token.span.clone() }); },
                VmInstruction::Emit { role } => { (nodeCount += 1i64);
                let tokenNode = UniNode::Token { value: input.token.clone() };
                if !stack.is_empty() { addTop(UniEdge { role: role.clone(), child: tokenNode.clone() }, &mut stack); } else { roots.push(tokenNode.clone()); } },
                ref instruction @ VmInstruction::Reframe { ref closeBefore, ref open, ref closeAfter, ref role } => { match reframeProblem(&stack.clone(), (*instruction).clone()) {
                    Some(problem) => { (nodeCount += 1i64);
                    record(Diagnostic { span: Some(input.token.span.clone()), ..(problem).clone() }, &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone());
                    let tokenNode = UniNode::Token { value: input.token.clone() };
                    if !stack.is_empty() { addTop(UniEdge { role: (*role).clone(), child: tokenNode.clone() }, &mut stack); } else { roots.push(tokenNode.clone()); } },
                    None => { (nodeCount += (1i64 + ((*open).len() as i64)));
                    for _ in (*closeBefore).iter().cloned() {
                        closeFrame(&mut roots, &mut stack);
                    };
                    for spec in (*open).iter().cloned() {
                        stack.push(VmFrame { kind: spec.kind.clone(), role: spec.role.clone(), edges: Vec::new(), openingSpan: input.token.span.clone() });
                    };
                    let tokenNode = UniNode::Token { value: input.token.clone() };
                    if !stack.is_empty() { addTop(UniEdge { role: (*role).clone(), child: tokenNode.clone() }, &mut stack); } else { roots.push(tokenNode.clone()); }
                    for _ in (*closeAfter).iter().cloned() {
                        closeFrame(&mut roots, &mut stack);
                    }; },
                } },
                VmInstruction::Report { code, message, severity } => { (nodeCount += 1i64);
                let tokenNode = UniNode::Token { value: input.token.clone() };
                record(Diagnostic { code: code.clone(), message: message.clone(), severity: severity.clone(), span: Some(input.token.span.clone()), dialect: None, details: Vec::new() }, &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone());
                if (severity == Severity::Fatal) { halted = true; } else { (); }
                if !stack.is_empty() { addTop(UniEdge { role: None.clone(), child: tokenNode.clone() }, &mut stack); } else { roots.push(tokenNode.clone()); } },
                VmInstruction::Close { expectedKind, role } => { (nodeCount += 1i64);
                let tokenNode = UniNode::Token { value: input.token.clone() };
                if stack.is_empty() { record(Diagnostic { code: "uniml.vm.orphan-close".to_string(), message: "close instruction has no open node".to_string(), severity: Severity::Error.clone(), span: Some(input.token.span.clone()), dialect: None.clone(), details: Vec::new() }, &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone());
                roots.push(tokenNode.clone()); } else { let frame = stack[stack.len() - 1].clone();
                addTop(UniEdge { role: role.clone(), child: tokenNode.clone() }, &mut stack);
                match expectedKind {
                    Some(expected) if (expected != frame.kind) => { record(Diagnostic { code: "uniml.vm.mismatched-close".to_string(), message: format!("expected to close '{}' but current node is '{}'", expected, frame.kind), severity: Severity::Error.clone(), span: Some(input.token.span.clone()), dialect: None.clone(), details: vec![("expected".to_string(), expected.clone()), ("actual".to_string(), frame.kind.clone())] }, &mut diagCount, &mut diagLimitReported, &mut diags, &mut halted, limits.clone()); },
                    _ => { let closed = stack[stack.len() - 1].clone();
                    stack = { let __v = (stack).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
                    attach(buildBranch(closed.clone(), Origin::SourceBacked.clone()), closed.role.clone(), &mut roots, &mut stack); },
                } } },
            } },
        }
        Stepped { state: VmState { stack: stack.clone(), nodeCount: nodeCount.clone(), lastTokenId: lastTokenId.clone(), diagnosticCount: diagCount.clone(), diagnosticLimitReported: diagLimitReported.clone(), finished: false, halted: halted.clone() }, batch: ProcessBatch { values: roots.clone(), diagnostics: diags.clone() } } } } }
    }

    pub fn stop(&self, state: VmState) -> ProcessBatch<UniNode> {
        let limits = self.limits.clone();
        let empty = DialectRegistry { byName: std::collections::HashMap::new() };
        let finishedDiagnostic = Diagnostic { code: "uniml.vm.finished".to_string(), message: "tree VM cannot accept input or finish more than once".to_string(), severity: Severity::Error, span: None, dialect: None, details: Vec::new() };
        if state.finished { ProcessBatch { values: Vec::new(), diagnostics: vec![finishedDiagnostic.clone()] } } else { { fn record(d: Diagnostic, diagCount: &mut i64, diagLimitReported: &mut bool, diags: &mut Vec<Diagnostic>, limits: Limits) {
            if ((*diagCount) < limits.maxDiagnostics) { ((*diagCount) += 1i64);
            (*diags).push(d.clone()); } else { if !((*diagLimitReported)) { (*diagLimitReported) = true;
            (*diags).push(Diagnostic { code: "uniml.limit.diagnostics".to_string(), message: format!("diagnostic count exceeds the {} limit", limits.maxDiagnostics), severity: Severity::Fatal.clone(), span: d.span.clone(), dialect: None, details: Vec::new() }); } else { (); } }
        }
        let mut stack = state.stack.clone();
        let mut diagCount = state.diagnosticCount.clone();
        let mut diagLimitReported = state.diagnosticLimitReported.clone();
        let mut roots: Vec<UniNode> = Vec::new();
        let mut diags: Vec<Diagnostic> = Vec::new();
        while !stack.is_empty() {
            let frame = stack[stack.len() - 1].clone();
            stack = { let __v = (stack).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
            record(Diagnostic { code: "uniml.vm.unclosed-node".to_string(), message: format!("unclosed '{}' node at end of input", frame.kind), severity: Severity::Error.clone(), span: Some(frame.openingSpan.clone()), dialect: None, details: Vec::new() }, &mut diagCount, &mut diagLimitReported, &mut diags, limits.clone());
            let branch = buildBranch(frame.clone(), Origin::Synthetic { reason: format!("unclosed:{}", frame.kind) });
            if !stack.is_empty() { let top = stack[stack.len() - 1].clone();
            stack = [&({ let __v = (stack).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() })[..], &[VmFrame { kind: top.kind.clone(), role: top.role.clone(), edges: [&(top.edges)[..], &[UniEdge { role: frame.role.clone(), child: branch.clone() }][..]].concat(), openingSpan: top.openingSpan.clone() }][..]].concat(); } else { roots.push(branch.clone()); }
        };
        ProcessBatch { values: roots.clone(), diagnostics: diags.clone() } } }
    }

    pub fn preflight(&self, stack: &Vec<VmFrame>, nodeCount: i64, input: VmToken) -> Option<Diagnostic> {
        let limits = self.limits.clone();
        let reframeProblem = |__a0, __a1| self.reframeProblem(__a0, __a1);
        let token = input.token.clone();
        if (codePointCount(token.lexeme.clone()) > limits.maxTokenCodePoints) { Some(Diagnostic { code: "uniml.limit.token".to_string(), message: format!("token exceeds the {} code-point limit", limits.maxTokenCodePoints), severity: Severity::Fatal.clone(), span: Some(token.span.clone()), dialect: None.clone(), details: Vec::new() }) } else { { let requiredNodes: i64 = match (input.instruction).clone() {
            _ => 2i64,
            ref r @ VmInstruction::Reframe { ref closeBefore, ref open, ref closeAfter, ref role } => match reframeProblem(stack, (*r).clone()) {
                None => (1i64 + ((*open).len() as i64)),
                Some(_) => 1i64,
            },
            _ => 1i64,
        };
        let peakDepth: i64 = match (input.instruction).clone() {
            _ => (((*stack).len() as i64) + 1i64),
            ref r @ VmInstruction::Reframe { ref closeBefore, ref open, ref closeAfter, ref role } => match reframeProblem(stack, (*r).clone()) {
                None => ((((*stack).len() as i64) - ((*closeBefore).len() as i64)) + ((*open).len() as i64)),
                Some(_) => ((*stack).len() as i64),
            },
            _ => ((*stack).len() as i64),
        };
        if (peakDepth > limits.maxDepth) { Some(Diagnostic { code: "uniml.limit.depth".to_string(), message: format!("tree depth exceeds the {} frame limit", limits.maxDepth), severity: Severity::Fatal.clone(), span: Some(token.span.clone()), dialect: None.clone(), details: Vec::new() }) } else { if ((nodeCount + requiredNodes) > limits.maxNodes) { Some(Diagnostic { code: "uniml.limit.nodes".to_string(), message: format!("tree exceeds the {} node limit", limits.maxNodes), severity: Severity::Fatal.clone(), span: Some(token.span.clone()), dialect: None.clone(), details: Vec::new() }) } else { None } } } }
    }

    pub fn reframeProblem(&self, stack: &Vec<VmFrame>, instruction: VmInstruction) -> Option<Diagnostic> {
        let VmInstruction::Reframe { closeBefore: __dstruct_closeBefore, open: __dstruct_open, closeAfter: __dstruct_closeAfter, role } = instruction.clone() else { unreachable!() };
        let closeBefore = __dstruct_closeBefore.clone();
        let open = __dstruct_open.clone();
        let closeAfter = __dstruct_closeAfter.clone();
        if ((closeBefore.iter().cloned().any(|__p0| { __p0.is_empty() }) || closeAfter.iter().cloned().any(|__p0| { __p0.is_empty() })) || open.iter().cloned().any(|__p0| { __p0.kind.is_empty() })) { Some(Diagnostic { code: "uniml.vm.invalid-reframe".to_string(), message: "reframe kinds must be non-empty".to_string(), severity: Severity::Error.clone(), span: None.clone(), dialect: None.clone(), details: Vec::new() }) } else { { fn close(expected: String, kinds: &mut Vec<String>, problem: &mut Option<Diagnostic>) {
            if (*problem).is_none() { if (*kinds).is_empty() { (*problem) = Some(Diagnostic { code: "uniml.vm.reframe-underflow".to_string(), message: format!("reframe cannot close '{}' because no frame is open", expected), severity: Severity::Error.clone(), span: None.clone(), dialect: None.clone(), details: Vec::new() }); } else { if ((*kinds)[(*kinds).len() - 1].clone() != expected) { (*problem) = Some(Diagnostic { code: "uniml.vm.mismatched-reframe".to_string(), message: format!("expected to reframe '{}' but current node is '{}'", expected, (*kinds)[(*kinds).len() - 1].clone()), severity: Severity::Error.clone(), span: None.clone(), dialect: None.clone(), details: vec![("expected".to_string(), expected.clone()), ("actual".to_string(), (*kinds)[(*kinds).len() - 1].clone())] }); } else { (*kinds) = { let __v = ((*kinds)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() }; } } } else { (); }
        }
        let mut kinds = (*stack).iter().cloned().map(|__p0| { __p0.kind }).collect::<Vec<_>>();
        let mut problem: Option<Diagnostic> = None.clone();
        closeBefore.iter().cloned().for_each(|__x| close(__x, &mut kinds, &mut problem));;
        if problem.is_none() { for spec in open.iter().cloned() {
            kinds.push(spec.kind.clone());
        };
        closeAfter.iter().cloned().for_each(|__x| close(__x, &mut kinds, &mut problem));; } else { (); }
        problem } }
    }
}
#[allow(dead_code)]
pub trait Processor<S, I, O> {
    fn start(&self) -> S;
    fn step(&self, state: S, input: I) -> Stepped<S, O>;
    fn stop(&self, state: S) -> ProcessBatch<O>;
}
#[allow(dead_code)]
pub trait DialectAdapter {
    fn id(&self) -> String;
    fn aliases(&self) -> Vec<String> { Vec::new() }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>>;
    fn project(&self, tree: UniNode) -> Projection { Projection::Identity { tree: tree } }
}
impl Processor<String, SourceChunk, VmToken> for LiteralProcessor {
    fn start(&self) -> String { self.start() }
    fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> { self.step(state, input) }
    fn stop(&self, state: String) -> ProcessBatch<VmToken> { self.stop(state) }
}
impl Processor<VmState, VmToken, UniNode> for TreeVm {
    fn start(&self) -> VmState { self.start() }
    fn step(&self, state: VmState, input: VmToken) -> Stepped<VmState, UniNode> { self.step(state, input) }
    fn stop(&self, state: VmState) -> ProcessBatch<UniNode> { self.stop(state) }
}
impl DialectAdapter for ConfiguredMarkdownDialect {
    fn id(&self) -> String { self.id() }
    fn aliases(&self) -> Vec<String> { self.aliases() }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { self.instructions(source) }
    fn project(&self, tree: UniNode) -> Projection { self.project(tree) }
}
impl Processor<String, SourceChunk, VmToken> for MarkdownInstructionProcessor {
    fn start(&self) -> String { self.start() }
    fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> { self.step(state, input) }
    fn stop(&self, state: String) -> ProcessBatch<VmToken> { self.stop(state) }
}
impl Processor<String, SourceChunk, VmToken> for RustProcessor {
    fn start(&self) -> String { self.start() }
    fn step(&self, state: String, input: SourceChunk) -> Stepped<String, VmToken> { self.step(state, input) }
    fn stop(&self, state: String) -> ProcessBatch<VmToken> { self.stop(state) }
}
#[derive(Clone, Copy)]
pub struct Literal;
impl DialectAdapter for Literal {
    fn id(&self) -> String { "uniml.literal".to_string() }
    fn aliases(&self) -> Vec<String> { vec!["literal".to_string(), "text".to_string(), "unknown".to_string()] }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { Literal_instructions(source) }
}
#[derive(Clone, Copy)]
pub struct CommonMarkDialect;
impl DialectAdapter for CommonMarkDialect {
    fn id(&self) -> String { "markdown.commonmark.0.31.2".to_string() }
    fn aliases(&self) -> Vec<String> { vec!["markdown".to_string(), "commonmark".to_string(), "text/markdown".to_string()] }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { CommonMarkDialect_instructions(source) }
}
#[derive(Clone, Copy)]
pub struct GfmDialect;
impl DialectAdapter for GfmDialect {
    fn id(&self) -> String { "markdown.gfm.0.29".to_string() }
    fn aliases(&self) -> Vec<String> { vec!["gfm".to_string()] }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { GfmDialect_instructions(source) }
}
#[derive(Clone, Copy)]
pub struct ScalaScriptMarkdownDialect;
impl DialectAdapter for ScalaScriptMarkdownDialect {
    fn id(&self) -> String { "markdown.scalascript".to_string() }
    fn aliases(&self) -> Vec<String> { vec!["ssc-markdown".to_string()] }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { ScalaScriptMarkdownDialect_instructions(source) }
}
#[derive(Clone, Copy)]
pub struct RustDialect;
impl DialectAdapter for RustDialect {
    fn id(&self) -> String { "uniml.rust".to_string() }
    fn aliases(&self) -> Vec<String> { vec!["rust".to_string(), "rs".to_string()] }
    fn instructions(&self, source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> { RustDialect_instructions(source) }
}
impl Container {
    pub fn frame(&self) -> String {
        let Blockquote = "markdown.blockquote".to_string();
        let List = "markdown.list".to_string();
        let ListItem = "markdown.list-item".to_string();
        match self {
            Container::Blockquote => Blockquote,
            Container::ListFrame { .. } => List,
            Container::ListItemFrame { .. } => ListItem,
        }
    }
}

pub fn Alphabet_isWhitespace(c: i64) -> bool {
    (((((c == 32i64) || (c == 9i64)) || (c == 10i64)) || (c == 13i64)) || (c == 12i64))
}

pub fn Alphabet_isDigit(c: i64) -> bool {
    ((c >= 48i64) && (c <= 57i64))
}

pub fn Alphabet_isOctDigit(c: i64) -> bool {
    ((c >= 48i64) && (c <= 55i64))
}

pub fn Alphabet_isHexDigit(c: i64) -> bool {
    ((Alphabet_isDigit(c) || ((c >= 97i64) && (c <= 102i64))) || ((c >= 65i64) && (c <= 70i64)))
}

pub fn Alphabet_isAsciiUpper(c: i64) -> bool {
    ((c >= 65i64) && (c <= 90i64))
}

pub fn isUpperStart(c: i64) -> bool {
    (Alphabet_isAsciiUpper(c) || ((c >= 128i64) && inUpperRanges(crate::runtime::_to_int(&c))))
}

pub fn inUpperRanges(cp: i64) -> bool {
    let upperRanges = vec![192i64, 214i64, 216i64, 222i64, 256i64, 256i64, 258i64, 258i64, 260i64, 260i64, 262i64, 262i64, 264i64, 264i64, 266i64, 266i64, 268i64, 268i64, 270i64, 270i64, 272i64, 272i64, 274i64, 274i64, 276i64, 276i64, 278i64, 278i64, 280i64, 280i64, 282i64, 282i64, 284i64, 284i64, 286i64, 286i64, 288i64, 288i64, 290i64, 290i64, 292i64, 292i64, 294i64, 294i64, 296i64, 296i64, 298i64, 298i64, 300i64, 300i64, 302i64, 302i64, 304i64, 304i64, 306i64, 306i64, 308i64, 308i64, 310i64, 310i64, 313i64, 313i64, 315i64, 315i64, 317i64, 317i64, 319i64, 319i64, 321i64, 321i64, 323i64, 323i64, 325i64, 325i64, 327i64, 327i64, 330i64, 330i64, 332i64, 332i64, 334i64, 334i64, 336i64, 336i64, 338i64, 338i64, 340i64, 340i64, 342i64, 342i64, 344i64, 344i64, 346i64, 346i64, 348i64, 348i64, 350i64, 350i64, 352i64, 352i64, 354i64, 354i64, 356i64, 356i64, 358i64, 358i64, 360i64, 360i64, 362i64, 362i64, 364i64, 364i64, 366i64, 366i64, 368i64, 368i64, 370i64, 370i64, 372i64, 372i64, 374i64, 374i64, 376i64, 377i64, 379i64, 379i64, 381i64, 381i64, 385i64, 386i64, 388i64, 388i64, 390i64, 391i64, 393i64, 395i64, 398i64, 401i64, 403i64, 404i64, 406i64, 408i64, 412i64, 413i64, 415i64, 416i64, 418i64, 418i64, 420i64, 420i64, 422i64, 423i64, 425i64, 425i64, 428i64, 428i64, 430i64, 431i64, 433i64, 435i64, 437i64, 437i64, 439i64, 440i64, 444i64, 444i64, 452i64, 452i64, 455i64, 455i64, 458i64, 458i64, 461i64, 461i64, 463i64, 463i64, 465i64, 465i64, 467i64, 467i64, 469i64, 469i64, 471i64, 471i64, 473i64, 473i64, 475i64, 475i64, 478i64, 478i64, 480i64, 480i64, 482i64, 482i64, 484i64, 484i64, 486i64, 486i64, 488i64, 488i64, 490i64, 490i64, 492i64, 492i64, 494i64, 494i64, 497i64, 497i64, 500i64, 500i64, 502i64, 504i64, 506i64, 506i64, 508i64, 508i64, 510i64, 510i64, 512i64, 512i64, 514i64, 514i64, 516i64, 516i64, 518i64, 518i64, 520i64, 520i64, 522i64, 522i64, 524i64, 524i64, 526i64, 526i64, 528i64, 528i64, 530i64, 530i64, 532i64, 532i64, 534i64, 534i64, 536i64, 536i64, 538i64, 538i64, 540i64, 540i64, 542i64, 542i64, 544i64, 544i64, 546i64, 546i64, 548i64, 548i64, 550i64, 550i64, 552i64, 552i64, 554i64, 554i64, 556i64, 556i64, 558i64, 558i64, 560i64, 560i64, 562i64, 562i64, 570i64, 571i64, 573i64, 574i64, 577i64, 577i64, 579i64, 582i64, 584i64, 584i64, 586i64, 586i64, 588i64, 588i64, 590i64, 590i64, 880i64, 880i64, 882i64, 882i64, 886i64, 886i64, 895i64, 895i64, 902i64, 902i64, 904i64, 906i64, 908i64, 908i64, 910i64, 911i64, 913i64, 929i64, 931i64, 939i64, 975i64, 975i64, 978i64, 980i64, 984i64, 984i64, 986i64, 986i64, 988i64, 988i64, 990i64, 990i64, 992i64, 992i64, 994i64, 994i64, 996i64, 996i64, 998i64, 998i64, 1000i64, 1000i64, 1002i64, 1002i64, 1004i64, 1004i64, 1006i64, 1006i64, 1012i64, 1012i64, 1015i64, 1015i64, 1017i64, 1018i64, 1021i64, 1071i64, 1120i64, 1120i64, 1122i64, 1122i64, 1124i64, 1124i64, 1126i64, 1126i64, 1128i64, 1128i64, 1130i64, 1130i64, 1132i64, 1132i64, 1134i64, 1134i64, 1136i64, 1136i64, 1138i64, 1138i64, 1140i64, 1140i64, 1142i64, 1142i64, 1144i64, 1144i64, 1146i64, 1146i64, 1148i64, 1148i64, 1150i64, 1150i64, 1152i64, 1152i64, 1162i64, 1162i64, 1164i64, 1164i64, 1166i64, 1166i64, 1168i64, 1168i64, 1170i64, 1170i64, 1172i64, 1172i64, 1174i64, 1174i64, 1176i64, 1176i64, 1178i64, 1178i64, 1180i64, 1180i64, 1182i64, 1182i64, 1184i64, 1184i64, 1186i64, 1186i64, 1188i64, 1188i64, 1190i64, 1190i64, 1192i64, 1192i64, 1194i64, 1194i64, 1196i64, 1196i64, 1198i64, 1198i64, 1200i64, 1200i64, 1202i64, 1202i64, 1204i64, 1204i64, 1206i64, 1206i64, 1208i64, 1208i64, 1210i64, 1210i64, 1212i64, 1212i64, 1214i64, 1214i64, 1216i64, 1217i64, 1219i64, 1219i64, 1221i64, 1221i64, 1223i64, 1223i64, 1225i64, 1225i64, 1227i64, 1227i64, 1229i64, 1229i64, 1232i64, 1232i64, 1234i64, 1234i64, 1236i64, 1236i64, 1238i64, 1238i64, 1240i64, 1240i64, 1242i64, 1242i64, 1244i64, 1244i64, 1246i64, 1246i64, 1248i64, 1248i64, 1250i64, 1250i64, 1252i64, 1252i64, 1254i64, 1254i64, 1256i64, 1256i64, 1258i64, 1258i64, 1260i64, 1260i64, 1262i64, 1262i64, 1264i64, 1264i64, 1266i64, 1266i64, 1268i64, 1268i64, 1270i64, 1270i64, 1272i64, 1272i64, 1274i64, 1274i64, 1276i64, 1276i64, 1278i64, 1278i64, 1280i64, 1280i64, 1282i64, 1282i64, 1284i64, 1284i64, 1286i64, 1286i64, 1288i64, 1288i64, 1290i64, 1290i64, 1292i64, 1292i64, 1294i64, 1294i64, 1296i64, 1296i64, 1298i64, 1298i64, 1300i64, 1300i64, 1302i64, 1302i64, 1304i64, 1304i64, 1306i64, 1306i64, 1308i64, 1308i64, 1310i64, 1310i64, 1312i64, 1312i64, 1314i64, 1314i64, 1316i64, 1316i64, 1318i64, 1318i64, 1320i64, 1320i64, 1322i64, 1322i64, 1324i64, 1324i64, 1326i64, 1326i64, 1329i64, 1366i64, 4256i64, 4293i64, 4295i64, 4295i64, 4301i64, 4301i64, 5024i64, 5109i64, 7312i64, 7354i64, 7357i64, 7359i64, 7680i64, 7680i64, 7682i64, 7682i64, 7684i64, 7684i64, 7686i64, 7686i64, 7688i64, 7688i64, 7690i64, 7690i64, 7692i64, 7692i64, 7694i64, 7694i64, 7696i64, 7696i64, 7698i64, 7698i64, 7700i64, 7700i64, 7702i64, 7702i64, 7704i64, 7704i64, 7706i64, 7706i64, 7708i64, 7708i64, 7710i64, 7710i64, 7712i64, 7712i64, 7714i64, 7714i64, 7716i64, 7716i64, 7718i64, 7718i64, 7720i64, 7720i64, 7722i64, 7722i64, 7724i64, 7724i64, 7726i64, 7726i64, 7728i64, 7728i64, 7730i64, 7730i64, 7732i64, 7732i64, 7734i64, 7734i64, 7736i64, 7736i64, 7738i64, 7738i64, 7740i64, 7740i64, 7742i64, 7742i64, 7744i64, 7744i64, 7746i64, 7746i64, 7748i64, 7748i64, 7750i64, 7750i64, 7752i64, 7752i64, 7754i64, 7754i64, 7756i64, 7756i64, 7758i64, 7758i64, 7760i64, 7760i64, 7762i64, 7762i64, 7764i64, 7764i64, 7766i64, 7766i64, 7768i64, 7768i64, 7770i64, 7770i64, 7772i64, 7772i64, 7774i64, 7774i64, 7776i64, 7776i64, 7778i64, 7778i64, 7780i64, 7780i64, 7782i64, 7782i64, 7784i64, 7784i64, 7786i64, 7786i64, 7788i64, 7788i64, 7790i64, 7790i64, 7792i64, 7792i64, 7794i64, 7794i64, 7796i64, 7796i64, 7798i64, 7798i64, 7800i64, 7800i64, 7802i64, 7802i64, 7804i64, 7804i64, 7806i64, 7806i64, 7808i64, 7808i64, 7810i64, 7810i64, 7812i64, 7812i64, 7814i64, 7814i64, 7816i64, 7816i64, 7818i64, 7818i64, 7820i64, 7820i64, 7822i64, 7822i64, 7824i64, 7824i64, 7826i64, 7826i64, 7828i64, 7828i64, 7838i64, 7838i64, 7840i64, 7840i64, 7842i64, 7842i64, 7844i64, 7844i64, 7846i64, 7846i64, 7848i64, 7848i64, 7850i64, 7850i64, 7852i64, 7852i64, 7854i64, 7854i64, 7856i64, 7856i64, 7858i64, 7858i64, 7860i64, 7860i64, 7862i64, 7862i64, 7864i64, 7864i64, 7866i64, 7866i64, 7868i64, 7868i64, 7870i64, 7870i64, 7872i64, 7872i64, 7874i64, 7874i64, 7876i64, 7876i64, 7878i64, 7878i64, 7880i64, 7880i64, 7882i64, 7882i64, 7884i64, 7884i64, 7886i64, 7886i64, 7888i64, 7888i64, 7890i64, 7890i64, 7892i64, 7892i64, 7894i64, 7894i64, 7896i64, 7896i64, 7898i64, 7898i64, 7900i64, 7900i64, 7902i64, 7902i64, 7904i64, 7904i64, 7906i64, 7906i64, 7908i64, 7908i64, 7910i64, 7910i64, 7912i64, 7912i64, 7914i64, 7914i64, 7916i64, 7916i64, 7918i64, 7918i64, 7920i64, 7920i64, 7922i64, 7922i64, 7924i64, 7924i64, 7926i64, 7926i64, 7928i64, 7928i64, 7930i64, 7930i64, 7932i64, 7932i64, 7934i64, 7934i64, 7944i64, 7951i64, 7960i64, 7965i64, 7976i64, 7983i64, 7992i64, 7999i64, 8008i64, 8013i64, 8025i64, 8025i64, 8027i64, 8027i64, 8029i64, 8029i64, 8031i64, 8031i64, 8040i64, 8047i64, 8120i64, 8123i64, 8136i64, 8139i64, 8152i64, 8155i64, 8168i64, 8172i64, 8184i64, 8187i64, 8450i64, 8450i64, 8455i64, 8455i64, 8459i64, 8461i64, 8464i64, 8466i64, 8469i64, 8469i64, 8473i64, 8477i64, 8484i64, 8484i64, 8486i64, 8486i64, 8488i64, 8488i64, 8490i64, 8493i64, 8496i64, 8499i64, 8510i64, 8511i64, 8517i64, 8517i64, 8544i64, 8559i64, 8579i64, 8579i64, 9398i64, 9423i64, 11264i64, 11311i64, 11360i64, 11360i64, 11362i64, 11364i64, 11367i64, 11367i64, 11369i64, 11369i64, 11371i64, 11371i64, 11373i64, 11376i64, 11378i64, 11378i64, 11381i64, 11381i64, 11390i64, 11392i64, 11394i64, 11394i64, 11396i64, 11396i64, 11398i64, 11398i64, 11400i64, 11400i64, 11402i64, 11402i64, 11404i64, 11404i64, 11406i64, 11406i64, 11408i64, 11408i64, 11410i64, 11410i64, 11412i64, 11412i64, 11414i64, 11414i64, 11416i64, 11416i64, 11418i64, 11418i64, 11420i64, 11420i64, 11422i64, 11422i64, 11424i64, 11424i64, 11426i64, 11426i64, 11428i64, 11428i64, 11430i64, 11430i64, 11432i64, 11432i64, 11434i64, 11434i64, 11436i64, 11436i64, 11438i64, 11438i64, 11440i64, 11440i64, 11442i64, 11442i64, 11444i64, 11444i64, 11446i64, 11446i64, 11448i64, 11448i64, 11450i64, 11450i64, 11452i64, 11452i64, 11454i64, 11454i64, 11456i64, 11456i64, 11458i64, 11458i64, 11460i64, 11460i64, 11462i64, 11462i64, 11464i64, 11464i64, 11466i64, 11466i64, 11468i64, 11468i64, 11470i64, 11470i64, 11472i64, 11472i64, 11474i64, 11474i64, 11476i64, 11476i64, 11478i64, 11478i64, 11480i64, 11480i64, 11482i64, 11482i64, 11484i64, 11484i64, 11486i64, 11486i64, 11488i64, 11488i64, 11490i64, 11490i64, 11499i64, 11499i64, 11501i64, 11501i64, 11506i64, 11506i64, 42560i64, 42560i64, 42562i64, 42562i64, 42564i64, 42564i64, 42566i64, 42566i64, 42568i64, 42568i64, 42570i64, 42570i64, 42572i64, 42572i64, 42574i64, 42574i64, 42576i64, 42576i64, 42578i64, 42578i64, 42580i64, 42580i64, 42582i64, 42582i64, 42584i64, 42584i64, 42586i64, 42586i64, 42588i64, 42588i64, 42590i64, 42590i64, 42592i64, 42592i64, 42594i64, 42594i64, 42596i64, 42596i64, 42598i64, 42598i64, 42600i64, 42600i64, 42602i64, 42602i64, 42604i64, 42604i64, 42624i64, 42624i64, 42626i64, 42626i64, 42628i64, 42628i64, 42630i64, 42630i64, 42632i64, 42632i64, 42634i64, 42634i64, 42636i64, 42636i64, 42638i64, 42638i64, 42640i64, 42640i64, 42642i64, 42642i64, 42644i64, 42644i64, 42646i64, 42646i64, 42648i64, 42648i64, 42650i64, 42650i64, 42786i64, 42786i64, 42788i64, 42788i64, 42790i64, 42790i64, 42792i64, 42792i64, 42794i64, 42794i64, 42796i64, 42796i64, 42798i64, 42798i64, 42802i64, 42802i64, 42804i64, 42804i64, 42806i64, 42806i64, 42808i64, 42808i64, 42810i64, 42810i64, 42812i64, 42812i64, 42814i64, 42814i64, 42816i64, 42816i64, 42818i64, 42818i64, 42820i64, 42820i64, 42822i64, 42822i64, 42824i64, 42824i64, 42826i64, 42826i64, 42828i64, 42828i64, 42830i64, 42830i64, 42832i64, 42832i64, 42834i64, 42834i64, 42836i64, 42836i64, 42838i64, 42838i64, 42840i64, 42840i64, 42842i64, 42842i64, 42844i64, 42844i64, 42846i64, 42846i64, 42848i64, 42848i64, 42850i64, 42850i64, 42852i64, 42852i64, 42854i64, 42854i64, 42856i64, 42856i64, 42858i64, 42858i64, 42860i64, 42860i64, 42862i64, 42862i64, 42873i64, 42873i64, 42875i64, 42875i64, 42877i64, 42878i64, 42880i64, 42880i64, 42882i64, 42882i64, 42884i64, 42884i64, 42886i64, 42886i64, 42891i64, 42891i64, 42893i64, 42893i64, 42896i64, 42896i64, 42898i64, 42898i64, 42902i64, 42902i64, 42904i64, 42904i64, 42906i64, 42906i64, 42908i64, 42908i64, 42910i64, 42910i64, 42912i64, 42912i64, 42914i64, 42914i64, 42916i64, 42916i64, 42918i64, 42918i64, 42920i64, 42920i64, 42922i64, 42926i64, 42928i64, 42932i64, 42934i64, 42934i64, 42936i64, 42936i64, 42938i64, 42938i64, 42940i64, 42940i64, 42942i64, 42942i64, 42944i64, 42944i64, 42946i64, 42946i64, 42948i64, 42951i64, 42953i64, 42953i64, 42960i64, 42960i64, 42966i64, 42966i64, 42968i64, 42968i64, 42997i64, 42997i64, 65313i64, 65338i64];
    let mut lo = 0i64;
    let mut hi = (((upperRanges.len() as i64) / 2i64) - 1i64);
    let mut found = false;
    while ((lo <= hi) && !(found)) {
        let mid = ((lo + hi) / 2i64);
        if (cp < upperRanges[((mid * 2i64)) as usize].clone()) { hi = (mid - 1i64); } else { if (cp > upperRanges[(((mid * 2i64) + 1i64)) as usize].clone()) { lo = (mid + 1i64); } else { found = true; } }
    };
    found
}

pub fn fromString(source: SourceId, text: String) -> SourceInput {
    SourceInput { source: source, chunks: vec![SourceChunk { text: text }] }
}

pub fn isHighSurrogate(char: i64) -> bool {
    ((char >= 55296i64) && (char <= 56319i64))
}

pub fn isLowSurrogate(char: i64) -> bool {
    ((char >= 56320i64) && (char <= 57343i64))
}

pub fn codePointCount(text: String) -> i64 {
    let count = 2125i64;
    let chars = text.encode_utf16().map(|__u| __u as i64).collect::<Vec<i64>>();
    let mut index = 0i64;
    let mut count = 0i64;
    while (index < (chars.len() as i64)) {
        let char = chars[(index) as usize].clone();
        if ((isHighSurrogate(char) && ((index + 1i64) < (chars.len() as i64))) && isLowSurrogate(chars[((index + 1i64)) as usize].clone())) { (index += 2i64); } else { (index += 1i64); }
        (count += 1i64);
    };
    count
}

pub fn advance(position: SourcePosition, lexeme: String) -> SourcePosition {
    let chars = lexeme.encode_utf16().map(|__u| __u as i64).collect::<Vec<i64>>();
    let mut index = 0i64;
    let mut offset = position.offset.clone();
    let mut line = position.line.clone();
    let mut column = position.column.clone();
    while (index < (chars.len() as i64)) {
        let char = chars[(index) as usize].clone();
        let width = if ((isHighSurrogate(char.clone()) && ((index + 1i64) < (chars.len() as i64))) && isLowSurrogate(chars[((index + 1i64)) as usize].clone())) { 2i64 } else { 1i64 };
        if (char == 10i64) { (line += 1i64);
        column = 1i64; } else { (column += 1i64); }
        (offset += 1i64);
        (index += width);
    };
    SourcePosition { offset: offset.clone(), line: line.clone(), column: column.clone() }
}

pub fn sourceTokens(root: UniNode) -> Vec<SourceToken> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let List = "markdown.list".to_string();
    let mut result: Vec<SourceToken> = Vec::new();
    let mut pending = vec![root];
    while !pending.is_empty() {
        match (pending[0].clone()).clone() {
            UniNode::Token { value } => { result.push(value);
            pending = pending[1..].to_vec(); },
            UniNode::Branch { kind: _, edges, span: _, origin: _ } => { pending = [&(edges.iter().cloned().map(|__p0| { __p0.child }).collect::<Vec<_>>().clone().into_iter().collect::<Vec<_>>())[..], &(pending[1..].to_vec())[..]].concat(); },
        }
    };
    result
}

pub fn ProcessBatch_empty<A>() -> ProcessBatch<A> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    ProcessBatch { values: Vec::new(), diagnostics: Vec::new() }
}

pub fn value<A>(value: A) -> ProcessBatch<A> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    ProcessBatch { values: vec![value], diagnostics: Vec::new() }
}

pub fn apply(adapters: Vec<std::rc::Rc<dyn DialectAdapter>>) -> Either<String, DialectRegistry> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    adapters.iter().cloned().fold(Either::Right(empty), |result, adapter| { match result { Either::Left(v) => Either::Left(v), Either::Right(v) => { let __p0 = v; __p0.register(adapter) } } })
}

pub fn Literal_instructions(source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
    std::rc::Rc::new(LiteralProcessor { source: source.source.clone() })
}

pub fn buildBranch(frame: VmFrame, origin: Origin) -> UniNode {
    let end = match frame.edges.last().cloned() {
        Some(UniEdge { role: _, child: UniNode::Token { value: token } }) => token.span.end,
        Some(UniEdge { role: _, child: UniNode::Branch { kind: _, edges: _, span, origin: _ } }) => span.end.clone(),
        None => frame.openingSpan.end.clone(),
    };
    UniNode::Branch { kind: frame.kind.clone(), edges: frame.edges.clone(), span: SourceSpan { source: frame.openingSpan.source.clone(), start: frame.openingSpan.start.clone(), end: end.clone() }, origin: origin.clone() }
}

pub fn validateToken(lastTokenId: Option<i64>, token: SourceToken) -> Vec<Diagnostic> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let mut diagnostics: Vec<Diagnostic> = Vec::new();
    for previous in lastTokenId.iter().cloned() {
        if (token.id <= previous) { diagnostics.push(Diagnostic { code: "uniml.token.non-monotonic-id".to_string(), message: format!("token id {} must be greater than previous id {}", token.id, previous), severity: Severity::Error.clone(), span: Some(token.span.clone()), dialect: None.clone(), details: Vec::new() }) } else { () };
    };
    if (token.span.end.offset < token.span.start.offset) { diagnostics.push(Diagnostic { code: "uniml.token.invalid-span".to_string(), message: "token span end precedes its start".to_string(), severity: Severity::Error.clone(), span: Some(token.span.clone()), dialect: None, details: Vec::new() }); } else { (); }
    diagnostics
}

pub fn UniAlphabet_isWhitespace(char: i64) -> bool {
    (((((char == 32i64) || (char == 9i64)) || (char == 10i64)) || (char == 13i64)) || (char == 12i64))
}

pub fn UniAlphabet_isDigit(char: i64) -> bool {
    ((char >= 48i64) && (char <= 57i64))
}

pub fn UniAlphabet_isOctDigit(char: i64) -> bool {
    ((char >= 48i64) && (char <= 55i64))
}

pub fn UniAlphabet_isHexDigit(char: i64) -> bool {
    ((UniAlphabet_isDigit(char) || ((char >= 97i64) && (char <= 102i64))) || ((char >= 65i64) && (char <= 70i64)))
}

pub fn UniAlphabet_isAsciiUpper(char: i64) -> bool {
    ((char >= 65i64) && (char <= 90i64))
}

pub fn isAsciiLower(char: i64) -> bool {
    ((char >= 97i64) && (char <= 122i64))
}

pub fn UniAlphabet_isAsciiLetter(char: i64) -> bool {
    (isAsciiLower(char) || UniAlphabet_isAsciiUpper(char))
}

pub fn UniAlphabet_isAsciiAlnum(char: i64) -> bool {
    (UniAlphabet_isAsciiLetter(char) || UniAlphabet_isDigit(char))
}

pub fn isNonAscii(char: i64) -> bool {
    (char >= 128i64)
}

pub fn isIdStart(char: i64) -> bool {
    (((UniAlphabet_isAsciiLetter(char) || (char == 95i64)) || (char == 36i64)) || (char >= 128i64))
}

pub fn isIdPart(char: i64) -> bool {
    (isIdStart(char) || UniAlphabet_isDigit(char))
}

pub fn isOpChar(char: i64) -> bool {
    (((((((((((((((((char == 43i64) || (char == 45i64)) || (char == 42i64)) || (char == 47i64)) || (char == 37i64)) || (char == 60i64)) || (char == 62i64)) || (char == 61i64)) || (char == 33i64)) || (char == 38i64)) || (char == 124i64)) || (char == 94i64)) || (char == 126i64)) || (char == 58i64)) || (char == 35i64)) || (char == 64i64)) || (char == 63i64))
}

pub fn isTypeNameStart(char: i64) -> bool {
    isUpperStart(char)
}

pub fn UniML_parse(source: SourceInput, dialect: std::rc::Rc<dyn DialectAdapter>, limits: Limits) -> ParseResult {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let lexer = dialect.instructions(source.clone());
    let mut lexState = lexer.start();
    let mut tokens: Vec<VmToken> = Vec::new();
    let mut diagnostics: Vec<Diagnostic> = Vec::new();
    for chunk in source.chunks.iter().cloned() {
        let stepped = lexer.step(lexState, chunk);
        lexState = stepped.state.clone();
        tokens.extend((stepped.batch.values).iter().cloned());
        diagnostics.extend((stepped.batch.diagnostics).iter().cloned());
    };
    let lexFinal = lexer.stop(lexState.clone());
    tokens.extend((lexFinal.values).iter().cloned());
    diagnostics.extend((lexFinal.diagnostics).iter().cloned());
    let vm = TreeVm { limits: limits };
    let mut vmState = vm.start();
    let mut roots: Vec<UniNode> = Vec::new();
    for token in tokens.iter().cloned() {
        let stepped = vm.step(vmState, token);
        vmState = stepped.state.clone();
        roots.extend((stepped.batch.values).iter().cloned());
        diagnostics.extend((stepped.batch.diagnostics).iter().cloned());
    };
    let vmFinal = vm.stop(vmState.clone());
    roots.extend((vmFinal.values).iter().cloned());
    diagnostics.extend((vmFinal.diagnostics).iter().cloned());
    let status = if diagnostics.iter().cloned().any(|__p0| { (__p0.severity == Severity::Fatal) }) { CompletionStatus::Halted.clone() } else { if diagnostics.iter().cloned().any(|__p0| { (__p0.severity == Severity::Error) }) { CompletionStatus::Incomplete.clone() } else { CompletionStatus::Complete.clone() } };
    ParseResult { roots: roots.clone(), diagnostics: diagnostics.clone(), status: status }
}

pub fn normalizeLabel(raw: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let trimmed = raw.trim().to_string();
    let mut builder: Vec<String> = Vec::new();
    let mut inSpace = false;
    let mut li = 0i64;
    while (li < crate::runtime::_str_length(&trimmed)) {
        let c = crate::runtime::_str_char_at(&trimmed, li);
        if isUnicodeWhitespace((c).0) { inSpace = true; } else { if (inSpace && !builder.is_empty()) { builder.push(" ".to_string()); } else { (); }
        inSpace = false;
        builder.push(crate::runtime::_str_substring(&trimmed, li, (li + 1i64))); }
        (li += 1i64);
    };
    foldCase((builder).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""))
}

pub fn MarkdownInlines_parse(content: String, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> Vec<InlinePiece> {
    let atoms = tokenize(content, refs, profile);
    let processed = processEmphasis(atoms);
    processed.iter().cloned().flat_map(flatten).collect::<Vec<_>>().clone().into_iter().collect::<Vec<_>>()
}

pub fn flatten(node: WNode) -> Vec<InlinePiece> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let DelimiterRun = "markdown.delimiter-run".to_string();
    match node {
        WNode::WFixed { pieces } => pieces,
        ref delim @ WNode::WDelim { ref lexeme, ref ch, ref canOpen, ref canClose } => if (*lexeme).is_empty() { Vec::new() } else { vec![InlinePiece::Tok { kind: DelimiterRun.clone(), lexeme: (*lexeme).clone(), role: Some("literal".to_string()), channel: TokenChannel::Syntax }] },
    }
}

pub fn text(lexeme: String) -> WNode {
    let Text = "markdown.text".to_string();
    WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: Text.clone(), lexeme: lexeme, role: None, channel: TokenChannel::Syntax }] }
}

pub fn tokenize(content: String, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> Vec<WNode> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let CodeContent = "markdown.code-content".to_string();
    let Html = "markdown.html".to_string();
    let Escape = "markdown.escape".to_string();
    let Entity = "markdown.entity".to_string();
    let BacktickRun = "markdown.backtick-run".to_string();
    let Autolink = "markdown.autolink".to_string();
    let ExpressionOpen = "markdown.expression-open".to_string();
    let ExpressionContent = "markdown.expression-content".to_string();
    let ExpressionClose = "markdown.expression-close".to_string();
    let SoftBreak = "markdown.soft-break".to_string();
    let HardBreak = "markdown.hard-break".to_string();
    let CodeSpan = "markdown.code-span".to_string();
    let Expression = "markdown.expression".to_string();
    fn flushText(nodes: &mut Vec<WNode>, pending: &mut Vec<String>) {
        if !(*pending).is_empty() { (*nodes).push(text(((*pending)).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")));
        (*pending) = Vec::new(); } else { (); }
    }
    let mut nodes: Vec<WNode> = Vec::new();
    let mut pending: Vec<String> = Vec::new();
    let mut i = 0i64;
    let n = crate::runtime::_str_length(&content);
    let gfm = (profile == MarkdownProfile::Gfm);
    let scala = (profile == MarkdownProfile::ScalaScript);
    while (i < n) {
        let c = crate::runtime::_str_char_at(&content, i);
        match (c).0 {
            10i64 | 13i64 => { let ending = if (((c == 13i64) && ((i + 1i64) < n)) && (crate::runtime::_str_char_at(&content, (i + 1i64)) == 10i64)) { "\r\n".to_string() } else { crate::runtime::_str_substring(&content, i, (i + 1i64)) };
            let pend = (pending).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("");
            let hard = (pend.ends_with("  ") || pend.ends_with("\\"));
            if (hard && pend.ends_with("\\")) { pending = { let __v = (pending).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() };
            flushText(&mut nodes, &mut pending);
            nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: HardBreak.clone(), lexeme: format!("{}{}", "\\".to_string(), ending), role: None.clone(), channel: TokenChannel::Syntax.clone() }] }); } else { flushText(&mut nodes, &mut pending);
            nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: if hard { HardBreak.clone() } else { SoftBreak.clone() }, lexeme: ending.clone(), role: None.clone(), channel: if hard { TokenChannel::Syntax.clone() } else { TokenChannel::Trivia.clone() } }] }); }
            (i += crate::runtime::_str_length(&ending)); },
            92i64 => { if (((i + 1i64) < n) && isAsciiPunctuation((crate::runtime::_str_char_at(&content, (i + 1i64))).0)) { flushText(&mut nodes, &mut pending);
            nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: Escape.clone(), lexeme: crate::runtime::_str_substring(&content, i, (i + 2i64)), role: None.clone(), channel: TokenChannel::Syntax.clone() }] });
            (i += 2i64); } else { pending.push("\\".to_string());
            (i += 1i64); } },
            96i64 => { let runLen = runLength(content.clone(), i, 96i64);
            let closeAt = findBacktickClose(content.clone(), (i + runLen), runLen.clone());
            match (closeAt).clone() {
                Some(start) => { flushText(&mut nodes, &mut pending);
                let openLex = crate::runtime::_str_substring(&content, i, (i + runLen));
                let inner = crate::runtime::_str_substring(&content, (i + runLen), start);
                let closeLex = crate::runtime::_str_substring(&content, start, (start + runLen));
                let mut pieces: Vec<InlinePiece> = Vec::new();
                pieces.push(InlinePiece::Open { branch: CodeSpan.clone(), kind: BacktickRun.clone(), lexeme: openLex, role: Some("delimiter.open".to_string()) });
                if !inner.is_empty() { pieces.push(InlinePiece::Tok { kind: CodeContent.clone(), lexeme: inner.clone(), role: Some("code".to_string()), channel: TokenChannel::Embedded.clone() }); } else { (); }
                pieces.push(InlinePiece::Close { branch: CodeSpan.clone(), kind: BacktickRun.clone(), lexeme: closeLex, role: Some("delimiter.close".to_string()) });
                nodes.push(WNode::WFixed { pieces: pieces.clone() });
                i = (start + runLen); },
                None => { pending.push(crate::runtime::_str_substring(&content, i, (i + runLen)));
                (i += runLen); },
            } },
            60i64 => { match (scanAngle(content.clone(), i)).clone() {
                Some((kind, endEx)) => { flushText(&mut nodes, &mut pending);
                let lex = crate::runtime::_str_substring(&content, i, endEx);
                let (mdKind, role, channel) = match (kind).clone() {
                    AngleKind::Autolink => (Autolink.clone(), Some("autolink".to_string()), TokenChannel::Syntax.clone()),
                    AngleKind::Html => (Html.clone(), Some("html".to_string()), TokenChannel::Embedded.clone()),
                };
                nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: mdKind.clone(), lexeme: lex, role: role.clone(), channel: channel.clone() }] });
                i = endEx.clone(); },
                None => { pending.push("<".to_string());
                (i += 1i64); },
            } },
            38i64 => { match scanEntity(content.clone(), i) {
                Some(endEx) => { flushText(&mut nodes, &mut pending);
                nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: Entity.clone(), lexeme: crate::runtime::_str_substring(&content, i, endEx), role: Some("entity".to_string()), channel: TokenChannel::Syntax.clone() }] });
                i = endEx.clone(); },
                None => { pending.push("&".to_string());
                (i += 1i64); },
            } },
            36i64 if ((scala && ((i + 1i64) < n)) && (crate::runtime::_str_char_at(&content, (i + 1i64)) == 123i64)) => { match scanExpression(content.clone(), i) {
                Some(endEx) => { flushText(&mut nodes, &mut pending);
                let open = crate::runtime::_str_substring(&content, i, (i + 2i64));
                let inner = crate::runtime::_str_substring(&content, (i + 2i64), (endEx - 1i64));
                let close = crate::runtime::_str_substring(&content, (endEx - 1i64), endEx);
                let mut pieces: Vec<InlinePiece> = Vec::new();
                pieces.push(InlinePiece::Open { branch: Expression.clone(), kind: ExpressionOpen.clone(), lexeme: open, role: Some("delimiter.open".to_string()) });
                if !inner.is_empty() { pieces.push(InlinePiece::Tok { kind: ExpressionContent.clone(), lexeme: inner.clone(), role: Some("expression".to_string()), channel: TokenChannel::Embedded.clone() }); } else { (); }
                pieces.push(InlinePiece::Close { branch: Expression.clone(), kind: ExpressionClose.clone(), lexeme: close, role: Some("delimiter.close".to_string()) });
                nodes.push(WNode::WFixed { pieces: pieces.clone() });
                i = endEx.clone(); },
                None => { pending.push("$".to_string());
                (i += 1i64); },
            } },
            33i64 if (((i + 1i64) < n) && (crate::runtime::_str_char_at(&content, (i + 1i64)) == 91i64)) => { match (tryLink(content.clone(), i, true, refs.clone(), profile.clone())).clone() {
                Some((node, endEx)) => { flushText(&mut nodes, &mut pending);
                nodes.push(node.clone());
                i = endEx.clone(); },
                None => { pending.push("!".to_string());
                (i += 1i64); },
            } },
            91i64 => { match (tryLink(content.clone(), i, false, refs.clone(), profile.clone())).clone() {
                Some((node, endEx)) => { flushText(&mut nodes, &mut pending);
                nodes.push(node.clone());
                i = endEx.clone(); },
                None => { pending.push("[".to_string());
                (i += 1i64); },
            } },
            42i64 | 95i64 => { flushText(&mut nodes, &mut pending);
            nodes.push(delimiterRun(content.clone(), i, (c.clone()).0));
            (i += runLength(content.clone(), i, (c.clone()).0)); },
            126i64 if gfm => { flushText(&mut nodes, &mut pending);
            nodes.push(delimiterRun(content.clone(), i, (c.clone()).0));
            (i += runLength(content.clone(), i, (c.clone()).0)); },
            _ if (gfm && isExtendedAutolinkStart(content.clone(), i, pending.clone())) => { let (dropNodes, keepText, localPart) = if (crate::runtime::_str_char_at(&content, i) == 64i64) { emailLocalBackscan(nodes.clone(), pending.clone()) } else { (0i64, "".to_string(), "".to_string()) };
            match (extendedAutolink(content.clone(), i, localPart.clone())).clone() {
                Some((backtrack, lexeme, _)) => { if (backtrack > 0i64) { if (dropNodes > 0i64) { nodes = { let __v = (nodes).clone(); let __k = (dropNodes) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() }; } else { (); }
                pending = if keepText.is_empty() { Vec::new() } else { vec![keepText.clone()] }; } else { (); }
                flushText(&mut nodes, &mut pending);
                nodes.push(WNode::WFixed { pieces: vec![InlinePiece::Tok { kind: Autolink.clone(), lexeme: lexeme.clone(), role: Some("extended".to_string()), channel: TokenChannel::Syntax.clone() }] });
                i = ((i - backtrack) + (lexeme.len() as i64)); },
                None => { pending.push(crate::runtime::_str_substring(&content, i, (i + 1i64)));
                (i += 1i64); },
            } },
            _ => { pending.push(crate::runtime::_str_substring(&content, i, (i + 1i64)));
            (i += 1i64); },
        }
    };
    flushText(&mut nodes, &mut pending);
    nodes
}

pub fn isExtendedAutolinkStart(content: String, i: i64, pending: Vec<String>) -> bool {
    match (crate::runtime::_str_char_at(&content, i)).0 {
        119i64 | 104i64 | 102i64 => validAutolinkPredecessor(content.clone(), i),
        64i64 => !pending.is_empty(),
        _ => false,
    }
}

pub fn validAutolinkPredecessor(content: String, i: i64) -> bool {
    if (i == 0i64) { true } else { { let p = crate::runtime::_str_char_at(&content, (i - 1i64));
    ((((isUnicodeWhitespace((p.clone()).0) || (p == 42i64)) || (p == 95i64)) || (p == 126i64)) || (p == 40i64)) } }
}

pub fn extendedAutolink(content: String, i: i64, localPart: String) -> Option<(i64, String, String)> {
    if (crate::runtime::_str_char_at(&content, i) == 64i64) { emailAutolink(content.clone(), i, localPart) } else { { let schemes = vec!["http://".to_string(), "https://".to_string(), "ftp://".to_string()];
    let scheme = schemes.iter().cloned().find(|__f| { let s = __f.clone(); crate::runtime::_str_region_matches(&content, true, i, &(s), 0i64, (s.len() as i64)) });
    if scheme.is_some() { domainAndPath(content.clone(), (i + (scheme.clone().unwrap().len() as i64))).map({ { let content = content.clone(); let trimAutolinkTail = trimAutolinkTail.clone(); move |end| { { let lexeme = trimAutolinkTail(crate::runtime::_str_substring(&content, i, end));
    (0i64, lexeme.clone(), lexeme.clone()) } } } }).filter(|__p0| { ((__p0.1.len() as i64) > (scheme.clone().unwrap().len() as i64)) }) } else { if crate::runtime::_str_region_matches(&content, true, i, &("www.".to_string()), 0i64, 4i64) { domainAndPath(content.clone(), i).map({ { let content = content.clone(); let trimAutolinkTail = trimAutolinkTail.clone(); move |end| { { let lexeme = trimAutolinkTail(crate::runtime::_str_substring(&content, i, end));
    (0i64, lexeme.clone(), format!("{}{}", "http://".to_string(), lexeme)) } } } }).filter(|__p0| { ((__p0.1.len() as i64) > 4i64) }) } else { None } } } }
}

pub fn emailAutolink(content: String, at: i64, local: String) -> Option<(i64, String, String)> {
    if local.is_empty() { None } else { { let mut j = (at + 1i64);
    while ((j < crate::runtime::_str_length(&content)) && isEmailDomainChar((crate::runtime::_str_char_at(&content, j)).0)) {
        (j += 1i64);
    };
    while ((j > (at + 1i64)) && (crate::runtime::_str_char_at(&content, (j - 1i64)) == 46i64)) {
        (j -= 1i64);
    };
    let domain = crate::runtime::_str_substring(&content, (at + 1i64), j);
    if !(validEmailDomain(domain.clone())) { None } else { { let lexeme = format!("{}{}{}", local, "@".to_string(), domain);
    Some((crate::runtime::_str_length(&local), lexeme.clone(), format!("{}{}", "mailto:".to_string(), lexeme))) } } } }
}

pub fn emailLocalBackscan(nodes: Vec<WNode>, pending: Vec<String>) -> (i64, String, String) {
    let Text = "markdown.text".to_string();
    fn localTextOf(node: WNode) -> Option<String> {
        if let WNode::WFixed { pieces: __vecfield0 } = (node).clone() {
            match __vecfield0.as_slice() {
                [InlinePiece::Tok { kind: __litpat1, lexeme, role: _, channel: _ }] if __litpat1 == "markdown.text" => Some(lexeme.clone()),
                _ => None,
            }
        } else {
            match node {
                ref d @ WNode::WDelim { ref lexeme, ref ch, ref canOpen, ref canClose } if ((*ch) == 95i64) => Some((*lexeme).clone()),
                _ => None,
            }
        }
    }
    let mut chunk = (pending).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("");
    let mut cut = crate::runtime::_str_length(&chunk);
    while ((cut > 0i64) && isEmailLocalChar((crate::runtime::_str_char_at(&chunk, (cut - 1i64))).0)) {
        (cut -= 1i64);
    };
    let mut local = crate::runtime::_str_substring_from(&chunk, cut);
    let mut keep = crate::runtime::_str_substring(&chunk, 0i64, cut);
    let mut drop = 0i64;
    let mut exhausted = (cut == 0i64);
    while ((exhausted && (drop < (nodes.len() as i64))) && localTextOf(nodes[((((nodes.len() as i64) - 1i64) - drop)) as usize].clone()).is_some()) {
        chunk = localTextOf(nodes[((((nodes.len() as i64) - 1i64) - drop)) as usize].clone()).clone().unwrap();
        cut = crate::runtime::_str_length(&chunk);
        while ((cut > 0i64) && isEmailLocalChar((crate::runtime::_str_char_at(&chunk, (cut - 1i64))).0)) {
            (cut -= 1i64);
        };
        local = format!("{}{}", crate::runtime::_str_substring_from(&chunk, cut), local);
        keep = crate::runtime::_str_substring(&chunk, 0i64, cut);
        (drop += 1i64);
        exhausted = (cut == 0i64);
    };
    if (!keep.is_empty() && !(validEmailPredecessor((crate::runtime::_str_char_at(&keep, (crate::runtime::_str_length(&keep) - 1i64))).0))) { (0i64, "".to_string(), "".to_string()) } else { (drop, keep.clone(), local.clone()) }
}

pub fn isEmailLocalChar(c: i64) -> bool {
    ((((((((c >= 97i64) && (c <= 122i64)) || ((c >= 65i64) && (c <= 90i64))) || ((c >= 48i64) && (c <= 57i64))) || (c == 46i64)) || (c == 45i64)) || (c == 95i64)) || (c == 43i64))
}

pub fn validEmailPredecessor(c: i64) -> bool {
    ((((isUnicodeWhitespace(c) || (c == 42i64)) || (c == 95i64)) || (c == 126i64)) || (c == 40i64))
}

pub fn isEmailDomainChar(c: i64) -> bool {
    (((((((c >= 97i64) && (c <= 122i64)) || ((c >= 65i64) && (c <= 90i64))) || ((c >= 48i64) && (c <= 57i64))) || (c == 46i64)) || (c == 45i64)) || (c == 95i64))
}

pub fn validEmailDomain(domain: String) -> bool {
    let segments = domain.splitn(-1i64 as usize, "\\.").map(|p| p.to_string()).collect::<Vec<String>>().clone().into_iter().collect::<Vec<_>>();
    let last = if domain.is_empty() { 32i64 } else { (crate::runtime::_str_char_at(&domain, (crate::runtime::_str_length(&domain) - 1i64))).0 };
    ((((((segments.len() as i64) >= 2i64) && segments.iter().cloned().all(|__p0| { !__p0.is_empty() })) && { let __v = (segments).clone(); let __k = (2i64) as usize; __v[__v.len().saturating_sub(__k)..].to_vec() }.iter().cloned().all(|__p0| { !(__p0.contains("_")) })) && (last != 45i64)) && (last != 95i64))
}

pub fn domainAndPath(content: String, from: i64) -> Option<i64> {
    let mut j = from;
    while ((j < crate::runtime::_str_length(&content)) && isEmailDomainChar((crate::runtime::_str_char_at(&content, j)).0)) {
        (j += 1i64);
    };
    let domain = crate::runtime::_str_substring(&content, from, j);
    let segments = domain.splitn(-1i64 as usize, "\\.").map(|p| p.to_string()).collect::<Vec<String>>().clone().into_iter().collect::<Vec<_>>();
    if (((segments.len() as i64) < 2i64) || (segments).iter().cloned().take(((segments.len() as i64) - 1i64) as usize).collect::<Vec<_>>().iter().cloned().any(|__p0| { __p0.is_empty() })) { None } else { { while (((j < crate::runtime::_str_length(&content)) && !(isUnicodeWhitespace((crate::runtime::_str_char_at(&content, j)).0))) && (crate::runtime::_str_char_at(&content, j) != 60i64)) {
        (j += 1i64);
    };
    Some(j.clone()) } }
}

pub fn trimAutolinkTail(raw: String) -> String {
    let mut s = raw;
    let mut changed = true;
    while (changed && !s.is_empty()) {
        changed = false;
        let last = crate::runtime::_str_char_at(&s, (crate::runtime::_str_length(&s) - 1i64));
        if ({ let __h: &str = &"?!.,:*_~".to_string(); let __n: &str = &char::from_u32(((last).0) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) } >= 0i64) { s = crate::runtime::_str_substring(&s, 0i64, (crate::runtime::_str_length(&s) - 1i64));
        changed = true; } else { if (last == 41i64) { let mut opens = 0i64;
        let mut closes = 0i64;
        let mut k = 0i64;
        while (k < crate::runtime::_str_length(&s)) {
            if (crate::runtime::_str_char_at(&s, k) == 40i64) { (opens += 1i64); } else { if (crate::runtime::_str_char_at(&s, k) == 41i64) { (closes += 1i64); } else { (); } }
            (k += 1i64);
        };
        if (closes > opens) { s = crate::runtime::_str_substring(&s, 0i64, (crate::runtime::_str_length(&s) - 1i64));
        changed = true; } else { (); } } else { if (last == 59i64) { let mut k = (crate::runtime::_str_length(&s) - 2i64);
        while ((k >= 0i64) && UniAlphabet_isAsciiAlnum((crate::runtime::_str_char_at(&s, k)).0)) {
            (k -= 1i64);
        };
        if (((k >= 0i64) && (crate::runtime::_str_char_at(&s, k) == 38i64)) && (k < (crate::runtime::_str_length(&s) - 2i64))) { s = crate::runtime::_str_substring(&s, 0i64, k);
        changed = true; } else { (); } } else { (); } } }
    };
    s
}

pub fn delimiterRun(content: String, start: i64, ch: i64) -> WNode {
    let len = runLength(content.clone(), start, ch);
    let before = if (start == 0i64) { 32i64 } else { (crate::runtime::_str_char_at(&content, (start - 1i64))).0 };
    let after = if ((start + len) >= crate::runtime::_str_length(&content)) { 32i64 } else { (crate::runtime::_str_char_at(&content, (start + len))).0 };
    let leftFlanking = (!(isUnicodeWhitespace(after.clone())) && ((!(isPunctuation(after.clone())) || isUnicodeWhitespace(before.clone())) || isPunctuation(before.clone())));
    let rightFlanking = (!(isUnicodeWhitespace(before.clone())) && ((!(isPunctuation(before.clone())) || isUnicodeWhitespace(after.clone())) || isPunctuation(after.clone())));
    let (canOpen, canClose) = if (ch == 95i64) { ((leftFlanking && (!(rightFlanking) || isPunctuation(before.clone()))), (rightFlanking && (!(leftFlanking) || isPunctuation(after.clone())))) } else { (leftFlanking, rightFlanking) };
    WNode::WDelim { lexeme: crate::runtime::_str_substring(&content, start, (start + len)), ch: ch, canOpen: canOpen, canClose: canClose }
}

pub fn runLength(content: String, start: i64, ch: i64) -> i64 {
    let mut i = start;
    while ((i < crate::runtime::_str_length(&content)) && (crate::runtime::_str_char_at(&content, i) == ch)) {
        (i += 1i64);
    };
    (i - start)
}

pub fn findBacktickClose(content: String, from: i64, runLen: i64) -> Option<i64> {
    let mut i = from;
    while (i < crate::runtime::_str_length(&content)) {
        if (crate::runtime::_str_char_at(&content, i) == 96i64) { let len = runLength(content.clone(), i.clone(), 96i64);
        if (len == runLen) { return Some(i.clone()); } else { (); }
        (i += len); } else { (i += 1i64); }
    };
    None
}

pub fn tryLink(content: String, start: i64, image: bool, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> Option<(WNode, i64)> {
    let openLen = if image { 2i64 } else { 1i64 };
    let textStart = (start + openLen);
    let closeBracket = matchBracket(content.clone(), textStart);
    match closeBracket {
        None => None.clone(),
        Some(labelEnd) => { let labelText = crate::runtime::_str_substring(&content, textStart, labelEnd);
        if (!(image) && containsLink(MarkdownInlines_parse(labelText.clone(), refs.clone(), profile.clone()))) { return None; } else { (); }
        let cursor = (labelEnd + 1i64);
        if ((cursor < crate::runtime::_str_length(&content)) && (crate::runtime::_str_char_at(&content, cursor) == 40i64)) { match (parseInlineDestination(content.clone(), cursor)).clone() {
            Some((_, _, destTitleSpans, endEx)) => Some((buildLink(content.clone(), start, textStart, labelEnd.clone(), labelText.clone(), image, destTitleSpans, endEx.clone(), refs.clone(), profile.clone()), endEx.clone())),
            None => tryReference(content.clone(), start, textStart, labelEnd.clone(), labelText.clone(), image, refs.clone(), profile.clone()),
        } } else { tryReference(content.clone(), start, textStart, labelEnd.clone(), labelText.clone(), image, refs.clone(), profile.clone()) } },
    }
}

pub fn tryReference(content: String, start: i64, labelStart: i64, labelEnd: i64, labelText: String, image: bool, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> Option<(WNode, i64)> {
    let cursor = (labelEnd + 1i64);
    if ((cursor < crate::runtime::_str_length(&content)) && (crate::runtime::_str_char_at(&content, cursor) == 91i64)) { match matchBracket(content.clone(), (cursor + 1i64)) {
        Some(refEnd) => { let refLabel = crate::runtime::_str_substring(&content, (cursor + 1i64), refEnd);
        let label = if refLabel.trim().to_string().is_empty() { labelText.clone() } else { refLabel.clone() };
        resolveRef(label, refs.clone()).map({ { let buildRefLink = buildRefLink.clone(); let content = content.clone(); let labelText = labelText.clone(); let profile = profile.clone(); let refEnd = refEnd.clone(); let refs = refs.clone(); move |_| { (buildRefLink(content.clone(), start.clone(), labelStart.clone(), labelEnd.clone(), labelText.clone(), image.clone(), (refEnd + 1i64), refs.clone(), profile.clone()), (refEnd + 1i64)) } } }) },
        None => None.clone(),
    } } else { resolveRef(labelText.clone(), refs.clone()).map({ { let buildRefLink = buildRefLink.clone(); let content = content.clone(); let labelText = labelText.clone(); let profile = profile.clone(); let refs = refs.clone(); move |_| { (buildRefLink(content.clone(), start.clone(), labelStart.clone(), labelEnd.clone(), labelText.clone(), image.clone(), (labelEnd + 1i64), refs.clone(), profile.clone()), (labelEnd + 1i64)) } } }) }
}

pub fn resolveRef(label: String, refs: std::collections::HashMap<String, LinkRef>) -> Option<LinkRef> {
    refs.get(&normalizeLabel(label)).cloned()
}

pub fn containsLink(pieces: Vec<InlinePiece>) -> bool {
    let Autolink = "markdown.autolink".to_string();
    let Link = "markdown.link".to_string();
    pieces.iter().cloned().any(|__pf| match __pf {
        InlinePiece::Open { branch, kind: _, lexeme: _, role: _ } => (branch == Link),
        InlinePiece::Tok { kind, lexeme: _, role: _, channel: _ } => (kind == Autolink),
        _ => false,
    })
}

pub fn buildLink(content: String, start: i64, labelStart: i64, labelEnd: i64, labelText: String, image: bool, spans: DestTitleSpans, endEx: i64, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> WNode {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let Indent = "markdown.indent".to_string();
    let LinkOpen = "markdown.link-open".to_string();
    let LinkClose = "markdown.link-close".to_string();
    let DestOpen = "markdown.dest-open".to_string();
    let Destination = "markdown.destination".to_string();
    let Title = "markdown.title".to_string();
    let DestClose = "markdown.dest-close".to_string();
    let Link = "markdown.link".to_string();
    let Image = "markdown.image".to_string();
    fn slice(kind: String, from: i64, to: i64, role: String, ch: TokenChannel, content: String, pieces: &mut Vec<InlinePiece>) {
        if (from < to) { (*pieces).push(InlinePiece::Tok { kind: kind, lexeme: crate::runtime::_str_substring(&content, from, to), role: Some(role), channel: ch }); } else { (); }
    }
    let branch = if image { Image.clone() } else { Link.clone() };
    let mut pieces: Vec<InlinePiece> = Vec::new();
    pieces.push(InlinePiece::Open { branch: branch.clone(), kind: LinkOpen.clone(), lexeme: crate::runtime::_str_substring(&content, start, labelStart), role: Some("delimiter.open".to_string()) });
    pieces.extend((MarkdownInlines_parse(labelText, refs, profile)).iter().cloned());
    pieces.push(InlinePiece::Tok { kind: LinkClose.clone(), lexeme: crate::runtime::_str_substring(&content, labelEnd, (labelEnd + 1i64)), role: Some("label.close".to_string()), channel: TokenChannel::Syntax.clone() });
    slice(DestOpen.clone(), (labelEnd + 1i64), spans.destStart.clone(), "dest.open".to_string(), TokenChannel::Syntax.clone(), content.clone(), &mut pieces);
    slice(Destination.clone(), spans.destStart.clone(), spans.destEnd.clone(), "destination".to_string(), TokenChannel::Syntax.clone(), content.clone(), &mut pieces);
    slice(Indent.clone(), spans.destEnd.clone(), spans.titleStart.clone(), "space".to_string(), TokenChannel::Trivia.clone(), content.clone(), &mut pieces);
    slice(Title.clone(), spans.titleStart.clone(), spans.titleEnd.clone(), "title".to_string(), TokenChannel::Syntax.clone(), content.clone(), &mut pieces);
    slice(Indent.clone(), spans.titleEnd.clone(), spans.closeStart.clone(), "space".to_string(), TokenChannel::Trivia.clone(), content.clone(), &mut pieces);
    pieces.push(InlinePiece::Close { branch: branch.clone(), kind: DestClose.clone(), lexeme: crate::runtime::_str_substring(&content, spans.closeStart, endEx), role: Some("dest.close".to_string()) });
    WNode::WFixed { pieces: pieces.clone() }
}

pub fn buildRefLink(content: String, start: i64, labelStart: i64, labelEnd: i64, labelText: String, image: bool, endEx: i64, refs: std::collections::HashMap<String, LinkRef>, profile: MarkdownProfile) -> WNode {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let LinkOpen = "markdown.link-open".to_string();
    let ReferenceLabel = "markdown.reference-label".to_string();
    let Link = "markdown.link".to_string();
    let Image = "markdown.image".to_string();
    let branch = if image { Image.clone() } else { Link.clone() };
    let mut pieces: Vec<InlinePiece> = Vec::new();
    pieces.push(InlinePiece::Open { branch: branch.clone(), kind: LinkOpen.clone(), lexeme: crate::runtime::_str_substring(&content, start, labelStart), role: Some("delimiter.open".to_string()) });
    pieces.extend((MarkdownInlines_parse(labelText, refs, profile)).iter().cloned());
    pieces.push(InlinePiece::Close { branch: branch.clone(), kind: ReferenceLabel.clone(), lexeme: crate::runtime::_str_substring(&content, labelEnd, endEx), role: Some("reference".to_string()) });
    WNode::WFixed { pieces: pieces.clone() }
}

pub fn parseInlineDestination(content: String, open: i64) -> Option<(String, Option<String>, DestTitleSpans, i64)> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let mut i = (open + 1i64);
    let n = crate::runtime::_str_length(&content);
    while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    let destStart = i;
    let mut dest = "".to_string();
    if ((i < n) && (crate::runtime::_str_char_at(&content, i) == 60i64)) { let end = crate::runtime::_str_index_of_from(&content, &(char::from_u32((62i64) as u32).unwrap_or('\u{FFFD}').to_string()), (i + 1i64));
    if ((end < 0i64) || crate::runtime::_str_substring(&content, (i + 1i64), end).contains(char::from_u32((10i64) as u32).unwrap_or('\u{FFFD}'))) { return None; } else { (); }
    dest = crate::runtime::_str_substring(&content, (i + 1i64), end);
    i = (end + 1i64); } else { let mut depth = 0i64;
    let mut sb: Vec<String> = Vec::new();
    let mut done = false;
    while ((i < n) && !(done)) {
        let c = crate::runtime::_str_char_at(&content, i);
        if ((c == 92i64) && ((i + 1i64) < n)) { sb = [&([&(sb)[..], &[crate::runtime::_str_substring(&content, i, (i + 1i64))][..]].concat())[..], &[crate::runtime::_str_substring(&content, (i + 1i64), (i + 2i64))][..]].concat();
        (i += 2i64); } else { if isUnicodeWhitespace((c.clone()).0) { done = true; } else { if (c == 40i64) { (depth += 1i64);
        sb.push(crate::runtime::_str_substring(&content, i, (i + 1i64)));
        (i += 1i64); } else { if (c == 41i64) { if (depth == 0i64) { done = true; } else { (depth -= 1i64);
        sb.push(crate::runtime::_str_substring(&content, i, (i + 1i64)));
        (i += 1i64); } } else { sb.push(crate::runtime::_str_substring(&content, i, (i + 1i64)));
        (i += 1i64); } } } }
    };
    dest = (sb).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join(""); }
    let destEnd = i;
    while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    let mut titleStart = i;
    let mut titleEnd = i;
    let mut title: Option<String> = None.clone();
    if ((i < n) && (((crate::runtime::_str_char_at(&content, i) == 34i64) || (crate::runtime::_str_char_at(&content, i) == 39i64)) || (crate::runtime::_str_char_at(&content, i) == 40i64))) { let open2 = crate::runtime::_str_char_at(&content, i);
    let close2 = if (open2 == 40i64) { 41i64 } else { (open2).0 };
    let end = crate::runtime::_str_index_of_from(&content, &(char::from_u32((close2) as u32).unwrap_or('\u{FFFD}').to_string()), (i + 1i64));
    if (end < 0i64) { return None; } else { (); }
    titleStart = i;
    titleEnd = (end + 1i64);
    title = Some(crate::runtime::_str_substring(&content, (i + 1i64), end));
    i = (end + 1i64); } else { (); }
    while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    if ((i >= n) || (crate::runtime::_str_char_at(&content, i) != 41i64)) { None } else { Some((dest.clone(), title.clone(), DestTitleSpans { destStart: destStart, destEnd: destEnd, titleStart: titleStart.clone(), titleEnd: titleEnd.clone(), closeStart: i }, (i + 1i64))) }
}

pub fn matchBracket(content: String, from: i64) -> Option<i64> {
    let mut i = from;
    let mut depth = 0i64;
    let n = crate::runtime::_str_length(&content);
    while (i < n) {
        let c = crate::runtime::_str_char_at(&content, i);
        match (c).0 {
            92i64 if ((i + 1i64) < n) => { (i += 2i64); },
            96i64 => { let len = runLength(content.clone(), i.clone(), 96i64);
            match findBacktickClose(content.clone(), (i + len), len.clone()) {
                Some(closeAt) => { i = (closeAt + len); },
                None => { (i += len); },
            } },
            91i64 => { (depth += 1i64);
            (i += 1i64); },
            93i64 => { if (depth == 0i64) { return Some(i.clone()); } else { (depth -= 1i64);
            (i += 1i64); } },
            _ => { (i += 1i64); },
        }
    };
    None
}

pub fn scanAngle(content: String, start: i64) -> Option<(AngleKind, i64)> {
    let Html = "markdown.html".to_string();
    let Autolink = "markdown.autolink".to_string();
    let n = crate::runtime::_str_length(&content);
    if ((start + 1i64) >= n) { None } else { scanAutolink(content.clone(), start).map(move |end| { (AngleKind::Autolink.clone(), end.clone()) }).or_else(|| scanRawHtml(content.clone(), start).map(move |end| { (AngleKind::Html.clone(), end.clone()) })) }
}

pub fn scanAutolink(content: String, start: i64) -> Option<i64> {
    let close = crate::runtime::_str_index_of_from(&content, &(char::from_u32((62i64) as u32).unwrap_or('\u{FFFD}').to_string()), (start + 1i64));
    if (close < 0i64) { return None; } else { (); }
    let inner = crate::runtime::_str_substring(&content, (start + 1i64), close);
    if (inner.is_empty() || inner.chars().any(|__ch| { let c = (__ch as u32) as i64; (isUnicodeWhitespace(c.clone()) || (c == 60i64)) })) { return None; } else { (); }
    let colon = { let __h: &str = &inner; let __n: &str = &char::from_u32((58i64) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) };
    let isUri = ((colon >= 2i64) && { let scheme = crate::runtime::_str_substring(&inner, 0i64, colon);
    ((MdChars_isAsciiLetter((crate::runtime::_str_char_at(&scheme, 0i64)).0) && scheme.chars().all(|__ch| { let c = (__ch as u32) as i64; (((MdChars_isAsciiAlnum(c.clone()) || (c == 43i64)) || (c == 46i64)) || (c == 45i64)) })) && (crate::runtime::_str_length(&scheme) <= 32i64)) });
    let isEmail = (!(isUri) && { let at = { let __h: &str = &inner; let __n: &str = &char::from_u32((64i64) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) };
    ((((at > 0i64) && (at < (crate::runtime::_str_length(&inner) - 1i64))) && !(crate::runtime::_str_substring_from(&inner, (at + 1i64)).contains(char::from_u32((64i64) as u32).unwrap_or('\u{FFFD}')))) && inner.chars().all(|__ch| { let c = (__ch as u32) as i64; (c != 32i64) })) });
    if (isUri || isEmail) { Some((close + 1i64)) } else { None }
}

pub fn scanRawHtml(content: String, start: i64) -> Option<i64> {
    let n = crate::runtime::_str_length(&content);
    if ((start + 1i64) >= n) { return None; } else { (); }
    let c1 = crate::runtime::_str_char_at(&content, (start + 1i64));
    if (c1 == 33i64) { if crate::runtime::_str_starts_with_at(&content, &("<!--".to_string()), start) { scanComment(content.clone(), start) } else { if crate::runtime::_str_starts_with_at(&content, &("<![CDATA[".to_string()), start) { { let end = crate::runtime::_str_index_of_from(&content, &("]]>".to_string()), (start + 9i64));
    if (end >= 0i64) { Some((end + 3i64)) } else { None } } } else { { let end = crate::runtime::_str_index_of_from(&content, &(char::from_u32((62i64) as u32).unwrap_or('\u{FFFD}').to_string()), (start + 2i64));
    if (end >= 0i64) { Some((end + 1i64)) } else { None } } } } } else { if (c1 == 63i64) { { let end = crate::runtime::_str_index_of_from(&content, &("?>".to_string()), (start + 2i64));
    if (end >= 0i64) { Some((end + 2i64)) } else { None } } } else { if (c1 == 47i64) { scanClosingTag(content.clone(), start) } else { if MdChars_isAsciiLetter((c1.clone()).0) { scanOpenTag(content.clone(), start) } else { None } } } }
}

pub fn scanComment(content: String, start: i64) -> Option<i64> {
    if crate::runtime::_str_starts_with_at(&content, &("<!-->".to_string()), start) { Some((start + 5i64)) } else { if crate::runtime::_str_starts_with_at(&content, &("<!--->".to_string()), start) { Some((start + 6i64)) } else { { let end = crate::runtime::_str_index_of_from(&content, &("-->".to_string()), (start + 4i64));
    if (end >= 0i64) { Some((end + 3i64)) } else { None } } } }
}

pub fn scanClosingTag(content: String, start: i64) -> Option<i64> {
    let n = crate::runtime::_str_length(&content);
    let mut i = (start + 2i64);
    if ((i >= n) || !(MdChars_isAsciiLetter((crate::runtime::_str_char_at(&content, i)).0))) { return None; } else { (); }
    while ((i < n) && (MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&content, i)).0) || (crate::runtime::_str_char_at(&content, i) == 45i64))) {
        (i += 1i64);
    };
    while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    if ((i < n) && (crate::runtime::_str_char_at(&content, i) == 62i64)) { Some((i + 1i64)) } else { None }
}

pub fn scanOpenTag(content: String, start: i64) -> Option<i64> {
    let n = crate::runtime::_str_length(&content);
    let mut i = (start + 1i64);
    if ((i >= n) || !(MdChars_isAsciiLetter((crate::runtime::_str_char_at(&content, i)).0))) { return None; } else { (); }
    while ((i < n) && (MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&content, i)).0) || (crate::runtime::_str_char_at(&content, i) == 45i64))) {
        (i += 1i64);
    };
    let mut ok = true;
    let mut done = false;
    while (!(done) && ok) {
        let wsStart = i;
        while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
            (i += 1i64);
        };
        if ((i < n) && (crate::runtime::_str_char_at(&content, i) == 62i64)) { done = true; } else { if ((((i + 1i64) < n) && (crate::runtime::_str_char_at(&content, i) == 47i64)) && (crate::runtime::_str_char_at(&content, (i + 1i64)) == 62i64)) { (i += 1i64);
        done = true; } else { if (i == wsStart) { ok = false; } else { let nameStart = i;
        if ((i < n) && ((MdChars_isAsciiLetter((crate::runtime::_str_char_at(&content, i)).0) || (crate::runtime::_str_char_at(&content, i) == 95i64)) || (crate::runtime::_str_char_at(&content, i) == 58i64))) { (i += 1i64);
        while ((i < n) && (MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&content, i)).0) || ({ let __h: &str = &"_.:-".to_string(); let __n: &str = &char::from_u32(((crate::runtime::_str_char_at(&content, i)).0) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) } >= 0i64))) {
            (i += 1i64);
        }; } else { (); }
        if (i == nameStart) { ok = false; } else { let afterName = i;
        while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
            (i += 1i64);
        };
        if ((i < n) && (crate::runtime::_str_char_at(&content, i) == 61i64)) { (i += 1i64);
        while ((i < n) && isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0)) {
            (i += 1i64);
        };
        if (i >= n) { ok = false; } else { let q = crate::runtime::_str_char_at(&content, i);
        if ((q == 39i64) || (q == 34i64)) { let close = crate::runtime::_str_index_of_from(&content, &(char::from_u32((crate::runtime::_to_int(&q)) as u32).unwrap_or('\u{FFFD}').to_string()), (i + 1i64));
        if (close < 0i64) { ok = false; } else { i = (close + 1i64); } } else { let valStart = i;
        while (((i < n) && !(isUnicodeWhitespace((crate::runtime::_str_char_at(&content, i)).0))) && ({ let __h: &str = &"\"'=<>`".to_string(); let __n: &str = &char::from_u32(((crate::runtime::_str_char_at(&content, i)).0) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) } < 0i64)) {
            (i += 1i64);
        };
        if (i == valStart) { ok = false; } else { (); } } } } else { i = afterName; } } } } }
        if (i >= n) { ok = false; } else { (); }
    };
    if (((ok && done) && (i < n)) && (crate::runtime::_str_char_at(&content, i) == 62i64)) { Some((i + 1i64)) } else { None }
}

pub fn scanEntity(content: String, start: i64) -> Option<i64> {
    let n = crate::runtime::_str_length(&content);
    if ((start + 1i64) >= n) { return None; } else { (); }
    if (crate::runtime::_str_char_at(&content, (start + 1i64)) == 35i64) { { let mut i = (start + 2i64);
    if ((i < n) && ((crate::runtime::_str_char_at(&content, i) == 120i64) || (crate::runtime::_str_char_at(&content, i) == 88i64))) { { (i += 1i64);
    let hexStart = i;
    while ((i < n) && isHex((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    if ((((i > hexStart) && ((i - hexStart) <= 6i64)) && (i < n)) && (crate::runtime::_str_char_at(&content, i) == 59i64)) { Some((i + 1i64)) } else { None } } } else { { let decStart = i;
    while ((i < n) && isAsciiDigit((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    if ((((i > decStart) && ((i - decStart) <= 7i64)) && (i < n)) && (crate::runtime::_str_char_at(&content, i) == 59i64)) { Some((i + 1i64)) } else { None } } } } } else { { let mut i = (start + 1i64);
    while ((i < n) && MdChars_isAsciiAlnum((crate::runtime::_str_char_at(&content, i)).0)) {
        (i += 1i64);
    };
    if (((i > (start + 1i64)) && (i < n)) && (crate::runtime::_str_char_at(&content, i) == 59i64)) { Some((i + 1i64)) } else { None } } }
}

pub fn scanExpression(content: String, start: i64) -> Option<i64> {
    let mut i = (start + 2i64);
    let mut depth = 1i64;
    let n = crate::runtime::_str_length(&content);
    while ((i < n) && (depth > 0i64)) {
        match (crate::runtime::_str_char_at(&content, i)).0 {
            123i64 => { (depth += 1i64);
            (i += 1i64); },
            125i64 => { (depth -= 1i64);
            (i += 1i64); },
            _ => { (i += 1i64); },
        }
    };
    if (depth == 0i64) { Some(i) } else { None }
}

pub fn isHex(c: i64) -> bool {
    ((isAsciiDigit(c) || ((c >= 97i64) && (c <= 102i64))) || ((c >= 65i64) && (c <= 70i64)))
}

pub fn processEmphasis(input: Vec<WNode>) -> Vec<WNode> {
    let DelimiterRun = "markdown.delimiter-run".to_string();
    let StrikethroughRun = "markdown.strikethrough-run".to_string();
    let Emphasis = "markdown.emphasis".to_string();
    let Strong = "markdown.strong".to_string();
    let Strikethrough = "markdown.strikethrough".to_string();
    let mut nodes = input;
    let mut closerIdx = 0i64;
    while (closerIdx < (nodes.len() as i64)) {
        match nodes[(closerIdx) as usize].clone() {
            ref closer @ WNode::WDelim { ref lexeme, ref ch, ref canOpen, ref canClose } if ((*canClose) && !(*lexeme).is_empty()) => { let mut openerIdx = (closerIdx - 1i64);
            let mut found = -1i64;
            while ((openerIdx >= 0i64) && (found < 0i64)) {
                match nodes[(openerIdx) as usize].clone() {
                    ref opener @ WNode::WDelim { ref lexeme, ref ch, ref canOpen, ref canClose } if ((((*ch) == (*ch)) && (*canOpen)) && !(*lexeme).is_empty()) => { if compatible((*opener).clone(), (*closer).clone()) { found = openerIdx; } else { (openerIdx -= 1i64); } },
                    _ => { (openerIdx -= 1i64); },
                }
            };
            if (found >= 0i64) { let WNode::WDelim { lexeme: opener_lexeme, ch: opener_ch, canOpen: opener_canOpen, canClose: opener_canClose } = (nodes[(found) as usize].clone()) else { unreachable!() };
            let r#use = if ((crate::runtime::_str_length(&opener_lexeme) >= 2i64) && (crate::runtime::_str_length(&(*lexeme)) >= 2i64)) { 2i64 } else { 1i64 };
            let (branch, kind) = if ((*ch) == 126i64) { (Strikethrough.clone(), StrikethroughRun.clone()) } else { if (r#use == 2i64) { (Strong.clone(), DelimiterRun.clone()) } else { (Emphasis.clone(), DelimiterRun.clone()) } };
            let openLex = crate::runtime::_str_substring_from(&opener_lexeme, (crate::runtime::_str_length(&opener_lexeme) - r#use));
            let closeLex = crate::runtime::_str_substring(&(*lexeme), 0i64, r#use);
            let newOpener = WNode::WDelim { lexeme: crate::runtime::_str_substring(&opener_lexeme, 0i64, (crate::runtime::_str_length(&opener_lexeme) - r#use)), ch: opener_ch.clone(), canOpen: opener_canOpen.clone(), canClose: opener_canClose.clone() };
            let newCloser = WNode::WDelim { lexeme: crate::runtime::_str_substring_from(&(*lexeme), r#use), ch: (*ch).clone(), canOpen: (*canOpen).clone(), canClose: (*canClose).clone() };
            let inner = (nodes)[((found + 1i64) as usize)..(closerIdx as usize)].to_vec().iter().cloned().flat_map(flatten).collect::<Vec<_>>().clone().into_iter().collect::<Vec<_>>();
            let wrap = WNode::WFixed { pieces: [&([&[InlinePiece::Open { branch: branch.clone(), kind: kind.clone(), lexeme: openLex, role: Some("delimiter.open".to_string()) }][..], &(inner)[..]].concat())[..], &[InlinePiece::Close { branch: branch.clone(), kind: kind.clone(), lexeme: closeLex, role: Some("delimiter.close".to_string()) }][..]].concat() };
            nodes = [&([&((nodes).iter().cloned().take(found as usize).collect::<Vec<_>>())[..], &(vec![newOpener, wrap, newCloser])[..]].concat())[..], &((nodes).iter().cloned().skip((closerIdx + 1i64) as usize).collect::<Vec<_>>())[..]].concat();
            closerIdx = (found + 1i64); } else { (closerIdx += 1i64); } },
            _ => { (closerIdx += 1i64); },
        }
    };
    nodes
}

pub fn compatible(opener: WNode, closer: WNode) -> bool {
    let oc = ((match &opener { WNode::WDelim { canOpen, .. } => canOpen.clone(), _ => unreachable!() }) && (match &opener { WNode::WDelim { canClose, .. } => canClose.clone(), _ => unreachable!() }));
    let cc = ((match &closer { WNode::WDelim { canOpen, .. } => canOpen.clone(), _ => unreachable!() }) && (match &closer { WNode::WDelim { canClose, .. } => canClose.clone(), _ => unreachable!() }));
    if (!(oc) && !(cc)) { true } else { { let sum = (crate::runtime::_str_length(&(match &opener { WNode::WDelim { lexeme, .. } => lexeme.clone(), _ => unreachable!() })) + crate::runtime::_str_length(&(match &closer { WNode::WDelim { lexeme, .. } => lexeme.clone(), _ => unreachable!() })));
    if ((sum % 3i64) != 0i64) { true } else { (((crate::runtime::_str_length(&(match &opener { WNode::WDelim { lexeme, .. } => lexeme.clone(), _ => unreachable!() })) % 3i64) == 0i64) && ((crate::runtime::_str_length(&(match &closer { WNode::WDelim { lexeme, .. } => lexeme.clone(), _ => unreachable!() })) % 3i64) == 0i64)) } } }
}

pub fn split(text: String) -> Vec<MdLine> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let chars = text.encode_utf16().map(|__u| __u as i64).collect::<Vec<i64>>();
    let mut lines: Vec<MdLine> = Vec::new();
    let mut lineStart = 0i64;
    let mut index = 0i64;
    while (index < (chars.len() as i64)) {
        let char = chars[(index) as usize].clone();
        match (char).clone() {
            10i64 => { lines.push(MdLine { content: ((chars)[(lineStart as usize)..(index as usize)].to_vec()).iter().map(|__e| crate::runtime::SscChar(*__e).to_string()).collect::<Vec<String>>().join(""), ending: "\n".to_string() });
            (index += 1i64);
            lineStart = index; },
            13i64 => { if (((index + 1i64) < (chars.len() as i64)) && (chars[((index + 1i64)) as usize].clone() == 10i64)) { lines.push(MdLine { content: ((chars)[(lineStart as usize)..(index as usize)].to_vec()).iter().map(|__e| crate::runtime::SscChar(*__e).to_string()).collect::<Vec<String>>().join(""), ending: "\r\n".to_string() });
            (index += 2i64); } else { lines.push(MdLine { content: ((chars)[(lineStart as usize)..(index as usize)].to_vec()).iter().map(|__e| crate::runtime::SscChar(*__e).to_string()).collect::<Vec<String>>().join(""), ending: "\r".to_string() });
            (index += 1i64); }
            lineStart = index; },
            _ => { (index += 1i64); },
        }
    };
    if (lineStart < (chars.len() as i64)) { lines.push(MdLine { content: ((chars).iter().cloned().skip(lineStart as usize).collect::<Vec<_>>()).iter().map(|__e| crate::runtime::SscChar(*__e).to_string()).collect::<Vec<String>>().join(""), ending: "".to_string() }); } else { (); }
    lines
}

pub fn asciiLower(s: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let mut i = 0i64;
    let mut needs = false;
    while ((i < crate::runtime::_str_length(&s)) && !(needs)) {
        let c = crate::runtime::_str_char_at(&s, i);
        if ((c >= 65i64) && (c <= 90i64)) { needs = true; } else { (); }
        (i += 1i64);
    };
    if !(needs) { s.clone() } else { { let mut out: Vec<String> = Vec::new();
    let mut k = 0i64;
    while (k < crate::runtime::_str_length(&s)) {
        let c = crate::runtime::_str_char_at(&s, k);
        out.push(format!("{}", if ((c >= 65i64) && (c <= 90i64)) { (((c + 32i64)) as i64) } else { (c).0 }));
        (k += 1i64);
    };
    (out).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("") } }
}

pub fn foldCase(s: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let mut out: Vec<String> = Vec::new();
    let mut i = 0i64;
    while (i < crate::runtime::_str_length(&s)) {
        let c = crate::runtime::_str_char_at(&s, i);
        out.push(format!("{}", if ((c >= 65i64) && (c <= 90i64)) { (((c + 32i64)) as i64) } else { if (c < 128i64) { (c).0 } else { crate::runtime::_char_to_lowercase((c).0) } }));
        (i += 1i64);
    };
    (out).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")
}

pub fn isAsciiWhitespace(c: i64) -> bool {
    let VerticalTab = 11i64;
    let FormFeed = 12i64;
    ((((((c == 32i64) || (c == 9i64)) || (c == 10i64)) || (c == VerticalTab)) || (c == FormFeed)) || (c == 13i64))
}

pub fn isUnicodeWhitespace(c: i64) -> bool {
    (isAsciiWhitespace(c) || { let v = crate::runtime::_to_int(&c);
    ((((((((v == 160i64) || (v == 5760i64)) || ((v >= 8192i64) && (v <= 8202i64))) || (v == 8232i64)) || (v == 8233i64)) || (v == 8239i64)) || (v == 8287i64)) || (v == 12288i64)) })
}

pub fn isAsciiPunctuation(c: i64) -> bool {
    (((((c >= 33i64) && (c <= 47i64)) || ((c >= 58i64) && (c <= 64i64))) || ((c >= 91i64) && (c <= 96i64))) || ((c >= 123i64) && (c <= 126i64)))
}

pub fn isPunctuation(c: i64) -> bool {
    (isAsciiPunctuation(c) || ((crate::runtime::_to_int(&c) >= 128i64) && bmpPunct(crate::runtime::_to_int(&c))))
}

pub fn bmpPunct(cp: i64) -> bool {
    let punctRanges = vec![161i64, 169i64, 171i64, 172i64, 174i64, 177i64, 180i64, 180i64, 182i64, 184i64, 187i64, 187i64, 191i64, 191i64, 215i64, 215i64, 247i64, 247i64, 706i64, 709i64, 722i64, 735i64, 741i64, 747i64, 749i64, 749i64, 751i64, 767i64, 885i64, 885i64, 894i64, 894i64, 900i64, 901i64, 903i64, 903i64, 1014i64, 1014i64, 1154i64, 1154i64, 1370i64, 1375i64, 1417i64, 1418i64, 1421i64, 1423i64, 1470i64, 1470i64, 1472i64, 1472i64, 1475i64, 1475i64, 1478i64, 1478i64, 1523i64, 1524i64, 1542i64, 1551i64, 1563i64, 1563i64, 1565i64, 1567i64, 1642i64, 1645i64, 1748i64, 1748i64, 1758i64, 1758i64, 1769i64, 1769i64, 1789i64, 1790i64, 1792i64, 1805i64, 2038i64, 2041i64, 2046i64, 2047i64, 2096i64, 2110i64, 2142i64, 2142i64, 2184i64, 2184i64, 2404i64, 2405i64, 2416i64, 2416i64, 2546i64, 2547i64, 2554i64, 2555i64, 2557i64, 2557i64, 2678i64, 2678i64, 2800i64, 2801i64, 2928i64, 2928i64, 3059i64, 3066i64, 3191i64, 3191i64, 3199i64, 3199i64, 3204i64, 3204i64, 3407i64, 3407i64, 3449i64, 3449i64, 3572i64, 3572i64, 3647i64, 3647i64, 3663i64, 3663i64, 3674i64, 3675i64, 3841i64, 3863i64, 3866i64, 3871i64, 3892i64, 3892i64, 3894i64, 3894i64, 3896i64, 3896i64, 3898i64, 3901i64, 3973i64, 3973i64, 4030i64, 4037i64, 4039i64, 4044i64, 4046i64, 4058i64, 4170i64, 4175i64, 4254i64, 4255i64, 4347i64, 4347i64, 4960i64, 4968i64, 5008i64, 5017i64, 5120i64, 5120i64, 5741i64, 5742i64, 5787i64, 5788i64, 5867i64, 5869i64, 5941i64, 5942i64, 6100i64, 6102i64, 6104i64, 6107i64, 6144i64, 6154i64, 6464i64, 6464i64, 6468i64, 6469i64, 6622i64, 6655i64, 6686i64, 6687i64, 6816i64, 6822i64, 6824i64, 6829i64, 7002i64, 7018i64, 7028i64, 7038i64, 7164i64, 7167i64, 7227i64, 7231i64, 7294i64, 7295i64, 7360i64, 7367i64, 7379i64, 7379i64, 8125i64, 8125i64, 8127i64, 8129i64, 8141i64, 8143i64, 8157i64, 8159i64, 8173i64, 8175i64, 8189i64, 8190i64, 8208i64, 8231i64, 8240i64, 8286i64, 8314i64, 8318i64, 8330i64, 8334i64, 8352i64, 8384i64, 8448i64, 8449i64, 8451i64, 8454i64, 8456i64, 8457i64, 8468i64, 8468i64, 8470i64, 8472i64, 8478i64, 8483i64, 8485i64, 8485i64, 8487i64, 8487i64, 8489i64, 8489i64, 8494i64, 8494i64, 8506i64, 8507i64, 8512i64, 8516i64, 8522i64, 8525i64, 8527i64, 8527i64, 8586i64, 8587i64, 8592i64, 9254i64, 9280i64, 9290i64, 9372i64, 9449i64, 9472i64, 10101i64, 10132i64, 11123i64, 11126i64, 11157i64, 11159i64, 11263i64, 11493i64, 11498i64, 11513i64, 11516i64, 11518i64, 11519i64, 11632i64, 11632i64, 11776i64, 11822i64, 11824i64, 11869i64, 11904i64, 11929i64, 11931i64, 12019i64, 12032i64, 12245i64, 12272i64, 12283i64, 12289i64, 12292i64, 12296i64, 12320i64, 12336i64, 12336i64, 12342i64, 12343i64, 12349i64, 12351i64, 12443i64, 12444i64, 12448i64, 12448i64, 12539i64, 12539i64, 12688i64, 12689i64, 12694i64, 12703i64, 12736i64, 12771i64, 12800i64, 12830i64, 12842i64, 12871i64, 12880i64, 12880i64, 12896i64, 12927i64, 12938i64, 12976i64, 12992i64, 13311i64, 19904i64, 19967i64, 42128i64, 42182i64, 42238i64, 42239i64, 42509i64, 42511i64, 42611i64, 42611i64, 42622i64, 42622i64, 42738i64, 42743i64, 42752i64, 42774i64, 42784i64, 42785i64, 42889i64, 42890i64, 43048i64, 43051i64, 43062i64, 43065i64, 43124i64, 43127i64, 43214i64, 43215i64, 43256i64, 43258i64, 43260i64, 43260i64, 43310i64, 43311i64, 43359i64, 43359i64, 43457i64, 43469i64, 43486i64, 43487i64, 43612i64, 43615i64, 43639i64, 43641i64, 43742i64, 43743i64, 43760i64, 43761i64, 43867i64, 43867i64, 43882i64, 43883i64, 44011i64, 44011i64, 64297i64, 64297i64, 64434i64, 64450i64, 64830i64, 64847i64, 64975i64, 64975i64, 65020i64, 65023i64, 65040i64, 65049i64, 65072i64, 65106i64, 65108i64, 65126i64, 65128i64, 65131i64, 65281i64, 65295i64, 65306i64, 65312i64, 65339i64, 65344i64, 65371i64, 65381i64, 65504i64, 65510i64, 65512i64, 65518i64, 65532i64, 65533i64];
    let mut lo = 0i64;
    let mut hi = (((punctRanges.len() as i64) / 2i64) - 1i64);
    let mut found = false;
    while ((lo <= hi) && !(found)) {
        let mid = ((lo + hi) / 2i64);
        let start = punctRanges[((mid * 2i64)) as usize].clone();
        let end = punctRanges[(((mid * 2i64) + 1i64)) as usize].clone();
        if (cp < start) { hi = (mid - 1i64); } else { if (cp > end) { lo = (mid + 1i64); } else { found = true; } }
    };
    found
}

pub fn isAsciiDigit(c: i64) -> bool {
    ((c >= 48i64) && (c <= 57i64))
}

pub fn MdChars_isAsciiLetter(c: i64) -> bool {
    (((c >= 97i64) && (c <= 122i64)) || ((c >= 65i64) && (c <= 90i64)))
}

pub fn MdChars_isAsciiAlnum(c: i64) -> bool {
    (isAsciiDigit(c) || MdChars_isAsciiLetter(c))
}

pub fn indentWidth(content: String) -> i64 {
    let mut col = 0i64;
    let mut i = 0i64;
    let mut done = false;
    while ((i < crate::runtime::_str_length(&content)) && !(done)) {
        match (crate::runtime::_str_char_at(&content, i)).0 {
            32i64 => { (col += 1i64);
            (i += 1i64); },
            9i64 => { (col += (4i64 - (col % 4i64)));
            (i += 1i64); },
            _ => { done = true; },
        }
    };
    col
}

pub fn indentCut(content: String, columns: i64) -> i64 {
    let mut col = 0i64;
    let mut i = 0i64;
    let mut stopped = false;
    while (((i < crate::runtime::_str_length(&content)) && (col < columns)) && !(stopped)) {
        match (crate::runtime::_str_char_at(&content, i)).0 {
            32i64 => { (col += 1i64);
            (i += 1i64); },
            9i64 => { (col += (4i64 - (col % 4i64)));
            (i += 1i64); },
            _ => { stopped = true; },
        }
    };
    if (col == columns) { i } else { -1i64 }
}

pub fn indentPrefixLength(content: String) -> i64 {
    let mut i = 0i64;
    while ((i < crate::runtime::_str_length(&content)) && ((crate::runtime::_str_char_at(&content, i) == 32i64) || (crate::runtime::_str_char_at(&content, i) == 9i64))) {
        (i += 1i64);
    };
    i
}

pub fn MarkdownProjection_project(result: ParseResult, profile: MarkdownProfile) -> MarkdownProjectionResult {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let _ = profile;
    if result.diagnostics.iter().cloned().any(|d| { (d.severity == Severity::Fatal) }) { MarkdownProjectionResult { document: None.clone(), diagnostics: result.diagnostics.clone() } } else { { let refs = collectDefinitions(result.roots.clone());
    let mut blocks: Vec<MarkdownBlock> = Vec::new();
    let mut references: Vec<MarkdownBlock> = Vec::new();
    for root in result.roots.iter().cloned() {
        match projectBlock(root, refs.clone()) {
            Some(ref defn @ MarkdownBlock::LinkDefinition { ref label, ref destination, ref title }) => references.push(defn.clone()),
            Some(block) => blocks.push(block.clone()),
            None => (),
        };
    };
    MarkdownProjectionResult { document: Some(MarkdownDocument { blocks: blocks.clone(), references: references.clone() }), diagnostics: result.diagnostics.clone() } } }
}

pub fn collectDefinitions(roots: Vec<UniNode>) -> std::collections::HashMap<String, MarkdownBlock> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let Definition = "markdown.definition".to_string();
    fn walk(node: UniNode, map: &mut std::collections::HashMap<String, MarkdownBlock>) {
        match (node).clone() {
            ref b @ UniNode::Branch { kind: ref __litpat2, edges: _, span: _, origin: _ } if __litpat2 == "markdown.definition" => { for defn in definitionOf((*b).clone()).iter().cloned() {
                let key = normalizeLabel((match &defn { MarkdownBlock::LinkDefinition { label, .. } => label.clone(), _ => unreachable!() }));
                if !((*map).contains_key(&(key))) { (*map) = {
                    let mut m2 = (*map).clone();
                    m2.insert(key.clone(), defn.clone());
                    m2
                  }; } else { (); };
            }; },
            UniNode::Branch { kind: _, edges, span: _, origin: _ } => { for e in edges.iter().cloned() {
                walk(e.child.clone(), map);
            }; },
            _ => { (); },
        }
    }
    let mut map: std::collections::HashMap<String, MarkdownBlock> = std::collections::HashMap::new();
    roots.iter().cloned().for_each(|__x| walk(__x, &mut map));;
    map
}

pub fn definitionOf(branch: UniNode) -> Option<MarkdownBlock> {
    let Destination = "markdown.destination".to_string();
    let Title = "markdown.title".to_string();
    let ReferenceLabel = "markdown.reference-label".to_string();
    let UniNode::Branch { kind, edges, span, origin } = branch.clone() else { unreachable!() };
    let mut label = "".to_string();
    let mut dest = "".to_string();
    let mut title: Option<String> = None.clone();
    for edge in edges.iter().cloned() {
        match edge.child {
            UniNode::Token { value: t } if (t.kind == ReferenceLabel) => label = ((t.lexeme).strip_prefix(("[".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (t.lexeme).clone())).strip_suffix(("]".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| ((t.lexeme).strip_prefix(("[".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (t.lexeme).clone())).clone()),
            UniNode::Token { value: t } if (t.kind == Destination) => dest = unwrapDestination(t.lexeme.clone()),
            UniNode::Token { value: t } if (t.kind == Title) => title = Some(stripTitle(t.lexeme.clone())),
            _ => (),
        };
    };
    if label.is_empty() { None } else { Some(MarkdownBlock::LinkDefinition { label: label.clone(), destination: dest.clone(), title: title.clone() }) }
}

pub fn projectBlock(node: UniNode, refs: std::collections::HashMap<String, MarkdownBlock>) -> Option<MarkdownBlock> {
    let SetextUnderline = "markdown.setext-underline".to_string();
    let Html = "markdown.html".to_string();
    let Heading = "markdown.heading".to_string();
    let Paragraph = "markdown.paragraph".to_string();
    let Blockquote = "markdown.blockquote".to_string();
    let List = "markdown.list".to_string();
    let ListItem = "markdown.list-item".to_string();
    let CodeBlock = "markdown.code-block".to_string();
    let HtmlBlock = "markdown.html-block".to_string();
    let Definition = "markdown.definition".to_string();
    let Table = "markdown.table".to_string();
    let ThematicBreak = "markdown.thematic-break".to_string();
    let FrontMatter = "markdown.front-matter".to_string();
    match (node).clone() {
        UniNode::Token { value: _ } => None.clone(),
        UniNode::Branch { kind, edges, span: _, origin: _ } => match (kind).as_str() {
            "markdown.paragraph" => Some(MarkdownBlock::Paragraph { inlines: trimBlockInlines(projectInlines(edges.clone(), refs.clone())) }),
            "markdown.heading" => { let level = headingLevel(edges.clone());
            let setext = edges.iter().cloned().any(|__pf| match __pf {
                UniEdge { role: _, child: UniNode::Token { value: t } } => (t.kind == SetextUnderline),
                _ => false,
            });
            Some(MarkdownBlock::Heading { level: level, inlines: trimBlockInlines(projectInlines(edges.clone(), refs.clone())), setext: setext }) },
            "markdown.thematic-break" => Some(MarkdownBlock::ThematicBreak.clone()),
            "markdown.blockquote" => Some(MarkdownBlock::BlockQuote { blocks: edges.iter().cloned().flat_map({ let projectBlock = projectBlock.clone(); let refs = refs.clone(); move |e| { projectBlock(e.child.clone(), refs.clone()) } }).collect::<Vec<_>>() }),
            "markdown.list" => { let items = edges.iter().cloned().filter_map(|__c| match __c {
                UniEdge { role: _, child: UniNode::Branch { kind: __litpat3, edges: itemEdges, span: _, origin: _ } } if __litpat3 == "markdown.list-item" => Some(ListItem { blocks: itemEdges.iter().cloned().flat_map({ let projectBlock = projectBlock.clone(); let refs = refs.clone(); move |e| { projectBlock(e.child.clone(), refs.clone()) } }).collect::<Vec<_>>(), task: taskState(itemEdges.clone()) }),
                _ => None,
            }).collect::<Vec<_>>();
            Some(MarkdownBlock::ListBlock { ordered: listOrdered(edges.clone()), start: listStart(edges.clone()), tight: !(listLoose(edges.clone())), items: items }) },
            "markdown.code-block" => Some(MarkdownBlock::CodeBlock { info: codeInfo(edges.clone()), literal: codeLiteral(edges.clone()), fenced: hasFence(edges.clone()) }),
            "markdown.front-matter" => Some(MarkdownBlock::CodeBlock { info: Some("yaml".to_string()), literal: codeLiteral(edges.clone()), fenced: true }),
            "markdown.html-block" => Some(MarkdownBlock::HtmlBlock { raw: concatTokens(edges.clone(), Html.clone()) }),
            "markdown.table" => Some(projectTable(edges.clone())),
            "markdown.definition" => definitionOf(node.clone()),
            _ => None.clone(),
        },
    }
}

pub fn headingLevel(edges: Vec<UniEdge>) -> i64 {
    let AtxMarker = "markdown.atx-marker".to_string();
    let SetextUnderline = "markdown.setext-underline".to_string();
    edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == AtxMarker) => Some((t.lexeme.len() as i64)),
        _ => None,
    }).or_else(|| edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == SetextUnderline) => Some(if t.lexeme.trim().to_string().starts_with("=") { 1i64 } else { 2i64 }),
        _ => None,
    })).unwrap_or(1i64)
}

pub fn listLoose(edges: Vec<UniEdge>) -> bool {
    let Blank = "markdown.blank".to_string();
    let ListItem = "markdown.list-item".to_string();
    let items = edges.iter().cloned().filter_map(|__c| match __c {
        UniEdge { role: _, child: ref b @ UniNode::Branch { kind: ref __litpat4, edges: _, span: _, origin: _ } } if __litpat4 == "markdown.list-item" => Some(b.clone()),
        _ => None,
    }).collect::<Vec<_>>();
    items.iter().cloned().enumerate().map(|(__i, __e)| (__e, __i as i64)).collect::<Vec<_>>().iter().cloned().any(|(item, idx)| { let itemEdges = (match &item { UniNode::Branch { edges, .. } => edges.clone(), _ => unreachable!() });
    let blankIdx = (itemEdges.iter().cloned().position(|__pf| match __pf {
        UniEdge { role: _, child: UniNode::Token { value: t } } => (t.kind == Blank),
        _ => false,
    }).map(|__i| __i as i64).unwrap_or(-1));
    if (blankIdx < 0i64) { false } else { if (idx < ((items.len() as i64) - 1i64)) { true } else { (itemEdges).iter().cloned().skip((blankIdx + 1i64) as usize).collect::<Vec<_>>().iter().cloned().any(|__pf| match __pf {
        UniEdge { role: _, child: _ } => true,
        _ => false,
    }) } } })
}

pub fn listOrdered(edges: Vec<UniEdge>) -> bool {
    (firstMarker(edges)).is_some_and(move |m| { (!m.is_empty() && isAsciiDigit((crate::runtime::_str_char_at(&m, 0i64)).0)) })
}

pub fn listStart(edges: Vec<UniEdge>) -> Option<i64> {
    firstMarker(edges).and_then({ move |m| { { let digits = m.chars().take_while(|__ch| isAsciiDigit(((*__ch) as u32) as i64)).collect::<String>();
    if digits.is_empty() { None } else { digits.parse::<i64>().ok() } } } })
}

pub fn firstMarker(edges: Vec<UniEdge>) -> Option<String> {
    let ListMarker = "markdown.list-marker".to_string();
    let ListItem = "markdown.list-item".to_string();
    (edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Branch { kind: __litpat5, edges: itemEdges, span: _, origin: _ } } if __litpat5 == "markdown.list-item" => Some(itemEdges.iter().cloned().find_map(|__c| match __c {
            UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == ListMarker) => Some(t.lexeme.clone()),
            _ => None,
        })),
        _ => None,
    })).flatten()
}

pub fn taskState(edges: Vec<UniEdge>) -> Option<bool> {
    let TaskMarker = "markdown.task-marker".to_string();
    edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == TaskMarker) => Some((t.lexeme.contains(char::from_u32((120i64) as u32).unwrap_or('\u{FFFD}')) || t.lexeme.contains(char::from_u32((88i64) as u32).unwrap_or('\u{FFFD}')))),
        _ => None,
    })
}

pub fn hasFence(edges: Vec<UniEdge>) -> bool {
    let FenceOpen = "markdown.fence-open".to_string();
    let FenceClose = "markdown.fence-close".to_string();
    edges.iter().cloned().any(|__pf| match __pf {
        UniEdge { role: _, child: UniNode::Token { value: t } } => ((t.kind == FenceOpen) || (t.kind == FenceClose)),
        _ => false,
    })
}

pub fn codeInfo(edges: Vec<UniEdge>) -> Option<String> {
    let Info = "markdown.info".to_string();
    edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == Info) => Some(decodeText(unescape(t.lexeme.trim().to_string()))),
        _ => None,
    }).filter(|__p0| { !__p0.is_empty() })
}

pub fn codeLiteral(edges: Vec<UniEdge>) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let LineBreak = "markdown.line-break".to_string();
    let CodeContent = "markdown.code-content".to_string();
    let mut buf: Vec<String> = Vec::new();
    edges.iter().cloned().for_each(|__pf| match __pf {
        UniEdge { role: _, child: UniNode::Token { value: t } } if ((t.kind == CodeContent) || ((t.kind == LineBreak) && (t.channel == TokenChannel::Embedded))) => buf.push(t.lexeme.clone()),
        _ => (),
    });;
    (buf).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")
}

pub fn concatTokens(edges: Vec<UniEdge>, kind: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let LineBreak = "markdown.line-break".to_string();
    fn walk(node: UniNode, buf: &mut Vec<String>, kind: String) {
        match node {
            UniNode::Token { value: t } if ((t.kind == kind) || ((t.kind == "markdown.line-break".to_string()) && (t.channel == TokenChannel::Embedded))) => { (*buf).push(t.lexeme.clone()); },
            UniNode::Branch { kind: _, edges: es, span: _, origin: _ } => { for e in es.iter().cloned() {
                walk(e.child.clone(), buf, kind.clone());
            }; },
            _ => { (); },
        }
    }
    let mut buf: Vec<String> = Vec::new();
    for e in edges.iter().cloned() {
        walk(e.child.clone(), &mut buf, kind.clone());
    };
    (buf).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")
}

pub fn projectTable(edges: Vec<UniEdge>) -> MarkdownBlock {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let MdKind_TableRow = "markdown.table-row".to_string();
    let TableDelim = "markdown.table-delim".to_string();
    let Table = "markdown.table".to_string();
    let rows = edges.iter().cloned().filter_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == MdKind_TableRow) => Some(t.lexeme.clone()),
        _ => None,
    }).collect::<Vec<_>>();
    let delim = edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == TableDelim) => Some(t.lexeme.clone()),
        _ => None,
    });
    let header = rows.first().cloned().map(splitCells).unwrap_or(Vec::new());
    let body = (rows).iter().cloned().skip(1i64 as usize).collect::<Vec<_>>().iter().cloned().map(splitCells).collect::<Vec<_>>();
    let alignments = delim.map(parseAlignments).unwrap_or(Vec::new());
    MarkdownBlock::Table { header: header, alignments: alignments, rows: body }
}

pub fn splitCells(row: String) -> Vec<TableCell> {
    let trimmed = ((row.trim().to_string()).strip_prefix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (row.trim().to_string()).clone())).strip_suffix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| ((row.trim().to_string()).strip_prefix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (row.trim().to_string()).clone())).clone());
    trimmed.splitn(-1i64 as usize, "\\|").map(|p| p.to_string()).collect::<Vec<String>>().clone().into_iter().collect::<Vec<_>>().iter().cloned().map(|cell| { TableCell { inlines: inlineText(cell.trim().to_string()) } }).collect::<Vec<_>>()
}

pub fn parseAlignments(delim: String) -> Vec<ColumnAlignment> {
    let trimmed = ((delim.trim().to_string()).strip_prefix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (delim.trim().to_string()).clone())).strip_suffix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| ((delim.trim().to_string()).strip_prefix(("|".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (delim.trim().to_string()).clone())).clone());
    trimmed.splitn(-1i64 as usize, "\\|").map(|p| p.to_string()).collect::<Vec<String>>().clone().into_iter().collect::<Vec<_>>().iter().cloned().map(|spec| { let s = spec.trim().to_string();
    let left = s.starts_with(":");
    let right = s.ends_with(":");
    if (left && right) { ColumnAlignment::Center.clone() } else { if right { ColumnAlignment::Right.clone() } else { if left { ColumnAlignment::Left.clone() } else { ColumnAlignment::Default.clone() } } } }).collect::<Vec<_>>()
}

pub fn inlineText(text: String) -> Vec<MarkdownInline> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let Text = "markdown.text".to_string();
    if text.is_empty() { Vec::new() } else { vec![MarkdownInline::Text { value: text.clone() }] }
}

pub fn trimBlockInlines(is: Vec<MarkdownInline>) -> Vec<MarkdownInline> {
    let Text = "markdown.text".to_string();
    let SoftBreak = "markdown.soft-break".to_string();
    let HardBreak = "markdown.hard-break".to_string();
    let withoutTrailingBreak = match (is).as_slice() {
        [rest @ .., MarkdownInline::SoftBreak] => { let rest = rest.to_vec(); rest.clone() },
        [rest @ .., MarkdownInline::HardBreak] => { let rest = rest.to_vec(); rest.clone() },
        _ => is.clone(),
    };
    let n = (withoutTrailingBreak.len() as i64);
    withoutTrailingBreak.iter().cloned().enumerate().map(|(__i, __e)| (__e, __i as i64)).collect::<Vec<_>>().iter().cloned().map(|(inline, idx)| { match inline {
        MarkdownInline::Text { value: v } => { let afterBreak = ((idx > 0i64) && isBreak(withoutTrailingBreak[((idx - 1i64)) as usize].clone()));
        let beforeBreak = (((idx + 1i64) < n) && isBreak(withoutTrailingBreak[((idx + 1i64)) as usize].clone()));
        let mut out = v.clone();
        if ((idx == 0i64) || afterBreak) { out = dropLeadingSpaces(out.clone()); } else { (); }
        if ((idx == (n - 1i64)) || beforeBreak) { out = dropTrailingSpaces(out.clone()); } else { (); }
        MarkdownInline::Text { value: out.clone() } },
        other => other.clone(),
    } }).collect::<Vec<_>>().clone().into_iter().collect::<Vec<_>>().iter().cloned().filter(|__pf| match __pf {
        MarkdownInline::Text { value: __litpat6 } if __litpat6 == "" => false,
        _ => true,
    }).collect::<Vec<_>>()
}

pub fn isBreak(inline: MarkdownInline) -> bool {
    let SoftBreak = "markdown.soft-break".to_string();
    let HardBreak = "markdown.hard-break".to_string();
    match inline {
        MarkdownInline::SoftBreak | MarkdownInline::HardBreak => true,
        _ => false,
    }
}

pub fn dropLeadingSpaces(v: String) -> String {
    let mut i = 0i64;
    while ((i < crate::runtime::_str_length(&v)) && ((crate::runtime::_str_char_at(&v, i) == 32i64) || (crate::runtime::_str_char_at(&v, i) == 9i64))) {
        (i += 1i64);
    };
    crate::runtime::_str_substring_from(&v, i)
}

pub fn dropTrailingSpaces(v: String) -> String {
    let mut i = crate::runtime::_str_length(&v);
    while ((i > 0i64) && ((crate::runtime::_str_char_at(&v, (i - 1i64)) == 32i64) || (crate::runtime::_str_char_at(&v, (i - 1i64)) == 9i64))) {
        (i -= 1i64);
    };
    crate::runtime::_str_substring(&v, 0i64, i)
}

pub fn projectInlines(edges: Vec<UniEdge>, refs: std::collections::HashMap<String, MarkdownBlock>) -> Vec<MarkdownInline> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let Text = "markdown.text".to_string();
    fn appendMerging(inline: MarkdownInline, out: &mut Vec<MarkdownInline>) {
        match ((*out).last().cloned(), inline.clone()) {
            (Some(MarkdownInline::Text { value: a }), MarkdownInline::Text { value: b }) => { (*out) = [&({ let __v = ((*out)).clone(); let __k = (1i64) as usize; __v[..__v.len().saturating_sub(__k)].to_vec() })[..], &[MarkdownInline::Text { value: format!("{}{}", a, b) }][..]].concat(); },
            _ => { (*out).push(inline.clone()); },
        }
    }
    let mut out: Vec<MarkdownInline> = Vec::new();
    for edge in edges.iter().cloned() {
        projectInline(edge, refs.clone()).iter().cloned().for_each(|__x| appendMerging(__x, &mut out));
    };
    out
}

pub fn projectInline(edge: UniEdge, refs: std::collections::HashMap<String, MarkdownBlock>) -> Option<MarkdownInline> {
    let Emphasis = "markdown.emphasis".to_string();
    let Strong = "markdown.strong".to_string();
    let Strikethrough = "markdown.strikethrough".to_string();
    let CodeSpan = "markdown.code-span".to_string();
    let Link = "markdown.link".to_string();
    let Image = "markdown.image".to_string();
    let Expression = "markdown.expression".to_string();
    match edge.child {
        UniNode::Token { value: t } => projectInlineToken(t),
        UniNode::Branch { kind, edges, span: _, origin: _ } => match (kind).as_str() {
            "markdown.emphasis" => Some(MarkdownInline::Emphasis { children: projectInlines(edges.iter().cloned().filter(|__x| !(isDelimiterEdge)(__x.clone())).collect::<Vec<_>>(), refs.clone()) }),
            "markdown.strong" => Some(MarkdownInline::Strong { children: projectInlines(edges.iter().cloned().filter(|__x| !(isDelimiterEdge)(__x.clone())).collect::<Vec<_>>(), refs.clone()) }),
            "markdown.strikethrough" => Some(MarkdownInline::Strikethrough { children: projectInlines(edges.iter().cloned().filter(|__x| !(isDelimiterEdge)(__x.clone())).collect::<Vec<_>>(), refs.clone()) }),
            "markdown.code-span" => Some(MarkdownInline::Code { value: codeSpanValue(edges.clone()) }),
            "markdown.link" => Some(projectLink(edges.clone(), refs.clone(), false)),
            "markdown.image" => Some(projectLink(edges.clone(), refs.clone(), true)),
            "markdown.expression" => Some(MarkdownInline::Expression { source: expressionSource(edges.clone()) }),
            _ => None,
        },
    }
}

pub fn projectInlineToken(t: SourceToken) -> Option<MarkdownInline> {
    let Html = "markdown.html".to_string();
    let Text = "markdown.text".to_string();
    let Escape = "markdown.escape".to_string();
    let Entity = "markdown.entity".to_string();
    let DelimiterRun = "markdown.delimiter-run".to_string();
    let Autolink = "markdown.autolink".to_string();
    let SoftBreak = "markdown.soft-break".to_string();
    let HardBreak = "markdown.hard-break".to_string();
    match (t.kind).as_str() {
        "markdown.text" => Some(MarkdownInline::Text { value: t.lexeme.clone() }),
        "markdown.escape" => Some(MarkdownInline::Text { value: crate::runtime::_str_substring_from(&t.lexeme, 1i64) }),
        "markdown.entity" => Some(MarkdownInline::Text { value: decodeEntity(t.lexeme.clone()) }),
        "markdown.soft-break" => Some(MarkdownInline::SoftBreak.clone()),
        "markdown.hard-break" => Some(MarkdownInline::HardBreak.clone()),
        "markdown.autolink" => { let inner = ((t.lexeme).strip_prefix(("<".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (t.lexeme).clone())).strip_suffix((">".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| ((t.lexeme).strip_prefix(("<".to_string()).as_str()).map(|__r| __r.to_string()).unwrap_or_else(|| (t.lexeme).clone())).clone());
        Some(MarkdownInline::Autolink { destination: autolinkDestination(inner.clone()), label: inner.clone() }) },
        "markdown.html" => Some(MarkdownInline::RawHtml { raw: t.lexeme.clone() }),
        "markdown.delimiter-run" => Some(MarkdownInline::Text { value: t.lexeme.clone() }),
        _ => None,
    }
}

pub fn codeSpanValue(edges: Vec<UniEdge>) -> String {
    let CodeContent = "markdown.code-content".to_string();
    let raw = edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == CodeContent) => Some(t.lexeme.clone()),
        _ => None,
    }).unwrap_or("".to_string());
    let spaced = raw.chars().map(|__ch| char::from_u32(({ let c = (__ch as u32) as i64; if ((c == 10i64) || (c == 13i64)) { 32i64 } else { c.clone() } }) as u32).unwrap_or('\u{FFFD}')).collect::<String>();
    if ((((crate::runtime::_str_length(&spaced) >= 2i64) && (crate::runtime::_str_char_at(&spaced, 0i64) == 32i64)) && (crate::runtime::_str_char_at(&spaced, crate::runtime::_str_length(&spaced) - 1i64) == 32i64)) && spaced.chars().any(|__ch| (|__p0| { (__p0 != 32i64) })((__ch as u32) as i64))) { crate::runtime::_str_substring(&spaced, 1i64, (crate::runtime::_str_length(&spaced) - 1i64)) } else { spaced.clone() }
}

pub fn expressionSource(edges: Vec<UniEdge>) -> String {
    let ExpressionContent = "markdown.expression-content".to_string();
    edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == ExpressionContent) => Some(t.lexeme.clone()),
        _ => None,
    }).unwrap_or("".to_string())
}

pub fn projectLink(edges: Vec<UniEdge>, refs: std::collections::HashMap<String, MarkdownBlock>, image: bool) -> MarkdownInline {
    let Destination = "markdown.destination".to_string();
    let Title = "markdown.title".to_string();
    let ReferenceLabel = "markdown.reference-label".to_string();
    let Link = "markdown.link".to_string();
    let Image = "markdown.image".to_string();
    let label = projectInlines(edges.iter().cloned().filter(|__x| !(isLinkStructural)(__x.clone())).collect::<Vec<_>>(), refs.clone());
    let destTok = edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == Destination) => Some(unwrapDestination(t.lexeme.clone())),
        _ => None,
    });
    let titleTok = edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == Title) => Some(stripTitle(t.lexeme.clone())),
        _ => None,
    });
    let refLabel = edges.iter().cloned().find_map(|__c| match __c {
        UniEdge { role: _, child: UniNode::Token { value: t } } if (t.kind == ReferenceLabel) => Some(t.lexeme.clone()),
        _ => None,
    });
    let (dest, title) = match destTok {
        Some(d) => (d, titleTok),
        None => { let labelText = refLabel.map(extractRefLabel).filter(|__p0| { !__p0.is_empty() }).unwrap_or(rawLabel(edges.clone()));
        match refs.get(&normalizeLabel(labelText)).cloned() {
            Some(defn) => ((match &defn { MarkdownBlock::LinkDefinition { destination, .. } => destination.clone(), _ => unreachable!() }), (match &defn { MarkdownBlock::LinkDefinition { title, .. } => title.clone(), _ => unreachable!() })),
            None => ("".to_string(), None.clone()),
        } },
    };
    if image { MarkdownInline::Image { alt: label.clone(), destination: dest.clone(), title: title.clone() } } else { MarkdownInline::Link { label: label.clone(), destination: dest.clone(), title: title.clone() } }
}

pub fn isDelimiterEdge(edge: UniEdge) -> bool {
    let DelimiterRun = "markdown.delimiter-run".to_string();
    let StrikethroughRun = "markdown.strikethrough-run".to_string();
    match edge.child {
        UniNode::Token { value: t } => ((t.kind == DelimiterRun) || (t.kind == StrikethroughRun)),
        _ => false,
    }
}

pub fn isLinkStructural(edge: UniEdge) -> bool {
    let LinkOpen = "markdown.link-open".to_string();
    let LinkClose = "markdown.link-close".to_string();
    let DestOpen = "markdown.dest-open".to_string();
    let Destination = "markdown.destination".to_string();
    let Title = "markdown.title".to_string();
    let DestClose = "markdown.dest-close".to_string();
    let ReferenceLabel = "markdown.reference-label".to_string();
    match edge.child {
        UniNode::Token { value: t } => (((((((t.kind == LinkOpen) || (t.kind == LinkClose)) || (t.kind == DestOpen)) || (t.kind == Destination)) || (t.kind == Title)) || (t.kind == DestClose)) || (t.kind == ReferenceLabel)),
        _ => false,
    }
}

pub fn autolinkDestination(text: String) -> String {
    let lower = asciiLower(text.clone());
    if lower.starts_with("www.") { format!("{}{}", "http://".to_string(), text) } else { if (!(lower.contains(":")) && text.contains("@")) { format!("{}{}", "mailto:".to_string(), text) } else { text.clone() } }
}

pub fn extractRefLabel(lex: String) -> String {
    let mut open = -1i64;
    let mut i = 0i64;
    while (i < crate::runtime::_str_length(&lex)) {
        if (crate::runtime::_str_char_at(&lex, i) == 92i64) { (i += 2i64); } else { if (crate::runtime::_str_char_at(&lex, i) == 91i64) { open = i; } else { (); }
        (i += 1i64); }
    };
    let close = { let __h: &str = &lex; let __n: &str = &char::from_u32((93i64) as u32).unwrap_or('\u{FFFD}').to_string(); __h.rfind(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) };
    if ((open >= 0i64) && (close > open)) { crate::runtime::_str_substring(&lex, (open + 1i64), close) } else { "".to_string() }
}

pub fn rawLabel(edges: Vec<UniEdge>) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    fn walk(node: UniNode, buf: &mut Vec<String>) {
        match node {
            UniNode::Token { value: t } => { (*buf).push(t.lexeme); },
            UniNode::Branch { kind: _, edges: es, span: _, origin: _ } => { for e in es.iter().cloned() {
                walk(e.child.clone(), buf);
            }; },
        }
    }
    let mut buf: Vec<String> = Vec::new();
    for e in edges.iter().cloned().filter(|__x| !(isLinkStructural)(__x.clone())).collect::<Vec<_>>().iter().cloned() {
        walk(e.child.clone(), &mut buf);
    };
    (buf).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("")
}

pub fn unwrapDestination(lex: String) -> String {
    unwrapDestinationSlice(lex)
}

pub fn unwrapDestinationSlice(lex: String) -> String {
    let s = if (lex.starts_with("<") && lex.ends_with(">")) { crate::runtime::_str_substring(&lex, 1i64, (crate::runtime::_str_length(&lex) - 1i64)) } else { lex.clone() };
    decodeText(unescape(s))
}

pub fn stripTitle(lex: String) -> String {
    let s = if ((crate::runtime::_str_length(&lex) >= 2i64) && ((((crate::runtime::_str_char_at(&lex, 0i64) == 34i64) && (crate::runtime::_str_char_at(&lex, crate::runtime::_str_length(&lex) - 1i64) == 34i64)) || ((crate::runtime::_str_char_at(&lex, 0i64) == 39i64) && (crate::runtime::_str_char_at(&lex, crate::runtime::_str_length(&lex) - 1i64) == 39i64))) || ((crate::runtime::_str_char_at(&lex, 0i64) == 40i64) && (crate::runtime::_str_char_at(&lex, crate::runtime::_str_length(&lex) - 1i64) == 41i64)))) { crate::runtime::_str_substring(&lex, 1i64, (crate::runtime::_str_length(&lex) - 1i64)) } else { lex.clone() };
    decodeText(unescape(s))
}

pub fn decodeText(s: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    if !(s.contains(char::from_u32((38i64) as u32).unwrap_or('\u{FFFD}'))) { s.clone() } else { { let mut buf: Vec<String> = Vec::new();
    let mut i = 0i64;
    while (i < crate::runtime::_str_length(&s)) {
        if (crate::runtime::_str_char_at(&s, i) == 38i64) { let semi = crate::runtime::_str_index_of_from(&s, &(char::from_u32((59i64) as u32).unwrap_or('\u{FFFD}').to_string()), (i + 1i64));
        let decoded = if (semi < 0i64) { s.clone() } else { decodeEntity(crate::runtime::_str_substring(&s, i, (semi + 1i64))) };
        if ((semi >= 0i64) && (decoded != crate::runtime::_str_substring(&s, i, (semi + 1i64)))) { buf.push(decoded.clone());
        i = (semi + 1i64); } else { buf.push("&".to_string());
        (i += 1i64); } } else { buf.push(crate::runtime::_str_substring(&s, i, (i + 1i64)));
        (i += 1i64); }
    };
    (buf).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("") } }
}

pub fn unescape(s: String) -> String {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    if !(s.contains(char::from_u32((92i64) as u32).unwrap_or('\u{FFFD}'))) { s.clone() } else { { let mut buf: Vec<String> = Vec::new();
    let mut i = 0i64;
    while (i < crate::runtime::_str_length(&s)) {
        if (((crate::runtime::_str_char_at(&s, i) == 92i64) && ((i + 1i64) < crate::runtime::_str_length(&s))) && isAsciiPunctuation((crate::runtime::_str_char_at(&s, (i + 1i64))).0)) { buf.push(crate::runtime::_str_substring(&s, (i + 1i64), (i + 2i64)));
        (i += 2i64); } else { buf.push(crate::runtime::_str_substring(&s, i, (i + 1i64)));
        (i += 1i64); }
    };
    (buf).iter().map(|__e| format!("{}", __e)).collect::<Vec<String>>().join("") } }
}

pub fn namedEntities() -> std::collections::HashMap<String, String> {
    let table = "AEligÆ AMP& AacuteÁ AbreveĂ AcircÂ AcyА Afr𝔄 AgraveÀ AlphaΑ AmacrĀ And⩓ AogonĄ Aopf𝔸 ApplyFunction⁡ AringÅ Ascr𝒜 Assign≔ AtildeÃ AumlÄ Backslash∖ Barv⫧ Barwed⌆ BcyБ Because∵ Bernoullisℬ BetaΒ Bfr𝔅 Bopf𝔹 Breve˘ Bscrℬ Bumpeq≎ CHcyЧ COPY© CacuteĆ Cap⋒ CapitalDifferentialDⅅ Cayleysℭ CcaronČ CcedilÇ CcircĈ Cconint∰ CdotĊ Cedilla¸ CenterDot· Cfrℭ ChiΧ CircleDot⊙ CircleMinus⊖ CirclePlus⊕ CircleTimes⊗ ClockwiseContourIntegral∲ CloseCurlyDoubleQuote” CloseCurlyQuote’ Colon∷ Colone⩴ Congruent≡ Conint∯ ContourIntegral∮ Copfℂ Coproduct∐ CounterClockwiseContourIntegral∳ Cross⨯ Cscr𝒞 Cup⋓ CupCap≍ DDⅅ DDotrahd⤑ DJcyЂ DScyЅ DZcyЏ Dagger‡ Darr↡ Dashv⫤ DcaronĎ DcyД Del∇ DeltaΔ Dfr𝔇 DiacriticalAcute´ DiacriticalDot˙ DiacriticalDoubleAcute˝ DiacriticalGrave` DiacriticalTilde˜ Diamond⋄ DifferentialDⅆ Dopf𝔻 Dot¨ DotDot⃜ DotEqual≐ DoubleContourIntegral∯ DoubleDot¨ DoubleDownArrow⇓ DoubleLeftArrow⇐ DoubleLeftRightArrow⇔ DoubleLeftTee⫤ DoubleLongLeftArrow⟸ DoubleLongLeftRightArrow⟺ DoubleLongRightArrow⟹ DoubleRightArrow⇒ DoubleRightTee⊨ DoubleUpArrow⇑ DoubleUpDownArrow⇕ DoubleVerticalBar∥ DownArrow↓ DownArrowBar⤓ DownArrowUpArrow⇵ DownBreve̑ DownLeftRightVector⥐ DownLeftTeeVector⥞ DownLeftVector↽ DownLeftVectorBar⥖ DownRightTeeVector⥟ DownRightVector⇁ DownRightVectorBar⥗ DownTee⊤ DownTeeArrow↧ Downarrow⇓ Dscr𝒟 DstrokĐ ENGŊ ETHÐ EacuteÉ EcaronĚ EcircÊ EcyЭ EdotĖ Efr𝔈 EgraveÈ Element∈ EmacrĒ EmptySmallSquare◻ EmptyVerySmallSquare▫ EogonĘ Eopf𝔼 EpsilonΕ Equal⩵ EqualTilde≂ Equilibrium⇌ Escrℰ Esim⩳ EtaΗ EumlË Exists∃ ExponentialEⅇ FcyФ Ffr𝔉 FilledSmallSquare◼ FilledVerySmallSquare▪ Fopf𝔽 ForAll∀ Fouriertrfℱ Fscrℱ GJcyЃ GT> GammaΓ GammadϜ GbreveĞ GcedilĢ GcircĜ GcyГ GdotĠ Gfr𝔊 Gg⋙ Gopf𝔾 GreaterEqual≥ GreaterEqualLess⋛ GreaterFullEqual≧ GreaterGreater⪢ GreaterLess≷ GreaterSlantEqual⩾ GreaterTilde≳ Gscr𝒢 Gt≫ HARDcyЪ Hacekˇ Hat^ HcircĤ Hfrℌ HilbertSpaceℋ Hopfℍ HorizontalLine─ Hscrℋ HstrokĦ HumpDownHump≎ HumpEqual≏ IEcyЕ IJligĲ IOcyЁ IacuteÍ IcircÎ IcyИ Idotİ Ifrℑ IgraveÌ Imℑ ImacrĪ ImaginaryIⅈ Implies⇒ Int∬ Integral∫ Intersection⋂ InvisibleComma⁣ InvisibleTimes⁢ IogonĮ Iopf𝕀 IotaΙ Iscrℐ ItildeĨ IukcyІ IumlÏ JcircĴ JcyЙ Jfr𝔍 Jopf𝕁 Jscr𝒥 JsercyЈ JukcyЄ KHcyХ KJcyЌ KappaΚ KcedilĶ KcyК Kfr𝔎 Kopf𝕂 Kscr𝒦 LJcyЉ LT< LacuteĹ LambdaΛ Lang⟪ Laplacetrfℒ Larr↞ LcaronĽ LcedilĻ LcyЛ LeftAngleBracket⟨ LeftArrow← LeftArrowBar⇤ LeftArrowRightArrow⇆ LeftCeiling⌈ LeftDoubleBracket⟦ LeftDownTeeVector⥡ LeftDownVector⇃ LeftDownVectorBar⥙ LeftFloor⌊ LeftRightArrow↔ LeftRightVector⥎ LeftTee⊣ LeftTeeArrow↤ LeftTeeVector⥚ LeftTriangle⊲ LeftTriangleBar⧏ LeftTriangleEqual⊴ LeftUpDownVector⥑ LeftUpTeeVector⥠ LeftUpVector↿ LeftUpVectorBar⥘ LeftVector↼ LeftVectorBar⥒ Leftarrow⇐ Leftrightarrow⇔ LessEqualGreater⋚ LessFullEqual≦ LessGreater≶ LessLess⪡ LessSlantEqual⩽ LessTilde≲ Lfr𝔏 Ll⋘ Lleftarrow⇚ LmidotĿ LongLeftArrow⟵ LongLeftRightArrow⟷ LongRightArrow⟶ Longleftarrow⟸ Longleftrightarrow⟺ Longrightarrow⟹ Lopf𝕃 LowerLeftArrow↙ LowerRightArrow↘ Lscrℒ Lsh↰ LstrokŁ Lt≪ Map⤅ McyМ MediumSpace  Mellintrfℳ Mfr𝔐 MinusPlus∓ Mopf𝕄 Mscrℳ MuΜ NJcyЊ NacuteŃ NcaronŇ NcedilŅ NcyН NegativeMediumSpace​ NegativeThickSpace​ NegativeThinSpace​ NegativeVeryThinSpace​ NestedGreaterGreater≫ NestedLessLess≪ NewLine\n Nfr𝔑 NoBreak⁠ NonBreakingSpace  Nopfℕ Not⫬ NotCongruent≢ NotCupCap≭ NotDoubleVerticalBar∦ NotElement∉ NotEqual≠ NotEqualTilde≂̸ NotExists∄ NotGreater≯ NotGreaterEqual≱ NotGreaterFullEqual≧̸ NotGreaterGreater≫̸ NotGreaterLess≹ NotGreaterSlantEqual⩾̸ NotGreaterTilde≵ NotHumpDownHump≎̸ NotHumpEqual≏̸ NotLeftTriangle⋪ NotLeftTriangleBar⧏̸ NotLeftTriangleEqual⋬ NotLess≮ NotLessEqual≰ NotLessGreater≸ NotLessLess≪̸ NotLessSlantEqual⩽̸ NotLessTilde≴ NotNestedGreaterGreater⪢̸ NotNestedLessLess⪡̸ NotPrecedes⊀ NotPrecedesEqual⪯̸ NotPrecedesSlantEqual⋠ NotReverseElement∌ NotRightTriangle⋫ NotRightTriangleBar⧐̸ NotRightTriangleEqual⋭ NotSquareSubset⊏̸ NotSquareSubsetEqual⋢ NotSquareSuperset⊐̸ NotSquareSupersetEqual⋣ NotSubset⊂⃒ NotSubsetEqual⊈ NotSucceeds⊁ NotSucceedsEqual⪰̸ NotSucceedsSlantEqual⋡ NotSucceedsTilde≿̸ NotSuperset⊃⃒ NotSupersetEqual⊉ NotTilde≁ NotTildeEqual≄ NotTildeFullEqual≇ NotTildeTilde≉ NotVerticalBar∤ Nscr𝒩 NtildeÑ NuΝ OEligŒ OacuteÓ OcircÔ OcyО OdblacŐ Ofr𝔒 OgraveÒ OmacrŌ OmegaΩ OmicronΟ Oopf𝕆 OpenCurlyDoubleQuote“ OpenCurlyQuote‘ Or⩔ Oscr𝒪 OslashØ OtildeÕ Otimes⨷ OumlÖ OverBar‾ OverBrace⏞ OverBracket⎴ OverParenthesis⏜ PartialD∂ PcyП Pfr𝔓 PhiΦ PiΠ PlusMinus± Poincareplaneℌ Popfℙ Pr⪻ Precedes≺ PrecedesEqual⪯ PrecedesSlantEqual≼ PrecedesTilde≾ Prime″ Product∏ Proportion∷ Proportional∝ Pscr𝒫 PsiΨ QUOT\" Qfr𝔔 Qopfℚ Qscr𝒬 RBarr⤐ REG® RacuteŔ Rang⟫ Rarr↠ Rarrtl⤖ RcaronŘ RcedilŖ RcyР Reℜ ReverseElement∋ ReverseEquilibrium⇋ ReverseUpEquilibrium⥯ Rfrℜ RhoΡ RightAngleBracket⟩ RightArrow→ RightArrowBar⇥ RightArrowLeftArrow⇄ RightCeiling⌉ RightDoubleBracket⟧ RightDownTeeVector⥝ RightDownVector⇂ RightDownVectorBar⥕ RightFloor⌋ RightTee⊢ RightTeeArrow↦ RightTeeVector⥛ RightTriangle⊳ RightTriangleBar⧐ RightTriangleEqual⊵ RightUpDownVector⥏ RightUpTeeVector⥜ RightUpVector↾ RightUpVectorBar⥔ RightVector⇀ RightVectorBar⥓ Rightarrow⇒ Ropfℝ RoundImplies⥰ Rrightarrow⇛ Rscrℛ Rsh↱ RuleDelayed⧴ SHCHcyЩ SHcyШ SOFTcyЬ SacuteŚ Sc⪼ ScaronŠ ScedilŞ ScircŜ ScyС Sfr𝔖 ShortDownArrow↓ ShortLeftArrow← ShortRightArrow→ ShortUpArrow↑ SigmaΣ SmallCircle∘ Sopf𝕊 Sqrt√ Square□ SquareIntersection⊓ SquareSubset⊏ SquareSubsetEqual⊑ SquareSuperset⊐ SquareSupersetEqual⊒ SquareUnion⊔ Sscr𝒮 Star⋆ Sub⋐ Subset⋐ SubsetEqual⊆ Succeeds≻ SucceedsEqual⪰ SucceedsSlantEqual≽ SucceedsTilde≿ SuchThat∋ Sum∑ Sup⋑ Superset⊃ SupersetEqual⊇ Supset⋑ THORNÞ TRADE™ TSHcyЋ TScyЦ Tab\t TauΤ TcaronŤ TcedilŢ TcyТ Tfr𝔗 Therefore∴ ThetaΘ ThickSpace   ThinSpace  Tilde∼ TildeEqual≃ TildeFullEqual≅ TildeTilde≈ Topf𝕋 TripleDot⃛ Tscr𝒯 TstrokŦ UacuteÚ Uarr↟ Uarrocir⥉ UbrcyЎ UbreveŬ UcircÛ UcyУ UdblacŰ Ufr𝔘 UgraveÙ UmacrŪ UnderBar_ UnderBrace⏟ UnderBracket⎵ UnderParenthesis⏝ Union⋃ UnionPlus⊎ UogonŲ Uopf𝕌 UpArrow↑ UpArrowBar⤒ UpArrowDownArrow⇅ UpDownArrow↕ UpEquilibrium⥮ UpTee⊥ UpTeeArrow↥ Uparrow⇑ Updownarrow⇕ UpperLeftArrow↖ UpperRightArrow↗ Upsiϒ UpsilonΥ UringŮ Uscr𝒰 UtildeŨ UumlÜ VDash⊫ Vbar⫫ VcyВ Vdash⊩ Vdashl⫦ Vee⋁ Verbar‖ Vert‖ VerticalBar∣ VerticalLine| VerticalSeparator❘ VerticalTilde≀ VeryThinSpace  Vfr𝔙 Vopf𝕍 Vscr𝒱 Vvdash⊪ WcircŴ Wedge⋀ Wfr𝔚 Wopf𝕎 Wscr𝒲 Xfr𝔛 XiΞ Xopf𝕏 Xscr𝒳 YAcyЯ YIcyЇ YUcyЮ YacuteÝ YcircŶ YcyЫ Yfr𝔜 Yopf𝕐 Yscr𝒴 YumlŸ ZHcyЖ ZacuteŹ ZcaronŽ ZcyЗ ZdotŻ ZeroWidthSpace​ ZetaΖ Zfrℨ Zopfℤ Zscr𝒵 aacuteá abreveă ac∾ acE∾̳ acd∿ acircâ acute´ acyа aeligæ af⁡ afr𝔞 agraveà alefsymℵ alephℵ alphaα amacrā amalg⨿ amp& and∧ andand⩕ andd⩜ andslope⩘ andv⩚ ang∠ ange⦤ angle∠ angmsd∡ angmsdaa⦨ angmsdab⦩ angmsdac⦪ angmsdad⦫ angmsdae⦬ angmsdaf⦭ angmsdag⦮ angmsdah⦯ angrt∟ angrtvb⊾ angrtvbd⦝ angsph∢ angstÅ angzarr⍼ aogoną aopf𝕒 ap≈ apE⩰ apacir⩯ ape≊ apid≋ apos' approx≈ approxeq≊ aringå ascr𝒶 ast* asymp≈ asympeq≍ atildeã aumlä awconint∳ awint⨑ bNot⫭ backcong≌ backepsilon϶ backprime‵ backsim∽ backsimeq⋍ barvee⊽ barwed⌅ barwedge⌅ bbrk⎵ bbrktbrk⎶ bcong≌ bcyб bdquo„ becaus∵ because∵ bemptyv⦰ bepsi϶ bernouℬ betaβ bethℶ between≬ bfr𝔟 bigcap⋂ bigcirc◯ bigcup⋃ bigodot⨀ bigoplus⨁ bigotimes⨂ bigsqcup⨆ bigstar★ bigtriangledown▽ bigtriangleup△ biguplus⨄ bigvee⋁ bigwedge⋀ bkarow⤍ blacklozenge⧫ blacksquare▪ blacktriangle▴ blacktriangledown▾ blacktriangleleft◂ blacktriangleright▸ blank␣ blk12▒ blk14░ blk34▓ block█ bne=⃥ bnequiv≡⃥ bnot⌐ bopf𝕓 bot⊥ bottom⊥ bowtie⋈ boxDL╗ boxDR╔ boxDl╖ boxDr╓ boxH═ boxHD╦ boxHU╩ boxHd╤ boxHu╧ boxUL╝ boxUR╚ boxUl╜ boxUr╙ boxV║ boxVH╬ boxVL╣ boxVR╠ boxVh╫ boxVl╢ boxVr╟ boxbox⧉ boxdL╕ boxdR╒ boxdl┐ boxdr┌ boxh─ boxhD╥ boxhU╨ boxhd┬ boxhu┴ boxminus⊟ boxplus⊞ boxtimes⊠ boxuL╛ boxuR╘ boxul┘ boxur└ boxv│ boxvH╪ boxvL╡ boxvR╞ boxvh┼ boxvl┤ boxvr├ bprime‵ breve˘ brvbar¦ bscr𝒷 bsemi⁏ bsim∽ bsime⋍ bsol\\ bsolb⧅ bsolhsub⟈ bull• bullet• bump≎ bumpE⪮ bumpe≏ bumpeq≏ cacuteć cap∩ capand⩄ capbrcup⩉ capcap⩋ capcup⩇ capdot⩀ caps∩︀ caret⁁ caronˇ ccaps⩍ ccaronč ccedilç ccircĉ ccups⩌ ccupssm⩐ cdotċ cedil¸ cemptyv⦲ cent¢ centerdot· cfr𝔠 chcyч check✓ checkmark✓ chiχ cir○ cirE⧃ circˆ circeq≗ circlearrowleft↺ circlearrowright↻ circledR® circledSⓈ circledast⊛ circledcirc⊚ circleddash⊝ cire≗ cirfnint⨐ cirmid⫯ cirscir⧂ clubs♣ clubsuit♣ colon: colone≔ coloneq≔ comma, commat@ comp∁ compfn∘ complement∁ complexesℂ cong≅ congdot⩭ conint∮ copf𝕔 coprod∐ copy© copysr℗ crarr↵ cross✗ cscr𝒸 csub⫏ csube⫑ csup⫐ csupe⫒ ctdot⋯ cudarrl⤸ cudarrr⤵ cuepr⋞ cuesc⋟ cularr↶ cularrp⤽ cup∪ cupbrcap⩈ cupcap⩆ cupcup⩊ cupdot⊍ cupor⩅ cups∪︀ curarr↷ curarrm⤼ curlyeqprec⋞ curlyeqsucc⋟ curlyvee⋎ curlywedge⋏ curren¤ curvearrowleft↶ curvearrowright↷ cuvee⋎ cuwed⋏ cwconint∲ cwint∱ cylcty⌭ dArr⇓ dHar⥥ dagger† dalethℸ darr↓ dash‐ dashv⊣ dbkarow⤏ dblac˝ dcaronď dcyд ddⅆ ddagger‡ ddarr⇊ ddotseq⩷ deg° deltaδ demptyv⦱ dfisht⥿ dfr𝔡 dharl⇃ dharr⇂ diam⋄ diamond⋄ diamondsuit♦ diams♦ die¨ digammaϝ disin⋲ div÷ divide÷ divideontimes⋇ divonx⋇ djcyђ dlcorn⌞ dlcrop⌍ dollar$ dopf𝕕 dot˙ doteq≐ doteqdot≑ dotminus∸ dotplus∔ dotsquare⊡ doublebarwedge⌆ downarrow↓ downdownarrows⇊ downharpoonleft⇃ downharpoonright⇂ drbkarow⤐ drcorn⌟ drcrop⌌ dscr𝒹 dscyѕ dsol⧶ dstrokđ dtdot⋱ dtri▿ dtrif▾ duarr⇵ duhar⥯ dwangle⦦ dzcyџ dzigrarr⟿ eDDot⩷ eDot≑ eacuteé easter⩮ ecaroně ecir≖ ecircê ecolon≕ ecyэ edotė eeⅇ efDot≒ efr𝔢 eg⪚ egraveè egs⪖ egsdot⪘ el⪙ elinters⏧ ellℓ els⪕ elsdot⪗ emacrē empty∅ emptyset∅ emptyv∅ emsp13  emsp14  emsp  engŋ ensp  eogonę eopf𝕖 epar⋕ eparsl⧣ eplus⩱ epsiε epsilonε epsivϵ eqcirc≖ eqcolon≕ eqsim≂ eqslantgtr⪖ eqslantless⪕ equals= equest≟ equiv≡ equivDD⩸ eqvparsl⧥ erDot≓ erarr⥱ escrℯ esdot≐ esim≂ etaη ethð eumlë euro€ excl! exist∃ expectationℰ exponentialeⅇ fallingdotseq≒ fcyф female♀ ffiligﬃ ffligﬀ fflligﬄ ffr𝔣 filigﬁ fjligfj flat♭ flligﬂ fltns▱ fnofƒ fopf𝕗 forall∀ fork⋔ forkv⫙ fpartint⨍ frac12½ frac13⅓ frac14¼ frac15⅕ frac16⅙ frac18⅛ frac23⅔ frac25⅖ frac34¾ frac35⅗ frac38⅜ frac45⅘ frac56⅚ frac58⅝ frac78⅞ frasl⁄ frown⌢ fscr𝒻 gE≧ gEl⪌ gacuteǵ gammaγ gammadϝ gap⪆ gbreveğ gcircĝ gcyг gdotġ ge≥ gel⋛ geq≥ geqq≧ geqslant⩾ ges⩾ gescc⪩ gesdot⪀ gesdoto⪂ gesdotol⪄ gesl⋛︀ gesles⪔ gfr𝔤 gg≫ ggg⋙ gimelℷ gjcyѓ gl≷ glE⪒ gla⪥ glj⪤ gnE≩ gnap⪊ gnapprox⪊ gne⪈ gneq⪈ gneqq≩ gnsim⋧ gopf𝕘 grave` gscrℊ gsim≳ gsime⪎ gsiml⪐ gt> gtcc⪧ gtcir⩺ gtdot⋗ gtlPar⦕ gtquest⩼ gtrapprox⪆ gtrarr⥸ gtrdot⋗ gtreqless⋛ gtreqqless⪌ gtrless≷ gtrsim≳ gvertneqq≩︀ gvnE≩︀ hArr⇔ hairsp  half½ hamiltℋ hardcyъ harr↔ harrcir⥈ harrw↭ hbarℏ hcircĥ hearts♥ heartsuit♥ hellip… hercon⊹ hfr𝔥 hksearow⤥ hkswarow⤦ hoarr⇿ homtht∻ hookleftarrow↩ hookrightarrow↪ hopf𝕙 horbar― hscr𝒽 hslashℏ hstrokħ hybull⁃ hyphen‐ iacuteí ic⁣ icircî icyи iecyе iexcl¡ iff⇔ ifr𝔦 igraveì iiⅈ iiiint⨌ iiint∭ iinfin⧜ iiota℩ ijligĳ imacrī imageℑ imaglineℐ imagpartℑ imathı imof⊷ impedƵ in∈ incare℅ infin∞ infintie⧝ inodotı int∫ intcal⊺ integersℤ intercal⊺ intlarhk⨗ intprod⨼ iocyё iogonį iopf𝕚 iotaι iprod⨼ iquest¿ iscr𝒾 isin∈ isinE⋹ isindot⋵ isins⋴ isinsv⋳ isinv∈ it⁢ itildeĩ iukcyі iumlï jcircĵ jcyй jfr𝔧 jmathȷ jopf𝕛 jscr𝒿 jsercyј jukcyє kappaκ kappavϰ kcedilķ kcyк kfr𝔨 kgreenĸ khcyх kjcyќ kopf𝕜 kscr𝓀 lAarr⇚ lArr⇐ lAtail⤛ lBarr⤎ lE≦ lEg⪋ lHar⥢ lacuteĺ laemptyv⦴ lagranℒ lambdaλ lang⟨ langd⦑ langle⟨ lap⪅ laquo« larr← larrb⇤ larrbfs⤟ larrfs⤝ larrhk↩ larrlp↫ larrpl⤹ larrsim⥳ larrtl↢ lat⪫ latail⤙ late⪭ lates⪭︀ lbarr⤌ lbbrk❲ lbrace{ lbrack[ lbrke⦋ lbrksld⦏ lbrkslu⦍ lcaronľ lcedilļ lceil⌈ lcub{ lcyл ldca⤶ ldquo“ ldquor„ ldrdhar⥧ ldrushar⥋ ldsh↲ le≤ leftarrow← leftarrowtail↢ leftharpoondown↽ leftharpoonup↼ leftleftarrows⇇ leftrightarrow↔ leftrightarrows⇆ leftrightharpoons⇋ leftrightsquigarrow↭ leftthreetimes⋋ leg⋚ leq≤ leqq≦ leqslant⩽ les⩽ lescc⪨ lesdot⩿ lesdoto⪁ lesdotor⪃ lesg⋚︀ lesges⪓ lessapprox⪅ lessdot⋖ lesseqgtr⋚ lesseqqgtr⪋ lessgtr≶ lesssim≲ lfisht⥼ lfloor⌊ lfr𝔩 lg≶ lgE⪑ lhard↽ lharu↼ lharul⥪ lhblk▄ ljcyљ ll≪ llarr⇇ llcorner⌞ llhard⥫ lltri◺ lmidotŀ lmoust⎰ lmoustache⎰ lnE≨ lnap⪉ lnapprox⪉ lne⪇ lneq⪇ lneqq≨ lnsim⋦ loang⟬ loarr⇽ lobrk⟦ longleftarrow⟵ longleftrightarrow⟷ longmapsto⟼ longrightarrow⟶ looparrowleft↫ looparrowright↬ lopar⦅ lopf𝕝 loplus⨭ lotimes⨴ lowast∗ lowbar_ loz◊ lozenge◊ lozf⧫ lpar( lparlt⦓ lrarr⇆ lrcorner⌟ lrhar⇋ lrhard⥭ lrm‎ lrtri⊿ lsaquo‹ lscr𝓁 lsh↰ lsim≲ lsime⪍ lsimg⪏ lsqb[ lsquo‘ lsquor‚ lstrokł lt< ltcc⪦ ltcir⩹ ltdot⋖ lthree⋋ ltimes⋉ ltlarr⥶ ltquest⩻ ltrPar⦖ ltri◃ ltrie⊴ ltrif◂ lurdshar⥊ luruhar⥦ lvertneqq≨︀ lvnE≨︀ mDDot∺ macr¯ male♂ malt✠ maltese✠ map↦ mapsto↦ mapstodown↧ mapstoleft↤ mapstoup↥ marker▮ mcomma⨩ mcyм mdash— measuredangle∡ mfr𝔪 mho℧ microµ mid∣ midast* midcir⫰ middot· minus− minusb⊟ minusd∸ minusdu⨪ mlcp⫛ mldr… mnplus∓ models⊧ mopf𝕞 mp∓ mscr𝓂 mstpos∾ muμ multimap⊸ mumap⊸ nGg⋙̸ nGt≫⃒ nGtv≫̸ nLeftarrow⇍ nLeftrightarrow⇎ nLl⋘̸ nLt≪⃒ nLtv≪̸ nRightarrow⇏ nVDash⊯ nVdash⊮ nabla∇ nacuteń nang∠⃒ nap≉ napE⩰̸ napid≋̸ naposŉ napprox≉ natur♮ natural♮ naturalsℕ nbsp  nbump≎̸ nbumpe≏̸ ncap⩃ ncaronň ncedilņ ncong≇ ncongdot⩭̸ ncup⩂ ncyн ndash– ne≠ neArr⇗ nearhk⤤ nearr↗ nearrow↗ nedot≐̸ nequiv≢ nesear⤨ nesim≂̸ nexist∄ nexists∄ nfr𝔫 ngE≧̸ nge≱ ngeq≱ ngeqq≧̸ ngeqslant⩾̸ nges⩾̸ ngsim≵ ngt≯ ngtr≯ nhArr⇎ nharr↮ nhpar⫲ ni∋ nis⋼ nisd⋺ niv∋ njcyњ nlArr⇍ nlE≦̸ nlarr↚ nldr‥ nle≰ nleftarrow↚ nleftrightarrow↮ nleq≰ nleqq≦̸ nleqslant⩽̸ nles⩽̸ nless≮ nlsim≴ nlt≮ nltri⋪ nltrie⋬ nmid∤ nopf𝕟 not¬ notin∉ notinE⋹̸ notindot⋵̸ notinva∉ notinvb⋷ notinvc⋶ notni∌ notniva∌ notnivb⋾ notnivc⋽ npar∦ nparallel∦ nparsl⫽⃥ npart∂̸ npolint⨔ npr⊀ nprcue⋠ npre⪯̸ nprec⊀ npreceq⪯̸ nrArr⇏ nrarr↛ nrarrc⤳̸ nrarrw↝̸ nrightarrow↛ nrtri⋫ nrtrie⋭ nsc⊁ nsccue⋡ nsce⪰̸ nscr𝓃 nshortmid∤ nshortparallel∦ nsim≁ nsime≄ nsimeq≄ nsmid∤ nspar∦ nsqsube⋢ nsqsupe⋣ nsub⊄ nsubE⫅̸ nsube⊈ nsubset⊂⃒ nsubseteq⊈ nsubseteqq⫅̸ nsucc⊁ nsucceq⪰̸ nsup⊅ nsupE⫆̸ nsupe⊉ nsupset⊃⃒ nsupseteq⊉ nsupseteqq⫆̸ ntgl≹ ntildeñ ntlg≸ ntriangleleft⋪ ntrianglelefteq⋬ ntriangleright⋫ ntrianglerighteq⋭ nuν num# numero№ numsp  nvDash⊭ nvHarr⤄ nvap≍⃒ nvdash⊬ nvge≥⃒ nvgt>⃒ nvinfin⧞ nvlArr⤂ nvle≤⃒ nvlt<⃒ nvltrie⊴⃒ nvrArr⤃ nvrtrie⊵⃒ nvsim∼⃒ nwArr⇖ nwarhk⤣ nwarr↖ nwarrow↖ nwnear⤧ oSⓈ oacuteó oast⊛ ocir⊚ ocircô ocyо odash⊝ odblacő odiv⨸ odot⊙ odsold⦼ oeligœ ofcir⦿ ofr𝔬 ogon˛ ograveò ogt⧁ ohbar⦵ ohmΩ oint∮ olarr↺ olcir⦾ olcross⦻ oline‾ olt⧀ omacrō omegaω omicronο omid⦶ ominus⊖ oopf𝕠 opar⦷ operp⦹ oplus⊕ or∨ orarr↻ ord⩝ orderℴ orderofℴ ordfª ordmº origof⊶ oror⩖ orslope⩗ orv⩛ oscrℴ oslashø osol⊘ otildeõ otimes⊗ otimesas⨶ oumlö ovbar⌽ par∥ para¶ parallel∥ parsim⫳ parsl⫽ part∂ pcyп percnt% period. permil‰ perp⊥ pertenk‱ pfr𝔭 phiφ phivϕ phmmatℳ phone☎ piπ pitchfork⋔ pivϖ planckℏ planckhℎ plankvℏ plus+ plusacir⨣ plusb⊞ pluscir⨢ plusdo∔ plusdu⨥ pluse⩲ plusmn± plussim⨦ plustwo⨧ pm± pointint⨕ popf𝕡 pound£ pr≺ prE⪳ prap⪷ prcue≼ pre⪯ prec≺ precapprox⪷ preccurlyeq≼ preceq⪯ precnapprox⪹ precneqq⪵ precnsim⋨ precsim≾ prime′ primesℙ prnE⪵ prnap⪹ prnsim⋨ prod∏ profalar⌮ profline⌒ profsurf⌓ prop∝ propto∝ prsim≾ prurel⊰ pscr𝓅 psiψ puncsp  qfr𝔮 qint⨌ qopf𝕢 qprime⁗ qscr𝓆 quaternionsℍ quatint⨖ quest? questeq≟ quot\" rAarr⇛ rArr⇒ rAtail⤜ rBarr⤏ rHar⥤ race∽̱ racuteŕ radic√ raemptyv⦳ rang⟩ rangd⦒ range⦥ rangle⟩ raquo» rarr→ rarrap⥵ rarrb⇥ rarrbfs⤠ rarrc⤳ rarrfs⤞ rarrhk↪ rarrlp↬ rarrpl⥅ rarrsim⥴ rarrtl↣ rarrw↝ ratail⤚ ratio∶ rationalsℚ rbarr⤍ rbbrk❳ rbrace} rbrack] rbrke⦌ rbrksld⦎ rbrkslu⦐ rcaronř rcedilŗ rceil⌉ rcub} rcyр rdca⤷ rdldhar⥩ rdquo” rdquor” rdsh↳ realℜ realineℛ realpartℜ realsℝ rect▭ reg® rfisht⥽ rfloor⌋ rfr𝔯 rhard⇁ rharu⇀ rharul⥬ rhoρ rhovϱ rightarrow→ rightarrowtail↣ rightharpoondown⇁ rightharpoonup⇀ rightleftarrows⇄ rightleftharpoons⇌ rightrightarrows⇉ rightsquigarrow↝ rightthreetimes⋌ ring˚ risingdotseq≓ rlarr⇄ rlhar⇌ rlm‏ rmoust⎱ rmoustache⎱ rnmid⫮ roang⟭ roarr⇾ robrk⟧ ropar⦆ ropf𝕣 roplus⨮ rotimes⨵ rpar) rpargt⦔ rppolint⨒ rrarr⇉ rsaquo› rscr𝓇 rsh↱ rsqb] rsquo’ rsquor’ rthree⋌ rtimes⋊ rtri▹ rtrie⊵ rtrif▸ rtriltri⧎ ruluhar⥨ rx℞ sacuteś sbquo‚ sc≻ scE⪴ scap⪸ scaronš sccue≽ sce⪰ scedilş scircŝ scnE⪶ scnap⪺ scnsim⋩ scpolint⨓ scsim≿ scyс sdot⋅ sdotb⊡ sdote⩦ seArr⇘ searhk⤥ searr↘ searrow↘ sect§ semi; seswar⤩ setminus∖ setmn∖ sext✶ sfr𝔰 sfrown⌢ sharp♯ shchcyщ shcyш shortmid∣ shortparallel∥ shy­ sigmaσ sigmafς sigmavς sim∼ simdot⩪ sime≃ simeq≃ simg⪞ simgE⪠ siml⪝ simlE⪟ simne≆ simplus⨤ simrarr⥲ slarr← smallsetminus∖ smashp⨳ smeparsl⧤ smid∣ smile⌣ smt⪪ smte⪬ smtes⪬︀ softcyь sol/ solb⧄ solbar⌿ sopf𝕤 spades♠ spadesuit♠ spar∥ sqcap⊓ sqcaps⊓︀ sqcup⊔ sqcups⊔︀ sqsub⊏ sqsube⊑ sqsubset⊏ sqsubseteq⊑ sqsup⊐ sqsupe⊒ sqsupset⊐ sqsupseteq⊒ squ□ square□ squarf▪ squf▪ srarr→ sscr𝓈 ssetmn∖ ssmile⌣ sstarf⋆ star☆ starf★ straightepsilonϵ straightphiϕ strns¯ sub⊂ subE⫅ subdot⪽ sube⊆ subedot⫃ submult⫁ subnE⫋ subne⊊ subplus⪿ subrarr⥹ subset⊂ subseteq⊆ subseteqq⫅ subsetneq⊊ subsetneqq⫋ subsim⫇ subsub⫕ subsup⫓ succ≻ succapprox⪸ succcurlyeq≽ succeq⪰ succnapprox⪺ succneqq⪶ succnsim⋩ succsim≿ sum∑ sung♪ sup1¹ sup2² sup3³ sup⊃ supE⫆ supdot⪾ supdsub⫘ supe⊇ supedot⫄ suphsol⟉ suphsub⫗ suplarr⥻ supmult⫂ supnE⫌ supne⊋ supplus⫀ supset⊃ supseteq⊇ supseteqq⫆ supsetneq⊋ supsetneqq⫌ supsim⫈ supsub⫔ supsup⫖ swArr⇙ swarhk⤦ swarr↙ swarrow↙ swnwar⤪ szligß target⌖ tauτ tbrk⎴ tcaronť tcedilţ tcyт tdot⃛ telrec⌕ tfr𝔱 there4∴ therefore∴ thetaθ thetasymϑ thetavϑ thickapprox≈ thicksim∼ thinsp  thkap≈ thksim∼ thornþ tilde˜ times× timesb⊠ timesbar⨱ timesd⨰ tint∭ toea⤨ top⊤ topbot⌶ topcir⫱ topf𝕥 topfork⫚ tosa⤩ tprime‴ trade™ triangle▵ triangledown▿ triangleleft◃ trianglelefteq⊴ triangleq≜ triangleright▹ trianglerighteq⊵ tridot◬ trie≜ triminus⨺ triplus⨹ trisb⧍ tritime⨻ trpezium⏢ tscr𝓉 tscyц tshcyћ tstrokŧ twixt≬ twoheadleftarrow↞ twoheadrightarrow↠ uArr⇑ uHar⥣ uacuteú uarr↑ ubrcyў ubreveŭ ucircû ucyу udarr⇅ udblacű udhar⥮ ufisht⥾ ufr𝔲 ugraveù uharl↿ uharr↾ uhblk▀ ulcorn⌜ ulcorner⌜ ulcrop⌏ ultri◸ umacrū uml¨ uogonų uopf𝕦 uparrow↑ updownarrow↕ upharpoonleft↿ upharpoonright↾ uplus⊎ upsiυ upsihϒ upsilonυ upuparrows⇈ urcorn⌝ urcorner⌝ urcrop⌎ uringů urtri◹ uscr𝓊 utdot⋰ utildeũ utri▵ utrif▴ uuarr⇈ uumlü uwangle⦧ vArr⇕ vBar⫨ vBarv⫩ vDash⊨ vangrt⦜ varepsilonϵ varkappaϰ varnothing∅ varphiϕ varpiϖ varpropto∝ varr↕ varrhoϱ varsigmaς varsubsetneq⊊︀ varsubsetneqq⫋︀ varsupsetneq⊋︀ varsupsetneqq⫌︀ varthetaϑ vartriangleleft⊲ vartriangleright⊳ vcyв vdash⊢ vee∨ veebar⊻ veeeq≚ vellip⋮ verbar| vert| vfr𝔳 vltri⊲ vnsub⊂⃒ vnsup⊃⃒ vopf𝕧 vprop∝ vrtri⊳ vscr𝓋 vsubnE⫋︀ vsubne⊊︀ vsupnE⫌︀ vsupne⊋︀ vzigzag⦚ wcircŵ wedbar⩟ wedge∧ wedgeq≙ weierp℘ wfr𝔴 wopf𝕨 wp℘ wr≀ wreath≀ wscr𝓌 xcap⋂ xcirc◯ xcup⋃ xdtri▽ xfr𝔵 xhArr⟺ xharr⟷ xiξ xlArr⟸ xlarr⟵ xmap⟼ xnis⋻ xodot⨀ xopf𝕩 xoplus⨁ xotime⨂ xrArr⟹ xrarr⟶ xscr𝓍 xsqcup⨆ xuplus⨄ xutri△ xvee⋁ xwedge⋀ yacuteý yacyя ycircŷ ycyы yen¥ yfr𝔶 yicyї yopf𝕪 yscr𝓎 yucyю yumlÿ zacuteź zcaronž zcyз zdotż zeetrfℨ zetaζ zfr𝔷 zhcyж zigrarr⇝ zopf𝕫 zscr𝓏 zwj‍ zwnj‌".to_string().split(char::from_u32((0i64) as u32).unwrap_or('\u{FFFD}')).map(|p| p.to_string()).collect::<Vec<String>>().iter().cloned().map(|record| { let cut = { let __h: &str = &record; let __n: &str = &char::from_u32((crate::runtime::_to_int(&1i64)) as u32).unwrap_or('\u{FFFD}').to_string(); __h.find(__n).map(|__b| __h[..__b].encode_utf16().count() as i64).unwrap_or(-1) };
    (crate::runtime::_str_substring(&record, 0i64, cut), crate::runtime::_str_substring_from(&record, (cut + 1i64))) }).collect::<Vec<_>>().into_iter().collect::<std::collections::HashMap<_, _>>();
    table
}

pub fn decodeEntity(lex: String) -> String {
    if (!(lex.starts_with("&")) || !(lex.ends_with(";"))) { lex.clone() } else { { let body = crate::runtime::_str_substring(&lex, 1i64, (crate::runtime::_str_length(&lex) - 1i64));
    if (body.starts_with("#x") || body.starts_with("#X")) { {
      match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| { codePointToString(i64::from_str_radix(&(crate::runtime::_str_substring_from(&body, 2i64)), (16i64) as u32).unwrap_or(0)) })) {
        Ok(__v) => __v,
        Err(__p) => { let _ = __p; lex }
      }
    } } else { if body.starts_with("#") { {
      match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| { codePointToString(i64::from_str_radix(&(crate::runtime::_str_substring_from(&body, 1i64)), 10u32).unwrap_or(0)) })) {
        Ok(__v) => __v,
        Err(__p) => { let _ = __p; lex }
      }
    } } else { namedEntities().get(&body).cloned().unwrap_or(lex.into()) } } } }
}

pub fn codePointToString(cp: i64) -> String {
    if ((cp <= 0i64) || (cp > 1114111i64)) { "�".to_string() } else { if (cp <= 65535i64) { format!("{}", ((cp) as i64)) } else { { let c = (cp - 65536i64);
    let hi = (55296i64 + (c >> 10i64));
    let lo = (56320i64 + (c & 1023i64));
    format!("{}{}", format!("{}", ((hi) as i64)), format!("{}", ((lo) as i64))) } } }
}

pub fn CommonMarkDialect_instructions(source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
    let MarkdownLimits_default = MarkdownLimits { core: Limits { maxDepth: 512i64, maxNodes: 10000000i64, maxTokenCodePoints: ((16i64 * 1024i64) * 1024i64), maxDiagnostics: 10000i64 }, maxSourceCodePoints: ((64i64 * 1024i64) * 1024i64), maxLineCodePoints: (1024i64 * 1024i64), maxDelimiterRun: (1024i64 * 1024i64), maxFenceCodePoints: ((16i64 * 1024i64) * 1024i64), maxReferences: 1000000i64, maxBlocks: 10000000i64 };
    std::rc::Rc::new(MarkdownInstructionProcessor { source: source.source.clone(), profile: MarkdownProfile::CommonMark, limits: MarkdownLimits_default.clone() })
}

pub fn GfmDialect_instructions(source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
    let MarkdownLimits_default = MarkdownLimits { core: Limits { maxDepth: 512i64, maxNodes: 10000000i64, maxTokenCodePoints: ((16i64 * 1024i64) * 1024i64), maxDiagnostics: 10000i64 }, maxSourceCodePoints: ((64i64 * 1024i64) * 1024i64), maxLineCodePoints: (1024i64 * 1024i64), maxDelimiterRun: (1024i64 * 1024i64), maxFenceCodePoints: ((16i64 * 1024i64) * 1024i64), maxReferences: 1000000i64, maxBlocks: 10000000i64 };
    std::rc::Rc::new(MarkdownInstructionProcessor { source: source.source.clone(), profile: MarkdownProfile::Gfm, limits: MarkdownLimits_default.clone() })
}

pub fn ScalaScriptMarkdownDialect_instructions(source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
    let MarkdownLimits_default = MarkdownLimits { core: Limits { maxDepth: 512i64, maxNodes: 10000000i64, maxTokenCodePoints: ((16i64 * 1024i64) * 1024i64), maxDiagnostics: 10000i64 }, maxSourceCodePoints: ((64i64 * 1024i64) * 1024i64), maxLineCodePoints: (1024i64 * 1024i64), maxDelimiterRun: (1024i64 * 1024i64), maxFenceCodePoints: ((16i64 * 1024i64) * 1024i64), maxReferences: 1000000i64, maxBlocks: 10000000i64 };
    std::rc::Rc::new(MarkdownInstructionProcessor { source: source.source.clone(), profile: MarkdownProfile::ScalaScript, limits: MarkdownLimits_default.clone() })
}

pub fn dialectFor(profile: MarkdownProfile) -> std::rc::Rc<dyn DialectAdapter> {
    match profile {
        MarkdownProfile::CommonMark => std::rc::Rc::new(CommonMarkDialect),
        MarkdownProfile::Gfm => std::rc::Rc::new(GfmDialect),
        MarkdownProfile::ScalaScript => std::rc::Rc::new(ScalaScriptMarkdownDialect),
    }
}

pub fn dialectId(profile: MarkdownProfile) -> String {
    dialectFor(profile).id()
}

pub fn Markdown_parse(source: SourceInput, profile: MarkdownProfile, limits: MarkdownLimits) -> ParseResult {
    UniML_parse(source, std::rc::Rc::new(ConfiguredMarkdownDialect { profile: profile, limits: limits.clone() }), limits.core.clone())
}

pub fn Markdown_project(result: ParseResult, profile: MarkdownProfile) -> MarkdownProjectionResult {
    MarkdownProjection_project(result, profile)
}

pub fn isIdentStart(c: i64) -> bool {
    ((((c >= 97i64) && (c <= 122i64)) || ((c >= 65i64) && (c <= 90i64))) || (c == 95i64))
}

pub fn RustLexer_isDigit(c: i64) -> bool {
    ((c >= 48i64) && (c <= 57i64))
}

pub fn isIdentPart(c: i64) -> bool {
    (isIdentStart(c) || RustLexer_isDigit(c))
}

pub fn isSpace(c: i64) -> bool {
    ((((c == 32i64) || (c == 9i64)) || (c == 10i64)) || (c == 13i64))
}

pub fn plainStringEnd(chars: Vec<i64>, from: i64, n: i64) -> i64 {
    let mut i = (from + 1i64);
    let mut closed = false;
    while ((i < n) && !(closed)) {
        let c = chars[(i) as usize].clone();
        if (c == 92i64) { (i += 2i64); } else { if (c == 34i64) { (i += 1i64);
        closed = true; } else { (i += 1i64); } }
    };
    if (i > n) { n } else { i }
}

pub fn charLiteralEnd(chars: Vec<i64>, from: i64, n: i64) -> i64 {
    let mut i = (from + 1i64);
    let mut closed = false;
    while ((i < n) && !(closed)) {
        let c = chars[(i) as usize].clone();
        if (c == 92i64) { (i += 2i64); } else { if (c == 39i64) { (i += 1i64);
        closed = true; } else { (i += 1i64); } }
    };
    if (i > n) { n } else { i }
}

pub fn rawStringHashes(chars: Vec<i64>, from: i64, n: i64) -> i64 {
    let mut i = from;
    if ((i < n) && (chars[(i.clone()) as usize].clone() == 98i64)) { (i += 1i64); } else { (); }
    if ((i >= n) || (chars[(i.clone()) as usize].clone() != 114i64)) { -1i64 } else { { (i += 1i64);
    let mut hashes = 0i64;
    while ((i < n) && (chars[(i.clone()) as usize].clone() == 35i64)) {
        (hashes += 1i64);
        (i += 1i64);
    };
    if ((i < n) && (chars[(i.clone()) as usize].clone() == 34i64)) { hashes } else { -1i64 } } }
}

pub fn rawStringEnd(chars: Vec<i64>, from: i64, n: i64, hashes: i64) -> i64 {
    let mut i = from;
    if ((i < n) && (chars[(i.clone()) as usize].clone() == 98i64)) { (i += 1i64); } else { (); }
    (i += 1i64);
    (i += hashes);
    (i += 1i64);
    let mut closed = false;
    while ((i < n) && !(closed)) {
        if (chars[(i.clone()) as usize].clone() == 34i64) { let mut k = (i + 1i64);
        let mut matched = 0i64;
        while (((matched < hashes) && (k < n)) && (chars[(k) as usize].clone() == 35i64)) {
            (matched += 1i64);
            (k += 1i64);
        };
        if (matched == hashes) { i = k;
        closed = true; } else { (i += 1i64); } } else { (i += 1i64); }
    };
    if (i > n) { n } else { i.clone() }
}

pub fn blockCommentEnd(chars: Vec<i64>, from: i64, n: i64) -> i64 {
    let mut i = (from + 2i64);
    let mut depth = 1i64;
    while ((i < n) && (depth > 0i64)) {
        if ((((i + 1i64) < n) && (chars[(i) as usize].clone() == 47i64)) && (chars[((i + 1i64)) as usize].clone() == 42i64)) { (depth += 1i64);
        (i += 2i64); } else { if ((((i + 1i64) < n) && (chars[(i) as usize].clone() == 42i64)) && (chars[((i + 1i64)) as usize].clone() == 47i64)) { (depth -= 1i64);
        (i += 2i64); } else { (i += 1i64); } }
    };
    if (i > n) { n } else { i }
}

pub fn isLifetime(chars: Vec<i64>, from: i64, n: i64) -> bool {
    if ((from + 1i64) >= n) { false } else { if !(isIdentStart(chars[((from + 1i64)) as usize].clone())) { false } else { if ((from + 2i64) >= n) { true } else { (chars[((from + 2i64)) as usize].clone() != 39i64) } } }
}

pub fn lex(text: String) -> Vec<RustLexToken> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let WindowCodeUnits = 1024i64;
    let total = crate::runtime::_str_length(&text);
    let mut out: Vec<RustLexToken> = Vec::new();
    let mut pos = 0i64;
    while (pos < total) {
        let mut span = WindowCodeUnits.clone();
        let mut placed = false;
        while !(placed) {
            let atEof = ((pos + span) >= total);
            let end = if atEof { total.clone() } else { (pos + span) };
            let window = lexWindow(crate::runtime::_str_substring(&text, pos, end), atEof);
            if (window.consumed > 0i64) { out.extend((window.tokens).iter().cloned());
            (pos += window.consumed);
            placed = true; } else { if atEof { pos = total.clone();
            placed = true; } else { span = (span * 2i64); } }
        };
    };
    out
}

pub fn lexWindow(text: String, atEof: bool) -> WindowLex {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let chars = text.encode_utf16().map(|__u| __u as i64).collect::<Vec<i64>>();
    let n = (chars.len() as i64);
    let mut out: Vec<RustLexToken> = Vec::new();
    let mut consumed = 0i64;
    let mut i = 0i64;
    let mut stop = false;
    while ((i < n) && !(stop)) {
        let start = i;
        let c = chars[(i) as usize].clone();
        let mut kind = "rust.punct".to_string();
        let hashes = if ((c == 114i64) || (c == 98i64)) { rawStringHashes(chars.clone(), i, n.clone()) } else { -1i64 };
        if isSpace(c.clone()) { kind = "rust.ws".to_string();
        while ((i < n) && isSpace(chars[(i) as usize].clone())) {
            (i += 1i64);
        }; } else { if (((c == 47i64) && ((i + 1i64) < n)) && (chars[((i + 1i64)) as usize].clone() == 47i64)) { kind = "rust.line-comment".to_string();
        while ((i < n) && (chars[(i) as usize].clone() != 10i64)) {
            (i += 1i64);
        }; } else { if (((c == 47i64) && ((i + 1i64) < n)) && (chars[((i + 1i64)) as usize].clone() == 42i64)) { kind = "rust.block-comment".to_string();
        i = blockCommentEnd(chars.clone(), i, n.clone()); } else { if (hashes >= 0i64) { kind = "rust.string".to_string();
        i = rawStringEnd(chars.clone(), i, n.clone(), hashes.clone()); } else { if (c == 34i64) { kind = "rust.string".to_string();
        i = plainStringEnd(chars.clone(), i, n.clone()); } else { if (((c == 98i64) && ((i + 1i64) < n)) && (chars[((i + 1i64)) as usize].clone() == 34i64)) { kind = "rust.string".to_string();
        i = plainStringEnd(chars.clone(), (i + 1i64), n.clone()); } else { if (((c == 98i64) && ((i + 2i64) < n)) && (chars[((i + 1i64)) as usize].clone() == 39i64)) { kind = "rust.char".to_string();
        i = charLiteralEnd(chars.clone(), (i + 1i64), n.clone()); } else { if ((c == 39i64) && isLifetime(chars.clone(), i, n.clone())) { kind = "rust.lifetime".to_string();
        (i += 1i64);
        while ((i < n) && isIdentPart(chars[(i) as usize].clone())) {
            (i += 1i64);
        }; } else { if (c == 39i64) { kind = "rust.char".to_string();
        i = charLiteralEnd(chars.clone(), i, n.clone()); } else { if RustLexer_isDigit(c.clone()) { kind = "rust.number".to_string();
        (i += 1i64);
        let mut going = true;
        while ((i < n) && going) {
            let d = chars[(i) as usize].clone();
            if isIdentPart(d.clone()) { (i += 1i64); } else { if (((d == 46i64) && ((i + 1i64) < n)) && RustLexer_isDigit(chars[((i + 1i64)) as usize].clone())) { (i += 1i64); } else { going = false; } }
        }; } else { if isIdentStart(c.clone()) { kind = "rust.ident".to_string();
        while ((i < n) && isIdentPart(chars[(i) as usize].clone())) {
            (i += 1i64);
        }; } else { (i += 1i64); } } } } } } } } } } }
        if (!(atEof) && (i >= n)) { stop = true; } else { out.push(RustLexToken { kind: kind.clone(), lexeme: crate::runtime::_str_substring(&text, start, i) });
        consumed = i; }
    };
    WindowLex { tokens: out.clone(), consumed: consumed }
}

pub fn isModifier(w: String) -> bool {
    ((((((w == "pub".to_string()) || (w == "async".to_string())) || (w == "unsafe".to_string())) || (w == "extern".to_string())) || (w == "default".to_string())) || (w == "move".to_string()))
}

pub fn itemKind(w: String) -> String {
    if (w == "fn".to_string()) { "rust.fn".to_string() } else { if (w == "struct".to_string()) { "rust.struct".to_string() } else { if (w == "enum".to_string()) { "rust.enum".to_string() } else { if (w == "trait".to_string()) { "rust.trait".to_string() } else { if (w == "impl".to_string()) { "rust.impl".to_string() } else { if (w == "mod".to_string()) { "rust.mod".to_string() } else { if (w == "use".to_string()) { "rust.use".to_string() } else { if (w == "const".to_string()) { "rust.const".to_string() } else { if (w == "static".to_string()) { "rust.const".to_string() } else { if (w == "type".to_string()) { "rust.type".to_string() } else { if (w == "union".to_string()) { "rust.struct".to_string() } else { if (w == "macro_rules".to_string()) { "rust.macro".to_string() } else { "".to_string() } } } } } } } } } } } }
}

pub fn isTrivia(kind: String) -> bool {
    (((kind == "rust.ws".to_string()) || (kind == "rust.line-comment".to_string())) || (kind == "rust.block-comment".to_string()))
}

pub fn isPunct(t: RustLexToken, s: String) -> bool {
    ((t.kind == "rust.punct".to_string()) && (t.lexeme == s))
}

pub fn nextSignificant(tokens: Vec<RustLexToken>, from: i64, until: i64) -> i64 {
    let mut i = from;
    while ((i < until) && isTrivia(tokens[(i.clone()) as usize].clone().kind)) {
        (i += 1i64);
    };
    i
}

pub fn headStartIndex(tokens: Vec<RustLexToken>, from: i64, until: i64, atRangeStart: bool) -> i64 {
    let mut i = from;
    let mut crossed = atRangeStart;
    let mut result = -1i64;
    while ((result < 0i64) && (i < until)) {
        let k = tokens[(i.clone()) as usize].clone().kind;
        if (k == "rust.ws".to_string()) { if tokens[(i.clone()) as usize].clone().lexeme.contains("\n") { crossed = true; } else { (); }
        (i += 1i64); } else { if ((k == "rust.line-comment".to_string()) || (k == "rust.block-comment".to_string())) { if crossed { result = i.clone(); } else { (i += 1i64); } } else { result = i.clone(); } }
    };
    if (result < 0i64) { until } else { result }
}

pub fn skipBalanced(tokens: Vec<RustLexToken>, from: i64, until: i64, open: String, close: String) -> i64 {
    let mut i = from;
    let mut depth = 0i64;
    let mut done = false;
    while ((i < until) && !(done)) {
        if isPunct(tokens[(i.clone()) as usize].clone(), open.clone()) { (depth += 1i64); } else { if isPunct(tokens[(i.clone()) as usize].clone(), close.clone()) { (depth -= 1i64);
        if (depth == 0i64) { done = true; } else { (); } } else { (); } }
        (i += 1i64);
    };
    i
}

pub fn skipHead(tokens: Vec<RustLexToken>, from: i64, until: i64) -> i64 {
    let mut i = nextSignificant(tokens.clone(), from, until);
    let mut going = true;
    while (going && (i < until)) {
        let t = tokens[(i.clone()) as usize].clone();
        if isPunct(t.clone(), "#".to_string()) { let br = nextSignificant(tokens.clone(), (i + 1i64), until.clone());
        let br2 = if ((br < until) && isPunct(tokens[(br.clone()) as usize].clone(), "!".to_string())) { nextSignificant(tokens.clone(), (br + 1i64), until.clone()) } else { br.clone() };
        if ((br2 < until) && isPunct(tokens[(br2.clone()) as usize].clone(), "[".to_string())) { i = nextSignificant(tokens.clone(), skipBalanced(tokens.clone(), br2.clone(), until.clone(), "[".to_string(), "]".to_string()), until.clone()); } else { going = false; } } else { if ((t.kind == "rust.ident".to_string()) && isModifier(t.lexeme.clone())) { let after = nextSignificant(tokens.clone(), (i + 1i64), until.clone());
        if ((after < until) && isPunct(tokens[(after.clone()) as usize].clone(), "(".to_string())) { i = nextSignificant(tokens.clone(), skipBalanced(tokens.clone(), after.clone(), until.clone(), "(".to_string(), ")".to_string()), until.clone()); } else { if ((after < until) && (tokens[(after.clone()) as usize].clone().kind == "rust.string".to_string())) { i = nextSignificant(tokens.clone(), (after + 1i64), until.clone()); } else { i = after.clone(); } } } else { going = false; } }
    };
    i
}

pub fn resolveKeyword(tokens: Vec<RustLexToken>, kw: i64, until: i64) -> i64 {
    let w = tokens[(kw) as usize].clone().lexeme;
    if ((w == "const".to_string()) || (w == "static".to_string())) { { let after = nextSignificant(tokens.clone(), (kw + 1i64), until);
    if (((after < until) && (tokens[(after.clone()) as usize].clone().kind == "rust.ident".to_string())) && (tokens[(after.clone()) as usize].clone().lexeme == "fn".to_string())) { after.clone() } else { kw } } } else { kw }
}

pub fn itemName(tokens: Vec<RustLexToken>, kw: i64, until: i64) -> String {
    let mut i = nextSignificant(tokens.clone(), (kw + 1i64), until);
    if ((i < until) && isPunct(tokens[(i.clone()) as usize].clone(), "<".to_string())) { i = nextSignificant(tokens.clone(), skipBalanced(tokens.clone(), i.clone(), until, "<".to_string(), ">".to_string()), until); } else { (); }
    if ((i < until) && (tokens[(i.clone()) as usize].clone().kind == "rust.ident".to_string())) { tokens[(i.clone()) as usize].clone().lexeme } else { "".to_string() }
}

pub fn itemEnd(tokens: Vec<RustLexToken>, kw: i64, until: i64) -> i64 {
    let mut i = kw;
    let mut depth = 0i64;
    let mut end = -1i64;
    while ((i < until) && (end < 0i64)) {
        let t = tokens[(i.clone()) as usize].clone();
        if isPunct(t.clone(), "{".to_string()) { (depth += 1i64); } else { if isPunct(t.clone(), "}".to_string()) { (depth -= 1i64);
        if (depth <= 0i64) { end = i.clone(); } else { (); } } else { if (isPunct(t.clone(), ";".to_string()) && (depth == 0i64)) { end = i.clone(); } else { (); } } }
        (i += 1i64);
    };
    if (end < 0i64) { (until - 1i64) } else { end }
}

pub fn bodyStart(tokens: Vec<RustLexToken>, kw: i64, endIdx: i64) -> i64 {
    let mut i = kw;
    let mut found = -1i64;
    while ((i <= endIdx) && (found < 0i64)) {
        if isPunct(tokens[(i.clone()) as usize].clone(), "{".to_string()) { found = (i + 1i64); } else { (); }
        (i += 1i64);
    };
    found
}

pub fn collect(tokens: Vec<RustLexToken>, from: i64, until: i64, depth: i64) -> Vec<ItemSpan> {
    let empty = DialectRegistry { byName: std::collections::HashMap::new() };
    let mut out: Vec<ItemSpan> = Vec::new();
    let mut i = from;
    while (i < until) {
        let headStart = headStartIndex(tokens.clone(), i.clone(), until.clone(), (i == from));
        if (headStart >= until) { i = until.clone(); } else { let kwRaw = skipHead(tokens.clone(), headStart.clone(), until.clone());
        let kw = if (kwRaw < until) { resolveKeyword(tokens.clone(), kwRaw.clone(), until.clone()) } else { kwRaw.clone() };
        let kind = if ((kw < until) && (tokens[(kw.clone()) as usize].clone().kind == "rust.ident".to_string())) { itemKind(tokens[(kw.clone()) as usize].clone().lexeme) } else { "".to_string() };
        if (kind == "".to_string()) { let skipTo = itemEnd(tokens.clone(), headStart.clone(), until.clone());
        i = if (skipTo >= headStart) { (skipTo + 1i64) } else { (headStart + 1i64) }; } else { let end = itemEnd(tokens.clone(), kw.clone(), until.clone());
        let bs = bodyStart(tokens.clone(), kw.clone(), end.clone());
        out.push(ItemSpan { start: headStart.clone(), end: end.clone(), kind: kind.clone(), name: itemName(tokens.clone(), kw.clone(), until.clone()), bodyOpen: if (bs < 0i64) { -1i64 } else { (bs - 1i64) } });
        if ((depth == 0i64) && ((kind == "rust.impl".to_string()) || (kind == "rust.mod".to_string()))) { if ((bs >= 0i64) && (bs < end)) { out.extend((collect(tokens.clone(), bs.clone(), end.clone(), (depth + 1i64))).iter().cloned()); } else { (); } } else { (); }
        i = (end + 1i64); } }
    };
    out
}

pub fn RustDialect_instructions(source: SourceInput) -> std::rc::Rc<dyn Processor<String, SourceChunk, VmToken>> {
    std::rc::Rc::new(RustProcessor { source: source.source.clone() })
}
