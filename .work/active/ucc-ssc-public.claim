feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-10T20:10:00Z
status: blocked
done-so-far: routes correct on the run path; three run-vs-build-rust divergences measured and all three now FIXED upstream (json-core, list methods, std/http+fs imports). Fixed my own bug on the branch (b6622cd): `hi(0)` on a tuple is invalid on BOTH lanes — the interpreter refuses it too — so it was never a divergence; the run-path proof never reached that line.
next: TWO errors left in public-matrix.ssc on scalascript main `7fea7a711` — down from 33. Two of my three fixes are MERGED upstream: f73944322 (string provenance) and 7b82d3356 (Value len/get/iter). Remaining: (1) the route handler — my branch `fix/rust-request-value` (69decab8f) sits UNMERGED in their repo, 273/273, proven by serving (/lambda -> lambda-ok, /path -> /path, /method -> GET); it also edits their pinned RustGenR5Test because RouteHandler became an enum. (2) `headOption` on a list is emitted as a Rust FIELD access — filed as `no-paren-list-method-becomes-a-field` with a control; their by-name refusal covers calls but not no-paren member access. DO NOT "fix" our source for (2): I tried `.headOption` and it made it worse (4 errors, receiver reported as Option<String>). When both land, this slice should compile — then run it and check the two routes answer. Quote the SHA FROM THE BANNER.
