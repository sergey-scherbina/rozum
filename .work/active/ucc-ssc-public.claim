feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-13T12:00:00Z
status: in-progress
done-so-far: routes correct on the run path; three run-vs-build-rust divergences measured and all three now FIXED upstream (json-core, list methods, std/http+fs imports). Fixed my own bug on the branch (b6622cd): `hi(0)` on a tuple is invalid on BOTH lanes — the interpreter refuses it too — so it was never a divergence; the run-path proof never reached that line.
next: UNBLOCKED — the slice BUILDS AND SERVES. Measured on scalascript main 2026-08-12: public-matrix.ssc -> 0 errors, 1,421,744-byte binary; started it and both routes answer, each returning {"error":"invalid or revoked token"} without a valid token, which is the guard working. Their std corpus is now BADRUST 0 (was 25 when I surveyed it) — 15 real fixes, 8 now refusing honestly. REMAINING to finish slice 1: (a) rebase feature/ucc-ssc-public on master and re-verify, (b) wire the deploy path (the .ssc binary vs the Rust control-serve on :8411 — they are complementary, :8412 vs :8411), (c) finish docs/specs/ucc-ssc-backend.md, (d) decide whether slice 1 lands as-is or waits for slice 2. Quote the SHA FROM THE BANNER; check the digest, not the stamp.

debt: public-matrix.ssc carries a workaround that becomes a LIE if my scalascript branch `fix/string-numeric-conversions` lands: `isInt` takes two local copies of its parameter because the direct `s.toInt.toString == s` was E0382, and the comment beside it says so. The fix makes `_to_int` borrow, so the direct form compiles — when it lands, simplify `isInt` back to one line and DELETE that comment. Same for the dot-splitting in `isNumeric`: it exists only because `toDouble` on a String emitted an invalid cast, which the same branch fixes.