feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-10T12:15:00Z
status: blocked
done-so-far: routes correct on the run path; three run-vs-build-rust divergences measured and all three now FIXED upstream (json-core, list methods, std/http+fs imports). Fixed my own bug on the branch (b6622cd): `hi(0)` on a tuple is invalid on BOTH lanes — the interpreter refuses it too — so it was never a divergence; the run-path proof never reached that line.
next: measured on scalascript `ed0c655b0` (cache HIT, stamp == tree, banner silent — their toolchain-cache break is FIXED, build normally, no workaround needed). Four of my five reports are `status: fixed` and VERIFIED by re-running my own repros (map-get and zipWithIndex both build and match `ssc run`). Slice still 17 errors, unchanged — because 8 of them were MIS-GROUPED by me: I blamed `Map.get`, they fixed that correctly, and the real cause is the operand PROVENANCE. Isolated it: a String from `Option.getOrElse` is not known to be a String, so concatenation emits `+` without the borrow (`format!(...) + viaGet`, expected `&str` found `String`), while a plain val on the line above compiles. Four hypotheses did NOT reproduce first (plain a+b, 3-term chain, call result, 4-term chain) — do not re-derive them. Added as the THIRD instance to `type-lost-across-a-boundary` in their INBOX (the only entry of mine still open), with repro `concat-a-value-from-getorelse.ssc`. That one shape covers 8 of the 17. Do NOT hand-roll a token check around this. Quote the SHA FROM THE BANNER.
