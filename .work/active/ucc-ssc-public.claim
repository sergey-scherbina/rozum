feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-10T10:30:00Z
status: blocked
done-so-far: routes correct on the run path; three run-vs-build-rust divergences measured and all three now FIXED upstream (json-core, list methods, std/http+fs imports). Fixed my own bug on the branch (b6622cd): `hi(0)` on a tuple is invalid on BOTH lanes — the interpreter refuses it too — so it was never a divergence; the run-path proof never reached that line.
next: 17 errors left in public-matrix.ssc on scalascript `502b9f181`, and ALL of them are now accounted for and reported — nothing here is unexplained. (a) 8 x borrow, `map-get-lowers-to-an-owned-key`; (b) 3 x a `match` on a Value not narrowing its arms — observation only, unminimised; (c) 1 x indexing a zipWithIndex result, `zipwithindex-result-is-not-indexable`; (d) 2 x `type-lost-across-a-boundary` — a DECLARED `List[String]` return indexes as a call, and `toInt` on a lambda parameter emits `String as i32`; each filed WITH a control in the same file that compiles; (e) 3 misc not yet minimised (Request/String and two others). All four entries sit in their INBOX, unrouted. NOTE: `toInt` is NOT broken in general — only inside a lambda; measured before reporting. Do NOT hand-roll a token check around any of this. Resume when (a) lands; quote the SHA FROM THE BANNER.

gotcha: BUILDING THEIR TOOLCHAIN IN A FRESH WORKTREE IS BROKEN as of scalascript 502b9f181 — their new toolchain cache stores only `lib`, no `bin`, and a cache HIT skips `cli/installBin`, which is the only launcher generator. You get `bin/ssc` (it is tracked in git) and NO `ssc-tools` / `ssc-standard`, so any build-rust measurement dies with rc=127. install.sh DOES fail loudly (exit 1, names the missing launcher) — read its rc, not your wrapper's (a wrapper ending in `echo` reports 0). Workaround: `SSC_TOOLCHAIN_CACHE_OFF=1 ./install.sh --dev`. Reported in the room 2026-08-10; delete this line once they fix it.