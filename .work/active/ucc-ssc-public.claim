feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-09T19:40:00Z
status: blocked
done-so-far: routes correct and proven on the run path; three run-vs-build-rust divergences measured, the third (generated Rust does not compile) closes the shipping path
next: measured on scalascript `ba40b376a` (stamp == tree, banner silent). TWO of the three blockers are GONE. (1) json-core: the six-line repro builds AND runs, 155 rustc errors -> 0. (2) list methods: my report `list-methods-pass-through-to-rust` is fixed-in fb9ac0923 — indexOf/find/zipWithIndex now build and match `ssc run` byte for byte, and an unlowered method on a known List/String receiver refuses BY NAME instead of passing through. (3) std/http + std/fs imports: the sibling report `build-rust-std-imports-unlowerable` does NOT reproduce — 0 errors, binary built; I answered it on their INBOX with the repro it was missing. REMAINING for public-matrix.ssc: 29 errors, of ONE dominant shape — 15 of 17 type mismatches are "expected a reference, got an owned String" (`Map.get` lowers to `HashMap::get(String)`), filed as `map-get-lowers-to-an-owned-key`; plus `Value.get`/`Value.exists` (3), which their in-flight claim `rust-value-get-exists` already covers. Do NOT hand-roll a token check around this. Resume when the borrow lowering lands; quote the SHA FROM THE BANNER.
