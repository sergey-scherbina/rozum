feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-09T20:30:00Z
status: blocked
done-so-far: routes correct on the run path; three run-vs-build-rust divergences measured and all three now FIXED upstream (json-core, list methods, std/http+fs imports). Fixed my own bug on the branch (b6622cd): `hi(0)` on a tuple is invalid on BOTH lanes — the interpreter refuses it too — so it was never a divergence; the run-path proof never reached that line.
next: measured on scalascript `4a93c440c` (stamp == tree, banner silent). public-matrix.ssc: 33 -> 29 -> 20 -> 17 errors today, 3 of them removed by MY OWN fix. Remaining 17, by shape: (a) 8 x "expected &str, got String" — filed as `map-get-lowers-to-an-owned-key`, in their INBOX, unrouted; (b) 3 x a `match` on a Value binding its arms as Value again so `l.iter()` / `m.get(...)` do not resolve — the borrow is CORRECT there now, the receiver is not narrowed; reported as an unminimised observation only; (c) 1 x indexing a `zipWithIndex` result lowers to a call — filed as `zipwithindex-result-is-not-indexable` WITH a literal-tuple control proving it is the result type, not tuples; (d) 5 misc (Request/String, a cast, an annotation). LESSON worth keeping: minimise before filing — 3 of 20 were mine, and reporting them would have cost them a triage round. Do NOT hand-roll a token check around any of this. Resume when (a) lands; quote the SHA FROM THE BANNER.
