feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-09T18:05:00Z
status: blocked
done-so-far: routes correct and proven on the run path; three run-vs-build-rust divergences measured, the third (generated Rust does not compile) closes the shipping path
next: STILL BLOCKED UPSTREAM but very close, measured 2026-08-09 on a toolchain built from scalascript `689401671` (banner silent, isolated worktree). `build-rust` on the std/json repro is down from 155 rustc errors to **exactly 1** — rustc's own summary says "due to 1 previous error" — and the four `__jsonCore*` extern refusals are GONE. What remains is a contract mismatch their `rust-emit-reachable` claim has explicitly taken: `error[E0308]` — `crate::runtime::_json_stringify` expects `&str`, gets `&HashMap<String,String>`, i.e. `jsonStringify(Map(...))` lowers to a call the runtime signature does not accept. NOTE the fix came via `rust-emit-reachable` ("emit what the entry point REACHES"), not the `rust-any-as-value` claim I was watching — that one is released. Do NOT hand-roll a token check to route around this; rejected on purpose (a second parser is what this slice exists to avoid). Resume when their E0308 lands: rebuild ssc-tools, re-run `build-rust clients/control/public-matrix.ssc`, and quote the SHA FROM THE BANNER — my earlier report was measured on a stale build and two of its three error classes were already fixed.
