feature/ucc-ssc-public 2026-08-08T17:13:53Z
agent: claude-opus-5
heartbeat: 2026-08-09T18:45:00Z
status: blocked
done-so-far: routes correct and proven on the run path; three run-vs-build-rust divergences measured, the third (generated Rust does not compile) closes the shipping path
next: json-core is FIXED and verified — on a toolchain built from scalascript `041a05328` (stamp == tree, banner silent) the six-line std/json repro builds AND RUNS: 155 rustc errors -> 0, prints {"a":"b"} / parsed. Their `rust-emit-reachable` landed it (NOT `rust-any-as-value`). STILL BLOCKED, but on a DIFFERENT and newly-reported defect: `public-matrix.ssc` gives 33 errors, none of them json — the rust lane passes unknown method names through verbatim. Minimised to three lines (indexOf / find / zipWithIndex: run lane prints them, build-rust emits `xs.indexOf(...)` which Vec has no such method for) and filed via their `scripts/inbox-add` as `list-methods-pass-through-to-rust`, repro committed, inbox-gate green, ON their main. The slice also needs `Value.get`, `Value.exists` and a `Value: From<Option<Value>>` bound. Do NOT hand-roll a token check around this. Resume when the method-lowering lands; quote the SHA FROM THE BANNER.
