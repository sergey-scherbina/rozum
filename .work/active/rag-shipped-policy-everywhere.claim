slug: rag-shipped-policy-everywhere
owner: claude (opus 5)
started: 2026-09-03
scope:
  - extract the shipped ranking policy (deep pool, balance, fuse, text fill-in,
    post-fusion rebalance) into ONE function; route all copies through it
  - fix the in-process agent tool (nadia), which uses raw BM25 with k=3 while both
    MCP surfaces use the fused path
  - give the Telegram/Discord meeting participant rag.search, opt-in per operator
    flag and gated on the same per-user read grant
