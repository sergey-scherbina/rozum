slug: gw-decompose-viewtokens
agent: claude (opus)
started: 2026-08-06
branch: feature/gw-decompose-viewtokens
worktree: .worktrees/feature/gw-decompose-viewtokens
scope:
  - crates/rozum-gateway/src/control.rs (extraction only, behaviour-preserving)
  - crates/rozum-gateway/src/private_store.rs, view_tokens.rs (new); paths.rs, matrix.rs, lib.rs
  - SPRINT.md
why: gw-monolith-decompose, third slice — the one the last commit named as removing a
     back-reference. Measured: view_token family = 9 items / 70 lines, calling exactly
     json_load, json_save_rbac, rand_token, ucc_site_dir. Those (+ atomic_write_private) are the
     0600 JSON store for sensitive control state; they go first so the chain points downward.
then: matrix.rs stops importing check_view_token from control.rs — one of its two named parent
      references disappears, which is the point of doing this slice next.
