feature/discriminating-task 2026-08-15T19:05:15Z
agent: claude-opus-5
heartbeat: 2026-08-15T19:05:15Z
status: in-progress
done-so-far: measured that the matrix cannot separate the two models — 9B 24/24, 4B 24/24, same
  24 cells, same conditions. The only measured difference is time: 9B is 1.84x slower overall and
  has a long tail (max 262s vs 71s). So a model decision needs a task that discriminates.
next: add one, designed rather than guessed. The current bug tasks are SIGNPOSTED (`// BUG:
  subtracts instead of adding`) and single-hop, which is why a 4B clears them. The new one puts the
  defect two calls below the failing test with no comment, and makes the half-fix fail a different
  case. Calibrate on the 4B first: aim for ~half, not 0/3 or 3/3.
