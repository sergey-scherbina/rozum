slug: gw-decompose-switchboard
agent: claude (opus)
started: 2026-08-05
branch: feature/gw-decompose-switchboard
worktree: .worktrees/feature/gw-decompose-switchboard
scope:
  - crates/rozum-gateway/src/gateway.rs (extraction only, behaviour-preserving)
  - crates/rozum-gateway/src/errors.rs (new)
  - crates/rozum-gateway/src/switchboard.rs (new)
  - crates/rozum-gateway/src/lib.rs (module decls)
  - SPRINT.md (gw-monolith-decompose entry)
why: gw-monolith-decompose. Measured: the Switchboard cluster (gateway.rs:123-939, ~815 lines) has
     ZERO references to GatewayState/ServeConfig/SwitchReq/Activity and nothing outside gateway.rs
     touches it. Only two leaks: error_json (already used by 4 files — it belongs in its own module,
     which the sprint entry itself asked for) and ModelSpec (3 external uses, moves with it).
net: gateway.rs 4386 → ~3520 expected. Tests are the net: rozum-gateway lib suite must stay green.
